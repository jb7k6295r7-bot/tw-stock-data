<!-- 這份文件與資料同版本控管。改資料結構就要同步改這裡。 -->
<!-- 任何專案都可以用 WebFetch 讀： -->
<!-- https://raw.githubusercontent.com/jb7k6295r7-bot/tw-stock-data/refs/heads/main/docs/READ_CONTRACT.md -->

# 資料庫讀取契約

`https://raw.githubusercontent.com/jb7k6295r7-bot/tw-stock-data/`

**適用對象**：任何要從這個資料庫取「數字」的專案或流程。
本文只講**怎麼讀不出錯**，抓取端的規則在 `tw-data-sources`，判讀門檻在 `tw-technical-analysis`。

---

## ⛔⛔ 讀本文之前：**要知道「有什麼檔」，先讀 `data/meta/_db_status.md`**

**本文回答「怎麼讀」，`_db_status.md` 回答「有什麼」。⛔ 不要把前者當後者用。**

`_db_status.md` 由 `db_status.py` **每趟自動產生**（檔頭有 `generated` 時戳），
所以它一定是當下的現況；而本文是人寫的，**會過期**。

⚠ 2026-09-09 實測代價：K線線據本文的路徑清單去找檔，
grep 不到某個欄位就推論「**資料庫沒有處置資料**」，一口氣報了三個錯誤結論
（處置、注意、交易日曆）——**而那三份檔案都存在**。
他自己的結案是：「**我用一個檔的欄位推論了整個資料庫**，
而只要先讀 `_db_status.md`，這三個錯一個都不會發生。」

⇒ **要斷言「資料庫沒有 X」，看現況清單，不是看本文、更不是看某一個檔有沒有那個欄位。**

---

## 零、界線：資料庫取代數字，不取代消息

- **量化欄位**（價格、量、籌碼、財報、股本）→ 讀資料庫
- **消息面** → **仍然走白名單搜尋 ＋ 14 天窗口 ＋ `sources/seen.md` 帳本**

資料庫裡沒有新聞，也不會有。把消息面接上資料庫是**做不到**，不是「還沒做」。

### ⚠ Rule 13 是專案綁定的

「不可用 Bash／curl 抓網頁繞過 WebFetch」的唯一例外，是使用者 2026-09-02
**針對本專案**放行的這個 repo 讀取。**其他專案要用，必須另外取得使用者同意**，
不可以自行沿用。第三方站台一律仍走 WebFetch。

---

## 一、路徑：用查詢的形狀決定

| 要回答什麼 | 讀哪裡 |
|---|---|
| **一檔的十年歷史（價格）** | `data/stocks/<代號>.csv` |
| **一檔的三大法人** | `data/stocks_inst/<代號>.csv`（**上市＋上櫃已合併**） |
| **一檔的融資融券** | `data/stocks_margin/<代號>.csv`（**上市＋上櫃已合併**，2026-09-07 新增）|
| **一檔的本益比／殖利率／股淨比** | `data/stocks_per/<代號>.csv`（**上市＋上櫃已合併**，2026-09-07 新增）⚠ 有兩個坑，見第五節 |
| **一檔的還原因子（除權息＋減資）** | `data/adj/<代號>.csv`（`event` 欄分辨是哪一種）|
| **一天的全市場**（篩選、排序） | `data/universe/daily/<日期>.csv` |
| 一天的全市場三大法人 | `data/universe/inst/`（上市）、`data/universe/otcinst/`（上櫃） |
| 一天的全市場融資融券 | `data/universe/margin/`（上市）、`data/universe/otcmargin/`（上櫃）。⚠ **表頭有兩代**，見下方欄位說明 |
| 一天的本益比／殖利率／股價淨值比 | `data/universe/per/`（上市）、`data/universe/otcper/`（上櫃） |
| 一天的除權除息計算結果 | `data/universe/exright/`（**只有上市**） |
| 一天的**減資**恢復買賣參考價 | `data/universe/reduce/`（**只有上市**，TWSE `reducation/TWTAUU`）|
| **興櫃的歷史** | `data/universe/esb/<代號>.csv` |
| **產業別／類股** | `data/meta/industry.csv`、`data/meta/sectors.csv` |
| **當期月營收** | `data/mops/revenue/<YYYY-MM>.csv` |
| **當期財報** | `data/mops/fs/<期別>_<業別>.csv`（損益）、`data/mops/bs/…`（資產負債） |
| 當日速查（追蹤股） | `data/latest/<代號>_price.csv` 等 |

**個股軸有五種**：價格、三大法人、**融資融券**、**本益比**、還原因子
（`data/stocks*`、`data/adj`）。
⚠ **除權息與減資仍然只有日期軸**（`universe/exright`、`universe/reduce`）——
要一檔的事件序列請讀 `data/adj/<代號>.csv`，那是從這兩個 feed 算出來的。

**用錯軸的代價**：拿日檔去湊一檔的歷史 = 掃 2,844 個檔、**0.55 GB**。
反過來拿 3,029 個個股檔去做單日篩選也一樣蠢。

### ★ 興櫃：看到一列不代表沒有歷史

**這是最容易誤判的一點。** 興櫃的歷史在 `data/universe/esb/`，**不在 `data/stocks/`**：

| 代號 | `data/universe/esb/` | `data/stocks/` |
|---|---|---|
| 5267 | **2,844 列**（2015-01-05 起，逐日） | **1 列**（2026-09-03） |
| 6434 | **2,844 列** | **1 列** |
| 7879 | 147 列（2026-01-23 上興櫃） | 1 列 |

成因：`fetch.py` 自 2026-09-03 才把興櫃納入 universe 日檔，先前的興櫃資料是
`--esb` 另外抓的。**兩邊欄位不同**——興櫃官方只給**高／低／均價**，沒有開盤與收盤，
硬合併就等於憑空生出兩個價格欄，所以刻意分開存。

讀興櫃時：`date, stock_id, high, low, avg_price, volume, amount, transactions, price_basis`，
`price_basis` 為 `無成交` 的列，價格欄是**空的**（不是 0）。

---

### ★ 產業別：三個會讀錯的地方

`data/meta/industry.csv`：`stock_id, name, market, industry_code, industry_name, listed_date`

**1. 類股是有層級的，不是互斥分類。**

- **13 電子工業（458）** ＝ 24 半導體(96) ＋ 25 電腦週邊(64) ＋ 26 光電(68) ＋ 27 通信網路(46)
  ＋ 28 電子零組件(104) ＋ 29 電子通路(23) ＋ 30 資訊服務(11) ＋ 31 其他電子(46)
- **07 化學生技醫療（88）** ＝ 21 化學工業(28) ＋ 22 生技醫療業(60)

母子關係記在 `sectors.csv` 的 `is_parent`／`children` 欄（由集合包含關係推出，非寫死）。

> **`industry.csv` 給的一律是子類股代碼。** 實測**代碼 07 是 0 檔**、13 也是 0 檔——
> 公司基本資料從不給母類股。**要排除生技醫療必須用 `22`，寫 `07` 會一檔都比對不到。**
> 「同一類股最多 N 檔」這種規則要先決定用哪一層，兩種選法結果差很多。

**2. 特別股不在裡面，要查母公司。**

在市的 27 檔 `kind=special`（1101B 台泥乙特、2002A 中鋼特…）**一檔都不在 `industry.csv`**。
特別股在行情端有自己的證券代號，但在公司基本資料裡併回母公司。
→ **去掉尾碼字母後用母公司代號查**（1101B → 1101）。

**3. 三個代碼沒有中文名稱（代碼本身有）。**

| 代碼 | 市場 | 檔數 | 狀態 |
|---|---|---|---|
| 32 | 上櫃＋興櫃 | 34 | ✅ **2026-09-09 補上：文化創意業**（來源見下） |
| 33 | 上櫃＋興櫃 | 7 | ✅ **2026-09-09 補上：農業科技業** |
| 91 | 上市 | 10 | 存託憑證（DR），MI_INDEX 沒有這個類股索引 |

> ★ **32／33 的名稱不是來自 `MI_INDEX`**（它只涵蓋上市類股），
> 是來自官方 **ISIN 證券編碼查詢**（`isin.twse.com.tw/isin/class_main.jsp`，
> 吃 `industry_code` 參數）。⛔ 三項判準全過才落地：
> ① 32 與 33 回**不同**清單（交集 0）⇒ 參數確實生效；
> ② 我方標 32 的 34 檔、33 的 7 檔**全部**落在對應清單裡（34/34、7/7）；
> ③ 該頁「產業別」欄的相異值**各只有一種**。
> 實測輸出在 `_tpex_probe.txt` 第 11 節，落地處是 `industry.py` 的 `EXTRA_NAMES`。
> ⛔ 它**只在 `MI_INDEX` 沒有名稱時才用**，永遠不覆蓋官方指數表的用字——
> 兩邊用字可能不同（櫃買把 17 叫「金融業」、上市表寫「金融保險」），
> 而「代碼對、名稱錯」是安靜的錯。

> ★ **91 不是類股，是證券種類。**（2026-09-08 實測推定，**非官方明文**）
> 兩個方向都對得起來：`industry_code == 91` 的**恰好 10 檔，名稱全部以 `-DR` 結尾**；
> 所有 `-DR` 結尾的檔，代碼**全部**是 91。而 `sectors.csv` 的 34 個類股裡**沒有 91**——
> 證交所《上市公司產業類別劃分暨調整要點》列的 33 種平行類別也不含它。
>
> 所以「91 查不到類股名稱」不是缺資料，是**問錯問題**：它本來就不在類股那一層。
> 要辨識 DR 用**名稱的 `-DR` 後綴**（10/10 命中，比代碼更直接）。
> ⚠ 這個對應關係與母類股 13／07 一樣是**從我方資料實測出來的，官方沒有這樣寫過**，
> 引用時要標明。

**不要用成員名單去猜名稱。**（91 的 10 檔 DR 與 `capital.py` 的 9 檔 DR mismatch 是同一批標的。）

**4. 興櫃不在 `industry.csv` 裡，另外一個檔。**（2026-09-09 新增）

`industry.csv` 的類股名單取自 TWSE `MI_INDEX`，**只涵蓋上市與上櫃**。
興櫃 364 檔在裡面**一列都沒有**——這不是漏填。

→ 興櫃的產業別在 **`data/meta/industry_esb.csv`**
（`stock_id, name, market, industry_code, industry_name, source, asof`），
來源是櫃買 `mopsfin_t187ap03_R` 公司名冊的 `SecuritiesIndustryCode`，
由 `industry_esb.py` 每趟 `capital.yml` 重建。

> ⛔ **為什麼不併進 `industry.csv`**：那個檔在多處被當成**母體**用
> （`parvalue_scan.py` 的 `in_universe`、`feeds.py` 的 `known`、
> 「ETF 不在母體裡所以要全收」）。併進去會讓母體突然多 363 檔，
> **而那些判斷不會報錯，只會安靜地改變**。
> ⇒ 要用興櫃產業別的人**明確去讀那個檔**。

> ★ **代碼是同一套編碼，這件事驗過**：拿上櫃那份名冊的
> `SecuritiesIndustryCode` 去對 `industry.csv` 的 `industry_code`，
> 2026-09-09 實測 **890 檔相同、0 檔不同**。
> 這個檢查每趟都重跑，掉下 100% 會報 ✗——所以名稱表才敢共用。

> ⚠ 363 檔裡有 **11 檔的 `industry_name` 是空的**，它們的代碼是 **32／33**
> ——**正是上表那兩個沒有中文名的代碼**。兩個缺口是同一個缺口。
> ⛔ 空著是對的，**不要填「其他」**。

> ⚠ 名冊比我方少一檔（4150 優你康）。缺席的正常解釋是
> **已轉上市櫃或終止興櫃**，⛔ 不用多天聯集去補——
> 那會讓退場的公司永遠留在名單裡。差異會列在 `_last_run.md`。

**4. 興櫃完全沒有產業別。** 兩個基本資料端點只涵蓋現存的上市櫃公司。
下市股同理——那不是漏抓。

**覆蓋率**：在市的上市 1,084／1,084、上櫃 876／876，**都是 100%**。

---

### ★ 月營收與財報：三件事

`data/mops/`。前五欄一律是 **`stock_id, name, period, market, 報表日期`**（正規化欄），
原始欄位保留在後面。

> ⛔ **一律讀正規化欄，不要讀原始欄。**（2026-09-08 新增第五欄 `報表日期`）
> 來源端**每一張表、每個市場用中文還是英文欄名是不固定的，同一列還會混用**。
> 2026-09-08 實測：
>
> | 檔 | 上市 | 上櫃 |
> |---|---|---|
> | `fs/2026Q2_ci.csv` | 全中文（`出表日期`／`年度`／`季別`…） | **全英文**（`Date`／`Year`／`Season`…） |
> | `fs/2026Q2_bd.csv` | 全中文 | **同一列混用**：`Date`＋`年度`＋`季別`＋`公司代號`＋`CompanyName` |
> | `bs/2026Q2_bd.csv` | 全中文 | 全中文 |
>
> 所以「上櫃用英文、上市用中文」這個規則**不存在**——讀 `年度` 這一欄，
> 同一個市場在 `ci` 是空的、在 `bd` 有值。**空白不代表沒有那個資料，
> 只代表那天來源用了另一組欄名**，而它不會報錯。
>
> 正規化欄已經涵蓋全部五個：年度與季別 → `period`；公司代號與名稱 →
> `stock_id`／`name`；出表日期 → `報表日期`。

**1. 只有「最新一期」，沒有歷史。**

這些端點**沒有日期參數**。所以資料是**從 2026-09-06 開始逐期累積**的——
選股（當期營收年增、毛利率）夠用，**回測沒有歷史可用**。
要個股的歷史月營收，得用 FinMind 逐檔抓。

**2. 財報按業別分表，不要期待一張表打天下。**

| 業別 | 檔名後綴 | 2026Q2 檔數 |
|---|---|---|
| 一般業 | `_ci` | 上市 1,048／上櫃 882 |
| 銀行 | `_basi` | 上市 10 |
| 證券 | `_bd` | 上市 3／上櫃 7 |
| 保險 | `_ins` | 上市 5 |

**欄位意義完全不同**——銀行是「利息淨收益、存放央行及拆借銀行同業」，
一般業是「營業收入、營業成本、營業毛利」。合併是錯的。
月營收所有業別共用同一組欄位，所以只有一檔。

> **上市與上櫃的原始欄名不同**：上市用 `公司代號`／`年度`／`季別`，
> 上櫃用 `SecuritiesCompanyCode`／`Year`／`Season`。
> **一律用正規化的 `stock_id`／`period`**，用原始欄名會只拿到一半而且看不出來。

**3. `_changes.log` 記的是財報更正，不是雜訊。**

`data/mops/_changes.log` 逐欄記錄同一期別的內容變動。**財報是會更正的**——
若你引用過某期的數字，看到那一期出現在 log 裡就要重新對帳。

**涵蓋率**：月營收上市 99.1%／上櫃 99.9%；財報一般業上市 95.8%／上櫃 99.1%。
缺的多半是當期未申報或屬於未涵蓋的業別分表。

---

## 二、權威來源：重複時誰贏

`data/stocks/`、`data/history/`、`data/latest/` 都可能有同一檔的價格。

- **歷史與回測 → 一律 `data/stocks/`**（唯一權威）
- **當日速查 → `data/latest/`**
- **`data/history/<代號>_price.csv` 建議停用**：它與 `data/stocks/` 完全重疊
  （2026-09-04 實測 3017 重疊的 405 天逐欄相同），但**由不同程式維護，會無聲飄移**。
  它還有價值的只剩 `_revenue`／`_margin`／`_per` 那些尚未轉置的欄位。

---

## 三、基準日：用讀的，不要推算

`data/latest/_manifest.json` 自己寫得很清楚：

> **`date_max` 才是真正的資料基準日；`target_trading_day` 只是查詢用的猜測值。**

- **不要用執行環境的系統時鐘推論「今天應該有資料」**——沙箱時鐘實測差過一天。
- 抓取排程是**台北 19:00 與 23:59（週一～週五）**。
  **盤中讀到的一定是前一交易日收盤**，這不是錯，但報告要寫出來。
- 23:59 那班會**修正** 19:00 的錯漏，被改的列寫進 `data/_changes.log`。
  跨越那個時點的分析要重新對帳。

---

## 四、讀取端的三個踩雷（都實測踩過）

**1. raw 的 404 會被 CDN 快取，而且 `?query` 破不掉。**
2026-09-04 實測：`data/stocks/_index.csv` 連查三次都 404，檔案其實在。
raw.githubusercontent **會忽略未知的 query 參數**，所以 `?r=亂數` 完全無效。
→ **改用 `refs/heads/main/...` 路徑形式**：
`https://raw.githubusercontent.com/<owner>/<repo>/refs/heads/main/<path>`

這是「查不到」的三種情況裡最容易被誤記成「官方確實沒有」的那一種。

**2. 不要用 WebFetch 讀大 JSON。** manifest 自己警告過：會被截斷，而且**被憑空補齊**，
看起來像完整的。要讀就讀 CSV。

**3. 檔案存在不等於欄位有值。** 例：`capital.csv` 的面額欄三個端點一律是空字串。

---

## 五、會改變結論的限制，引用時必須揭露

| 限制 | 影響 |
|---|---|
| **`data/stocks/` 的價格全部是未還原價** | 要還原自己乘 `data/adj/` 的因子，見下方 |
| **上櫃的還原因子來自第三方（FinMind），不是官方** | **引用上櫃因子時要揭露來源不同**；`_index.csv` 的 `market` 欄是 `tpex` 的就是這一批。⛔ 這一列原本寫「TPEx 沒有歷史來源（`bulletin/revivt` 是純前瞻公告表）」——**2026-09-10 撤回**，見下 |
| **`data/adj/` 查不到某檔＝那一檔沒有事件**（不再是「上櫃就沒有」）| 上市上櫃現在都有因子。查不到就是真的沒有除權息也沒有減資 |
| ~~融資融券、本益比只有日期軸，沒有個股軸~~ | **2026-09-07 已解除**：`data/stocks_margin/`（2,426 檔、5,083,638 列）與 `data/stocks_per/`（2,121 檔、4,919,397 列）都已轉置，兩者皆上市＋上櫃合併 |
| ⚠ **`stocks_per` 的 `close` 欄不可當價格** | **上櫃全期空白**；上市 **2015-01-05 ~ 2017-04-12 空白**（551 列）。價格一律讀 `data/stocks/`。8 檔抽驗（上市 4、上櫃 4）結果完全一致 |
| ⚠⚠ **`fs_quarter` 的格式上市與上櫃不同** | 上市寫 `115/2`、上櫃寫 `115Q2`。**只寫一種解析式，另一半市場會靜默失敗**——parse 不到就當成「沒有財報期別」，而不會報錯。詳見下方 |
| **股本 32 檔對不起來** | 9 檔 DR ＋ 23 檔非 10 元面額（代號帶 `*`），維持 mismatch 是刻意的。2026-09-07 上櫃股本補進來之後，多出來的 7 檔全部也帶 `*`——**判準是名稱有沒有 `*`，不是檔數** |
| ⚠ **上櫃有 127 檔的股數與股本來自不同日期** | `shares` 取自 daily（每日更新），`capital` 取自 `tpex-mopsfin-O`（較舊的快照）。`note` 會寫「股數以 daily 為準=…；股本取自…的較舊快照」。**這不是錯誤**，是可轉債轉換與員工認股讓股數持續長大。要算佔股本比重用 `shares`；要精確的 資本額÷股數 就別跨這兩欄 |
| **成交金額 ÷ 股數 ≠ 均價** | 官方兩欄統計範圍不同（金額含盤後定價與鉅額），全市場約 **0.2%** 的股票隱含均價會落在當日高低之外。要均價用高低中值或收盤 |
| **★ 日檔只收「當天有成交」的證券** | 某檔在某個交易日**沒有那一列 ≠ 漏抓**，是那天沒成交（無成交或停牌，本庫分不出這兩者）。詳見下方 |
| ~~月營收與財報只有 `fetch.py` 清單內的股票有~~ | **已解除**（2026-09-06）：`mops.py` 走全市場端點、`mops_history.py` 回補 2015 起。當初判它「靜默截斷」是 **WebFetch 的限制，不是端點的性質** |

### ★★ 日檔沒有那一列，不代表漏抓（2026-09-07 稽核確認）

**`data/universe/daily/<日期>.csv` 只包含那一天「有成交」的證券。**

實測（2018-12-22／2018-11-05／2022-10-28／2026-09-04 四天，上市＋上櫃共 7,890 列）：
**`volume == 0` 的列一列都沒有**，最小成交量永遠 ≥ 100 股。

所以某檔在某個交易日查不到那一列，代表的是**那一天它沒有成交**——
可能是無成交（冷門股），也可能是停牌（全額交割、重大訊息、減資換發新股）。
**本庫分不出這兩者**，因為沒有停牌公告來源。

#### 這會影響什麼

任何「每個交易日每檔都該有一列」的計算都會錯：

- **均線、扣抵值、OBV、ATR、周轉率**：序列有洞。要決定是補值、跳過還是縮短視窗，
  **那是判讀方法**（屬「K線趨勢模型分析」專案），不是資料面能替它決定的。
- **法人連續買賣超天數**：中間夾一個無成交日時，「連續」怎麼算會改變答案。
- **前一交易日收盤**：`adjust.py` 的前收盤交叉核對就是因此有 **44 個事件無法核對**——
  事件前一個交易日該檔沒成交，序列裡沒有那一列。**那不是不符。**

#### 規模（2026-09-07 全庫稽核）

逐檔在首末之間的缺漏共 **50,922 段**，其中 537 段前後有該檔的減資事件。
最集中的日子落在 **2018 Q4 與 2022 Q4**（單日 60~111 檔，約當天 3~6%）——
兩段都是空頭，冷門股整天沒成交的機率上升，與上面的性質一致。

最長的幾段也對得上：`00643K 群益深証中小+R` 自 2021-11-24 起連續缺 686 個交易日，
缺漏前後兩端**名稱相同**（不是代號被回收再掛牌），而它有成交時的量只有 4,000～14,000 股，
是一檔近乎休眠的 ETF。`4414 如興`、`6131 悅達`、`4413 飛寶` 這些則是長期停牌。

### ★ 還原價怎麼用（差一天就整段錯）

- 單次因子 `f = 參考價 ÷ 事件前收盤價`
  - 除權息：`除權息參考價 ÷ 除權息前收盤價`，幾乎一定 ≤ 1
  - **減資**：`恢復買賣參考價 ÷ 停止買賣前收盤價`，**一定 > 1**（股數變少、每股價跳上去）
- `F(d) = 所有事件日 **嚴格大於** d 的因子連乘`；**還原價 = 原始價 × F(d)**
- 最新價的 F = 1，**現價不動**，被調整的是歷史

**`cum_factor` 適用於「該列日期**之前**」，不含當日**——事件當日的收盤本身已經是
事件後的價，再乘一次就重複扣。查法照抄：

```python
def factor_at(rows, d):        # rows 已按日期升冪
    for r in rows:
        if r["date"] > d:      # 第一個「除權息日 > d」的事件
            return float(r["cum_factor"])
    return 1.0                 # 最後一次事件之後 → 不動
```

> **因子不是一定 ≤ 1。** 兩種情形：
> ① 現金增資認股價高於市價時，理論除權參考價會**高於**前收盤
>   （實例：3312 弘憶股 2016-04-14，f=1.0017，`權值+息值` 為負）。約 0.09%，是合法事件。
> ② **減資的因子本來就 > 1，而且可以很大**——3536 誠創 2015-03-20 前收 6.58 →
>   參考 13.33，**f = 2.026**。減資九成的話會接近 10。
>   拿「因子應該 ≤ 1」去過濾，等於把減資整批丟掉。

#### ⛔⛔ `kind` 欄有**兩套詞彙**，而它們**按市場切開**（2026-09-13 全庫實測）

```
上市（TWSE）  息 / 權 / 權息
上櫃（TPEx）  除息 / 除權 / 除權息
```

⇒ ⛔ **`kind == "權"` 這種寫法會靜靜地只拿到上市**，上櫃整族掉光。
⚠ 而它不會報錯、不會少檔、不會缺欄——只是那一群**不在結果裡**。

實測（2,375 個 `data/adj/` 檔，其中 2,329 個有除權息事件）：

```
1,229 檔只有「息/權/權息」（1,229 檔全是上市）
1,074 檔只有「除息/除權/除權息」（1,072 上櫃 ＋ 2 上市）
   26 檔兩套都有（25 上市 ＋ 1 上櫃）
```

逐市場逐 kind（共 25,300 筆有效事件）：

```
上市 12,260   息 10,183  除息    101 ｜ 權 653  權息 913  除權  35  除權息    62
上櫃 13,040   息      1  除息 10,174 ｜ 權   1  權息   0  除權 925  除權息 1,644
```

⇒ 帶「權」的事件全庫是 **4,233 筆（16.7%）**——上市 1,663、上櫃 2,570。
⛔ 只數上市那一套會得到 1,567，**少 2.7 倍**。

##### ⇒ 怎麼讀才對

```python
# ⛔ 錯：會靜靜地只拿到上市
if r["kind"] == "權": ...

# ✓ 對：比「有沒有那個字」，⛔ 不是比整串相等
has_share = "權" in r["kind"]     # 權／權息／除權／除權息 全中
has_cash  = "息" in r["kind"]     # 息／權息／除息／除權息 全中
```

⚠ 而減資那一族（`退還股款`／`現金減資`／`彌補虧損`）兩市場**用同一套詞**，
⛔ 不要因為上面這條就以為每一欄都有兩套。

#### ⭐⭐⭐ F2 那 21 筆：**21/21 全部有官方依據，「我方因子錯」＝ 0 筆**（2026-09-14 定案）

```
✅ 上市 12 筆   官方逐筆漲跌停 ⇒ 收盤在官方漲停之內（多筆是**剛好鎖漲停**）
✅ 上櫃  6 筆   官方櫃買公告區歷史表 ⇒ **前收與參考價逐位相同** ⇒ 因子＝官方參考價回推
✅ ETF   3 筆   無漲跌幅限制的證券真的大跌（2020-03-19 崩盤日、00663L 分割）
```

##### ⛔⛔ 而上櫃那 6 筆能判，是因為我又一句否定句是錯的

我 2026-09-14 寫過「上櫃官方只給當日，補不回來」。
⛔ **錯。** `data/meta/otc_exright_history.csv` **早就存在**：13,106 列、**2008 年起**、
來源是櫃買公告區 `bulletin/exDailyQ`（一次請求回十一年半）。

⚠ 而 `otc_exright_history.py` 的檔頭**自己就寫著**同一個教訓：
> 我對三個地方寫過「上櫃除權息的歷史官方來源：仍然沒有」……⛔ 那句話是錯的。

⇒ ⭐ **這是同一句否定句在這個 repo 裡第四次被推翻。**
⛔ 而前三次的紀錄就在我要動的那個檔的檔頭裡——**我沒讀它就又寫了一次**。

##### 上櫃那 6 筆的逐筆對照

| 代號 | 事件日 | 我方 前收／參考 | 官方 前收／參考 | |
|---|---|---|---|---|
| 2736 | 2024-05-28 | 27.9／26.58 | 27.90／26.58 | ✅ |
| 4991 | 2025-12-22 | 192.5／189.42 | 192.50／189.42 | ✅ |
| 3562 | 2017-03-16 | 14／10.9 | 14.00／10.90 | ✅ |
| 2718 | 2018-11-28 | 20／18.59 | 20.00／18.59 | ✅ |
| 5468 | 2021-01-27 | 13.6／12.56 | 13.60／12.56 | ✅ |
| 5301 | 2015-08-12 | 51.4／44.9 | 51.40／44.90 | ✅ |

---

#### ⭐ 而順手量到一件：**兩站的參考價捨入規則不同**

```
上市（TWSE  exright，11,751 列）  ref 較高 **0.00%**   ⇒ 單邊 ⇒ **無條件捨去**
上櫃（櫃買公告區，13,106 列）      ref 較高 14.15%｜較低 10.77%｜最大 ±0.0050
                                  ⇒ 兩邊都有 ⇒ **四捨五入**
```
⇒ ⭐ 所以 `cum_factor` 的單邊漂移**只發生在上市**，上櫃沒有東西要修。
⚠ 而我原本把上櫃那個雙邊分佈歸咎於「FinMind 自己在捨入」——⛔ **冤枉它了**：
拿官方表量出同樣的 14.15%／10.77% ⇒ **FinMind 是忠實反映官方的**。

---

#### （以下為 2026-09-14 較早的重判紀錄，保留）

回測線的 F2（事件日收盤 ÷ 參考價 出 `[0.895, 1.105]`）抓到 21 筆。
⭐ 官方逐筆 `漲停價格` 回補下來之後**全部重判一次**：

```
✅ 在官方漲跌停之內  12 筆   ← **全部是上市**，⇒ 我方因子**沒有問題**
⚠ 未證實             6 筆   ← **全部是上櫃**（TWT49U 只涵蓋上市，沒有官方數字可比）
✅ 真的行情          3 筆   （無漲跌幅限制的 ETF：00712／00837B 2020-03-19、00663L 分割）
```

| 原本的分組 | 上市（官方判） | 上櫃（未證實） |
|---|---|---|
| 證據①「漲跌停」7 筆 | 5：910482／6224／8033／9105／2464 ✅ | 2：2736／4991 |
| 證據②「現增」7 筆 | 3：9928／2243／1475 ✅ | 4：3562／2718／5468／5301 |
| 未判定 4 筆 | 4：911608／2429／6225／7610 ✅ | 0 |

⇒ ⛔ **「我方因子錯」在有官方數字的範圍內是 0 筆。**

##### ⛔⛔ 兩條證據**都**被推翻了，而它們錯在同一個地方

```
證據①「成交價不可能超出 ref × 1.10」
   ⇒ ⛔ 漲停的基準是 `減除股利參考價`，不是 `除權息參考價`
證據②「現增是股東付錢買新股 ⇒ 每股價值不該掉 ⇒ 官方套錯了式」
   ⇒ ⛔ 官方公式**本來就含現增那一項**：
        除權息參考價 = (前收 − 息值 + 現增認購價×現增配股率)/(1+無償配股率+現增配股率)
      ⇒ 參考價確實會掉，而那不是「套錯式」
```

⭐ 共同點：**兩條都是我自己推的，而我沒有拿官方的同一個量去對過。**
⚠ 而它們一起指向同一族案子（大額現金增資除權），所以互相「印證」——
⛔ 那不是兩個獨立判準，是**同一個錯誤前提的兩種寫法**。

⭐ 而典型的樣子是：**收盤剛好等於官方漲停**（2429 42.75、6225 48.80、
7610 2425.00、1475 51.80 都是逐位相同）⇒ 那天鎖漲停，不是資料錯。

##### ⛔⛔ 證據①（交易所漲跌停）**已作廢**——官方的數字自己推翻了它

原本寫的是：「成交價不可能超出 `ref × 1.10` ⇒ 超出了就**證明**參考價不是我方那個。」

⚠ 2026-09-13 把官方逐筆的 `漲停價格` 補存下來之後，那句話**對現金增資除權不成立**：

```
910482 2017-06-19  我方 ref 0.58    官方漲停 0.82    收 0.75   ⇒ ✅ 在內
6224   2021-01-13  我方 ref 101.05  官方漲停 113.00  收 113.00 ⇒ ✅ 剛好鎖漲停
8033   2023-03-06  我方 ref 55.24   官方漲停 62.30   收 61.50  ⇒ ✅ 在內
9105   2023-10-06  我方 ref 2.91    官方漲停 4.49    收 3.44   ⇒ ✅ 在內
2464   2026-09-02  ——官方數字回來之後也在內
```

⇒ ⭐ 成因：**漲停的基準是官方的 `減除股利參考價`，⛔ 不是 `除權息參考價`**
（2353 宏碁 2015-01-05 是逐位證據：漲停 22.80 ＝ ⌊21.35 × 1.07⌋）。
含現增的案子兩者差很多 ⇒ 官方讓價格從**比較高**的那個基準往上走。

⇒ ⛔ **那 7 筆裡，5 筆（全是上市）被官方數字清掉；另外 2 筆是上櫃**
（2736 高野 2024-05-28、4991 F-環宇 2025-12-22），⚠ 而 TWT49U 只涵蓋上市
⇒ 它們仍然只有那個**已被推翻的推論** ⇒ ⛔ 標成「未證實」，不是「已證明因子錯」。

⭐ 而 `factor_limit_check.py` 現在**官方優先**：母體 20,207 筆裡官方判 11,106 筆，
超出剩 **2 筆**（就是那兩筆上櫃）。

**② 股數大增、而 factor ≠ 股數比 ⇒ 那次增股是【有償】的（現增）**（7 筆）

現增是股東**付錢**買新股 ⇒ 每股價值不該掉 ⇒ 官方那個「除權參考價」套錯了式。

```
代號   日期          我方 f    股數比    f ÷ 股數比
3562  2017-03-16   0.7786   0.2253      3.455
9928  2017-04-06   0.8695   0.4692      1.853
2718  2018-11-28   0.9295   0.5303      1.753
2243  2023-01-30   0.9721   0.6968      1.395
5468  2021-01-27   0.9235   0.7125      1.296
5301  2015-08-12   0.8735   0.7832      1.115
1475  2021-04-15   0.9345   0.8485      1.101
```

⚠ **7610 2026-07-13 不在這一族**：它的 `f ÷ 股數比 = 0.988` ⇒ 那次**是**無償配股
⇒ ⛔ 它留在「未判定」。

##### ⇒ 讀的人要怎麼用

```
⛔ 這 14 筆的 cum_factor 在事件日**之前**整段都偏掉（連乘）
⚠ 逐筆清單：data/meta/_factor_limit_check.csv（那 7 筆）
⭐ 而 daily.yml 每天跑 `factor_limit_check.py`：**筆數不可以變多**
```

#### ⭐⭐ 減資因子有**第三個**來源，而它把「誰是異類」翻過來了（2026-09-13）

上櫃官方減資表 `data/meta/otc_reduce_history.csv` 有一欄 **`shares_per_1000`（換股比率）**
——⭐ 它**不是從價格推的**，跟官方參考價是**兩欄分別公告**的。

```
官方【換股比率】與官方【參考價】互相一致：223 / 233
⇒ 兩個獨立公告的欄位對得上 ⇒ **價格那一邊沒問題**
```

⇒ 而那 17 筆「我方 `shares` 算出的換股比對不上官方參考價」的，**17 筆全部**是
官方換股比率**站在參考價那一邊**：

```
代號   日期          官方換股比率   價格隱含    我方 shares
6241  2026-08-25     0.729644    0.729715    0.934983
4502  2015-11-23     0.360000    0.360065    0.552632
3310  2016-10-20     0.750000    0.750422    0.359528
（其餘 14 筆同樣形狀，見 data/meta/_reduce_shares_check.csv 的 `keep_ratio_official` 欄）
```

⇒ ⛔ **異類是我方 `shares`，不是還原因子。**
⚠ 推定成因：**減資之後在同一個停牌窗裡又增資**，而我方 `shares` 讀到的是增資後的數字
（⛔ **未證實**——我還沒找到獨立的增資股數來源）。

##### ⇒ 這對讀 `shares` 的人代表什麼

```
✅ 還原因子（data/adj 的 factor／cum_factor）  ⇒ 這 17 筆**沒問題**
⛔ `shares` 欄在**減資事件日前後**            ⇒ 可能跨過了不只一件事
   ⇒ 拿它算「這次減資的換股比」會偏大
```

⇒ 要換股比就讀官方那一欄（上櫃）；⚠ 上市沒有對應的官方表，
⛔ 那 303 筆目前只有 `shares` 一個第二來源。

#### ⛔⛔ 「除權」把**兩件事**綁在一起，而兩者的算式不同（2026-09-13 實測）

台灣的「除權」同時涵蓋**無償配股**與**現金增資認股**：

```
無償配股（盈餘／資本公積轉增資）  ref = 前收 ÷ (1 + 配股率)
   ⇒ 純稀釋，factor **等於**股數比（shares 前 ÷ shares 後）
現金增資認股                      ref = (前收 + 認購價 × 認購率) ÷ (1 + 認購率)
   ⇒ 股東**要付錢** ⇒ factor **接近 1**，⛔ 而股數可以翻好幾倍
```

⭐ 這跟減資那兩式（彌補虧損 vs 退還股款）是**同一族**：一邊拿錢、一邊付錢。

⇒ 實測（1,211 筆「權／除權」，事件後 40 個交易日內股數確實變過）：

```
factor ÷ 股數比　中位數 1.0697　只有 343 筆（28.3%）落在 ±0.5%
在「股數 3 個交易日內就變了」那 628 筆裡：
    438 筆 factor > 股數比　只有 7 筆 factor < 股數比   ← ⭐ 單邊，不是隨機誤差
```

⇒ ⛔ **不可以拿「股數比」單獨去驗除權因子**——那 72% 不是因子錯，是少了現增那一式。
兩個實例（兩筆都是現增，股東付了錢 ⇒ 價格本來就不該掉）：

```
3562  2017-03-16 除權   shares 13,090,000 → 58,090,000（×4.44）  收 14.00 → 14.00
2718  2018-11-28 除權   shares 22,576,000 → 42,576,000（×1.89）  收 20.00 → 20.95
```

⚠ 而「權息／除權息」那 1,680 筆的 `factor ÷ 股數比` 中位數是 **0.9771**
⇒ 隱含 2.29% 的現金殖利率 ⇒ **股數那一半對得上，現金那一半也落在對的量級。**

#### ★★ 面額變更：`data/adj/` 完全沒有涵蓋（2026-09-08 全庫掃描確認）

`data/adj/` 只有 **exright（除權息）** 與 **reduce（減資）** 兩種事件，
**變更股票面額一筆都沒有**。面額 10 元改 1 元 → 股數十倍、股價十分之一，
那一天的收盤跳躍**與股價無關，是計價單位換了**。用還原價算長區間報酬時，
那一筆會變成單筆 −90% 這種量級的**假報酬，而且不會報錯**。

`tw-stock-db` skill 陷阱 13 給了判定規則（收盤比 `< 0.55` 或 `> 1.8`
且因子表無對應事件），但註明門檻與「約 26 筆」是移交來的、資料庫線沒複核過。
**`parvalue_scan.py` 2026-09-08 做了那次全庫掃描**（報告在
`data/meta/_parvalue_scan.md`）：3,030 檔、5,524,021 個相鄰對。

| 分類 | 筆數 | 證據 |
|---|---:|---|
| 股數是整數倍（上櫃日檔有 `shares`） | **14** | ⭐ 三個訊號，**倍數說得出來** |
| 只有比值＋名稱＋停牌天數（上市沒有 `shares`） | **10** | 三件事一致，但缺倍數 |
| 成因未查明（8101 華冠） | 1 | **不歸類**，待查 |
| **視為面額變更** | **24** | 移交說「約 26」，量級對得上 |

**怎麼認（依證據強度排序）：**

1. ⭐⭐ **`shares` 欄的倍率**——面額 10→1 ＝ **股數 ×10、股價 ÷10**。
   實測 14 筆的倍率全是乾淨的整數比（×2.00、×2.50、×4.00、×10.00、×20.00），
   而且**與收盤比互為倒數**。價格比值只能說「跳了」、名稱只能說「現在是非 10 元
   面額」，**只有股數說得出倍數**——這是三個訊號裡唯一能定量的。
   ⚠ **只有上櫃日檔有 `shares`，上市是空的**，所以它只涵蓋得到一半。
2. **證券名稱末尾的 `*`**——TWSE／TPEx 用它標「非 10 元面額」，
   `data/stocks/` 每一列都帶 `name`。
   ⛔ **它標的是「現在是不是非 10 元面額」，不是「剛剛換過面額」**——
   一檔股票拿到 `*` 之後**再換一次面額，名稱不會再變**，
   所以「`*` 出現」這個訊號**只抓得到第一次**（24 筆裡 13 筆）。
3. **收盤比 `< 0.55`**——實測 24 筆的範圍是 **0.045 ~ 0.544**。
   ⚠ 最接近門檻的 6613 朋億* 是 0.544，**門檻只要降到 0.54 就會漏掉它**。
4. **隔 6~8 個交易日**——換發股票要停止買賣，所以面額變更**幾乎不會出現在
   相鄰交易日**。⚠ 反過來說，「隔 1 個交易日」那一類**不是**面額變更。

**⛔ 四個會把這條用錯的地方：**

- **`*` 沒出現 ≠ 沒有面額變更**，但原因不是「名稱欄不可靠」。
  ⚠⚠ **2026-09-08 第一版在這裡寫錯過，訂正如下**：當時看到 8070 長華、
  8422 可寧衛、2327 國巨在檔案第一列就帶 `*`，就推論 `name` 欄是回填的當期名稱。
  **那是錯的**——去看來源日檔 `data/universe/daily/` 就推翻得掉：
  6548 在 2016-09-13 是 `長華科`、2019-01-08 是 `長科`、2019-09-09 是 `長科*`，
  **日檔保留的是當日名稱，沒有回填**。8070 一開始就有 `*`，是因為它在資料起點
  （2015-01-05）之前就已經是非 10 元面額了。
  → 真正的原因是**「`*` 只在第一次換面額時出現」**（見上面第 2 點）。
  名單裡就有現成的證據：**6548 長科出現兩次**——2019-09-09（`*` 出現、股數 ×10）
  與 2022-09-05（`*` 沒變、股數 ×2.5）。
  ⛔ 教訓：**「檔案第一列就長這樣」不能拿來推論欄位語意**，
  要去看寫入這個欄位的來源檔——那才是直接證據。
- **事件要用「區間」對，不能用「同一天」對。** `data/adj/` 的事件日
  **不保證是交易日**——3293 鈊象 2024-07-24 除權息當天颱風停市，
  沿著交易日曆去數容錯天數永遠數不到它，於是一筆正常的除權息被誤判成面額變更。
  正確判準：事件日落在 `(前一次有成交的日子, 這一次有成交的日子]` 之內。
- **上緣 1.8 目前一筆樣本都沒有**，因為本庫 2015 起的面額變更全是「面額變小」。
  面額由小改大會落在那一側，**不要因為沒樣本就把上緣拿掉**。
- **ETF 沒有漲跌幅限制的那一類會混進來。** `data/stocks/` 3,030 檔裡只有
  1,984 檔在 `industry.csv` 母體內；追蹤國外標的的 ETF（00672L、00887）
  單日就能跳超過 ±10%，**「±10% 上限」這個論證對它們不成立**。先篩母體。
  ⚠ 另外，±10% 是 **2015-06-01 起**的制度，之前是 ±7%；本庫從 2015-01-05 起算，
  最前面那五個月適用的是舊上限。（不影響上面的結論，但引用這個論證時要知道。）

⚠ **這 24 筆目前仍然只是「讀取端的人工檢查」，沒有寫成資料欄位、
也沒有進 `data/adj/`。** 要變成自動還原，得先有面額變更的官方事件來源
（`parvalue_probe.py` 正在驗 TWSE `change/TWTB8U`，2026-09-08 那趟被限流沒答出）。
**在那之前不要假裝它有被還原。**

#### ★ 上櫃因子的來源與憑據（2026-09-06 併入）

上櫃走 **FinMind**（`TaiwanStockDividendResult` 與
`TaiwanStockCapitalReductionReferencePrice`），因為 TPEx 官方沒有歷史。
憑什麼信它，有兩層獨立驗證：

1. **拿上市那一半對官方**：抽 60 檔逐筆比 TWSE 的 11,729＋302 筆事件——
   除權息 555/555 相符、減資 24/24 相符、**不符 0.00%**，
   且「只有我方有」= 0（FinMind 沒漏事件）。
2. **★ 全量對我們自己的收盤價**：`adjust.py` 把上櫃的 7,124＋232 筆事件的
   「前收盤」逐筆去對 `data/stocks/`——**19,340 筆核對、0 不符**。
   這一層是全量、而且對的是交易所原始行情，不是同一個第三方。

⚠ **除權息的參考價欄是 `after_price`，不是 `reference_price`。**
後者在「權」（股票股利）的紀錄裡等於前收盤，用它算出來的因子會是 1.0，
等於**股票股利完全不還原**且不會報錯。這是交叉驗證抓出來的（第一次驗是 5.41% 不符）。

#### ⛔⛔ 上櫃的 adj **不是每天更新的**——用之前先看那份判準檔

上櫃的除權息／減資因子走 FinMind（`otc_adj.py`），
而那一支是**手動的幾小時全掃**（971 檔 × 2），⛔ **不在每日排程裡**。
⇒ 一筆新的上櫃減資發生之後，`data/adj/` 可能好幾天沒有它，
⚠ **而那段期間那一檔的還原序列就是假報酬。**

⭐ 2026-09-10 起每天打一發官方 `bulletin/revivt` 當判準：

    data/meta/otc_reduce_history.csv    官方 283 筆，回到 2013-01-16
    `_last_run.md` 的 `otc_reduce_history` 區塊會列出
      「官方有、我方 `data/adj/` 沒有」，⭐ 並且**分成涵蓋期內／外**

⛔ **涵蓋期內（≥ 首個日檔）的缺口是會叫的斷言**（歷史最低值、單調收斂）；
涵蓋期外那些是歷史欠帳，只 info 不 check。

⚠ 實例（本支上線當天抓到的）：**6461 益得 2026-09-09 減資，我方沒有。**
用我方自己的價格證實：09-01 收 **16.65**（＝官方 `last_close`，一分不差）、
09-02～09-08 停牌無列、09-09 收 25.75 ⇒ **沒有那個因子就是 +54.7% 的假報酬**。

⇒ **要算上櫃的還原報酬之前，先看那個區塊有沒有未歸因的缺口。**

#### ⛔⛔ 撤回：「上櫃除權息／減資沒有官方歷史來源」（2026-09-10）

那句是我方寫的，**錯**。市場情報分析線照**全站選單**（TPEx 485 條）找到：

    bulletin/exDailyQ   上櫃除權息（POST；⛔ 無斜線的 GET 會**靜默回今天**）
    bulletin/revivt     上櫃減資，**234 筆歷史**，抽驗 12/12 相符
    bulletin/pvChgRslt  上櫃變更面額

⚠ **但這不代表可以換供料。** `exDailyQ` 與我方 `data/adj/` 反方向比對：
它**少 17 筆減資 ＋ 5 筆轉上市後的事件** ⇒ ⛔ 拿它當「完整來源」會**刪掉還原因子**。
⭐ 這正是「只比一個方向就宣告一致」那一族的錯——**兩個方向都要比**。

⇒ 現況不變：上櫃因子仍走 FinMind（兩層獨立驗證見上），
官方那三支的用途是**判準與交叉驗證**，不是取代。

#### ⛔⛔ 轉上市股：**兩張官方表要聯集，只用一張一定會缺一段**

市場情報分析線 2026-09-10 12:52 全期對帳（2015-01-01 ～ 2026-09-09）的結論：

    TWT49U（上市除權息）  只涵蓋該檔在**上市期間**的事件
    exDailyQ（上櫃除權息）只涵蓋該檔在**上櫃期間**的事件

⇒ **重建任何一檔轉上市股的還原序列時，兩張表要聯集。**
實測：反方向（我方有、TWT49U 沒有）66 筆裡有 **57 筆**正是這一類，涉及 14 檔——

    3563 8462 2233 1597 3092 6438 6426 5306 1752 3652 8476 6472 4736 6446

拿去 TPEx `exDailyQ` 比對 **57 / 57 全部在裡面**，⛔ 未解釋 0 筆。

⚠ 另外 9 筆是**變更股票面額**（`event=parvalue`），`TWT49U` 本來就不含。
那 9 檔全部是名稱帶 `*` 的股票，而櫃買 `peQryDate` 的官方註解寫著
「名稱附註 `*` 者，表該股票為無面額或面額非屬 10 元」——兩邊對得起來。

⭐ **對帳結果：上市除權息 2015-01-01 ～ 2026-09-09 全期零缺漏、零 factor 錯誤**
（2018～2026 段 7,731/7,731；2015～2017 段 2,262 筆，見
`sources/adj_twse_audit_2018_2026.md` 與 `sources/adj_twse_audit_20260910.md`）。
⚠ 2015～2017 那 76 筆「我方多出來的」**尚未用這套分類法回頭重做**，仍是未查證。

#### ★ 減資（2026-09-06 併入）：三件跟除權息不一樣的事

1. **`event` 欄**（`data/adj/<代號>.csv` 的最後一欄）分辨 `exright` 與 `reduce`。
   舊的讀取端用欄名定位不受影響，但**要區分兩者意義時一定要看這欄**。
2. **`pre_close` 不是前一交易日的收盤。** 減資會停止買賣數個交易日，
   減資列的 `pre_close` 是**停牌前最後一個有成交的日子**的收盤。
   拿日曆的前一交易日去對，會整片假警報。
3. **減資與除權息同一天時只算一次。** 官方公式
   「恢復買賣參考價＝（停止買賣前收盤價 − **息值** − 每股退還股款）／減資換股率」，
   息值已含在減資參考價裡。`adjust.py` 會丟掉同日的除權息列並印出來，
   **兩邊都收會把除息扣兩次**。

### ⭐⭐ 5.x 「欄位在不在」**從來不是**「資料有沒有」的答案

2026-09-10 一天之內，兩條線在兩個欄位上摔了**方向相反**的兩次：

```
margin.note       欄位不在，但那九年其實抓得回來   ⇒ **低估**了自己有的
daily.last_price  欄位在，但那兩天一個值都沒有     ⇒ **高估**了自己有的
```

⛔ 而「那就看填值率」只做對了一半——同一天我方也摔了：
`last_price` 在 2026-09-10 的全庫填值率是 **352／71 萬列 ≈ 0.05%**，
⚠ 而它**完全正常**。⇒ 兩條線據此判它是空殼欄、建議刪掉，
⛔ **判錯了，而且方向是要人去刪一個好欄位。**

⇒ **判準：一個百分比不算答案。每一欄要跟三件事一起看：**

```
① 生效日     這一欄第一次出現在表頭是哪一天  ⛔ 生效日之前不算缺值
② 適用子集   它設計上該有值的那一群          ⚠ 本庫唯一可觀測的分群鍵是 `market`
③ 子集內填值率
```

⚠ 而 ② 是**推不出來的**：沒有任何欄位記著「這一欄只有興櫃該有值」。
⇒ `data/meta/_schema_audit.md`（每天重跑）把**每個 market 各自的分子分母**
全部列出來，⭐ 讓「只有某一群有值」自己顯形，⛔ 不替它下結論。

#### 已知的兩個實例，引用時直接照這個讀

| 欄 | 生效日 | 適用子集 | 子集外 |
|---|---|---|---|
| `daily.last_price` | **2026-09-10** | 只有 `market == "emerging"` | ⭐ `twse`／`tpex` 一律空是**設計**：它們的 `close` 本來就是最後撮合價 |
| `margin.note` | 見下 | 全部 | — |

⛔ `last_price` **不會回補**：興櫃的歷史逐日檔我方本來就沒有全市場來源。
⚠ 而興櫃的 `close` 存的是**加權均價**（`price_basis` 標著），
兩者真的不同——2026-09-09 那天 363 檔裡有 **318 檔**不一樣。

### ⭐ 5.y `margin.note`／`otcmargin.note` 的可用區間：**判準是動的，⛔ 不可以寫死年份**

`note`（官方信用交易註記）2026-09-10 才加進每日那一份，歷史正在回補中。
⚠ 而回補是**逐年分批**（每批推一次，任何失敗最多賠一年）
⇒ 「有 note 的最後一天」永遠停在某個 12/31，**而且每跑完一年就往前一年**。

```
⛔ 不要寫    2024-01-02 起不可執行
⭐ 要寫      可執行的日子 ＝ **該日檔的表頭有 note 欄**的那些日子
```

⚠ 這一條有代價證明：2026-09-10 兩條線把「2023-12-29 有、2024-01-02 沒有」
讀成**一次回補事故**，並據此把閘門那一列寫死成「2024 起不可執行」。
⛔ 而那是回補的**進度前緣**——當天稍晚它就走到 2024-12-31，那句話當天就過期了。

⭐ 通則：**分界的成因有兩種，在資料上長得一模一樣——一次破壞，或一個正在前進的前緣。**
分辨方法只有一個，而且很便宜：**過一段時間再量一次，看它動不動。**

#### ⛔⛔ 而上櫃那一半有**第三種**成因，2026-09-14 才分出來

```
data/universe/otcmargin/　2015-01-05 ~ 2026-09-08　 9 欄　⛔ 沒有 note
　　　　　　　　　　　　　2026-09-09　　　　　　　　13 欄　⛔ 仍然沒有 note（過渡態）
　　　　　　　　　　　　　2026-09-10 起　　　　　　 14 欄　✅ 有 note
```

⚠ 那個分界**不是**來源沒有這一欄——它是**我方 header 2026-09-09 才加**。
⇒ ⛔ 「來源沒有這一欄」與「我方當時沒收這一欄」**在檔案上長得一模一樣**，
而只看我方檔案永遠分不出來。

⭐ 分辨方法一樣便宜：**去問來源**。實測（`_tpex_probe.txt` [13]，Actions）：

```
2015-01-05　20 欄、572 列｜有「備註」欄｜**有值 132 列**
2019-11-13　20 欄、740 列｜有「備註」欄｜**有值 259 列**
2023-05-10　20 欄、773 列｜有「備註」欄｜**有值 220 列**
2026-09-11　20 欄、920 列｜有「備註」欄｜**有值 285 列**
```

⇒ ⭐ 來源**一直都有** ⇒ `feeds.py --need-col note` 可以回補全歷史（已在跑）。
⛔ 判準是「有欄位**而且有值 > 0**」——一個「有欄位、整欄空」的回應，
對閘門來說跟沒有一樣，⚠ 而它在紙上看起來像「補到了」。

⚠ 在回補跑完之前，**上櫃 2026-09-10 以前的停資停券註記請當成「不存在」**，
⛔ 不是「沒有被停」——那是全歷史偽陰性（K線線的前置閘門在上櫃那一半空轉）。

### ⛔⛔ 5.v 興櫃那 363 檔**沒有開盤價**——K 棒套上去會全錯，而且不報錯

K線線 2026-09-10 23:45 點名的。⚠ 這不是缺值，是**來源就沒有**：
興櫃官方端點只給**最高／最低／加權均價／最後成交價**，⛔ **沒有開盤價**。

實測（`data/universe/daily/2026-09-09.csv`，`_schema_audit.md` 每天重算）：
```
欄        emerging          tpex            twse
open        0 ／ 363      989 ／ 989    1,374 ／ 1,374
close     363 ／ 363      989 ／ 989    1,374 ／ 1,374   ⚠ 但興櫃的 close 是**均價**
```

#### ⛔ 三種會出事的讀法

```
① df["open"].fillna(0)          ⇒ 開盤價 0 ⇒ 每一根都是**漲停的長紅棒**
② df.dropna(subset=["open"])    ⇒ 興櫃 363 檔**整批消失**，⚠ 而且沒有任何訊息
③ 直接畫 K 棒                    ⇒ 實體從 0 畫到收盤 ⇒ ⛔ 型態判讀**全部無意義**
```
⚠ ①③ 最危險：它們**不會報錯**，會產出一份看起來完全正常的圖與訊號。

#### ⇒ 判準

```
⭐ 要 K 棒／型態／跳空 ⇒ **先用 `market != "emerging"` 濾掉**，⛔ 不是補值
⭐ 要價格序列（報酬、MA、波動）⇒ 興櫃可以用，但要知道 `close` 是**加權均價**
   （`price_basis` 標著），⛔ 與上市／上櫃的收盤價不是同一種東西
⭐ 要最後成交價 ⇒ 2026-09-10 起有 `last_price`（⛔ 只有興櫃有值，見 5.x）
   ⚠ 但它可能由**一筆 1 股**的交易決定：09-09 那天 363 檔裡有 318 檔與均價不同
```

⛔ **不可以**把空的 `open` 補成 `close`／前收／任何推算值：
補了之後那一欄就再也分不出「官方沒給」與「官方給了」。

### ⛔⛔ 5.u 「停止買賣中」與「已下市」在資料庫裡**曾經**分不開

K線分析線 2026-09-11 01:40 點名的，而它會改變判讀：

```
已下市      ⇒ 永遠不會回來，不進母體，沒有後續問題
停止買賣中  ⇒ ⚠ **它會復牌**。復牌後，前一段與後一段的價格中間隔了好幾個月
              ⛔ 接起來算均線／扣抵／型態／ATR ＝ 把一段**不存在的時間**當成交易日
```
⚠ 而在日檔裡兩者長得一模一樣：**都只是「日線沒有列了」**。

⇒ 三分類（三份判準檔都在 `data/meta/`／`data/universe/`）：
```
停更 ∧ 在 data/meta/delisted.csv        → 已下市，不進母體
停更 ∧ 在 data/universe/stophalt/       → 停止買賣中，⚠ 復牌後不可接續讀
停更 ∧ 兩張都不在                        → ⛔ 這才是真正的未解釋洞
```

#### ⛔⛔ `stophalt` **沒有歷史**：漏抓一天就永久少一天

`violation/stop` 的 `date=` 參數**官方會忽略**（K線線實測）
⇒ 它只有「**今天仍在停止買賣中**」的即時狀態。
⚠ 補抓成本是**無限大**——拿不回來 ⇒ 這一支每天跑，⛔ 而它壞掉的樣子是
「那一天沒有檔」，不是錯誤。

⭐ 唯一能回推的是每一列的 `halt_since`（官方的「停止買賣開始日期」）
⇒ **第一次抓到那天，就能把當下那一段的起點整段補回去**。
⛔ 但**更早結束過的停止買賣段落補不回來**——那些是永久的洞。

#### ⛔ `data/meta/suspend`（TWTAWU）**不是**這張表

那一支是「**短暫停牌後復牌**」：10,034 列裡 9,594 列是權證，
未復牌的 21 檔普通股 `halt_date` 全停在 2013~2014。
⚠ 兩者名字很像，⛔ 但是兩張表——`suspend` 查不到「長期停止買賣中」。

⚠ 上櫃（TPEx）的對應來源**尚未找到** ⇒ 目前 `stophalt` 只有 `market=twse`，
⛔ 上櫃的停止買賣一律標【不可判定】，**不可以**讀成「上櫃沒有停止買賣的股票」。

### 5.w 「這一檔在 D 那天**是不是**融資融券標的」：`data/meta/margin_membership.csv`

> ⭐ **2026-09-12 已落地**（原本標【待落地】，此處撤下）。三個檔都在 main 上，
> 而判準是**檔裡真的有內容**、⛔ 不是「`daily.yml` 裡有那一步」：
>
>     data/meta/margin_membership.csv   2,739 段
>     data/meta/halt_spans.csv            365 段
>     data/meta/hole_kinds.csv          1,402 列
>
> ⚠ 下面那組數字（1,406／1,048／134／99）**是拿 main 上現在那一份重算過的**，
> 逐格相同 ⇒ 不是抄舊信。
> ⭐ 而 K線分析線 09-11 給的那條規矩留著，⛔ 不因為這次落地就撤：
> **契約寫了但抓不到，下一個人會以為是自己錯** ⇒ 沒落地的一律標【待落地】，
> ⛔ 不可以先寫成既成事實。

⛔ **不要**去別的地方找端點——那份歷史我方已經有十一年半：
`data/universe/margin/`（上市）與 `otcmargin/`（上櫃）的**逐日成員**本身就是它。
`margin_universe.py` 每天把逐日集合壓成**逐檔區間**：

```
market, stock_id, start, end, days, open_ended
讀法：某檔在 D 那天是不是標的 ＝ 有沒有一段 start ≤ D ≤ end
```

現況（2026-09-10）：
```
twse  1,406 檔曾經是標的｜中途**成為**標的 688 段｜中途**不再是** 272 段｜進出多次 134 檔
tpex  1,048 檔曾經是標的｜中途**成為**標的 598 段｜中途**不再是** 250 段｜進出多次  99 檔
```

#### ⛔⛔ 兩個一定要分開的東西

```
不在表上          ＝ **不是**融資融券標的
在表上、note 有 O ＝ **是**標的，但被暫停融資買進（波動過劇／股權過度集中）
```
⚠ 兩者對讀取端的意思**完全相反**：被暫停的那一檔**融資餘額還在**，只是不能新增
⇒ 它的「融資餘額低且持續下降」不是槓桿出清，是被官方掐住。
⭐ `margin_membership.csv` 只回答前者，⛔ 一個字都不碰 `note`。

#### ⚠ 而區間會**跨過**「不可判定」的日子

一天整批沒抓到，會一次生出幾百筆假的「移除」，隔天又全部「加入」回來
⇒ 低於**鄰近 ±10 個交易日中位數** 70% 的日子標成不可判定，
那一天**既不算在、也不算不在**（⛔ 不是在那裡把區間斷開）。
⚠ 目前上市 0 天、上櫃 0 天觸發——⭐ 而「0 天」不代表資料乾淨，
只代表這個判準沒抓到；它抓得到的證明在 `selftest_margin_universe.py`（合成崩塌日）。

### ⚠ 5.z 減資因子：**上櫃已是官方比例，⛔ 而上市 303 筆全是回推的**

K線線 2026-09-10 20:20 裁定「還原用 `factor_official`、斷點門檻用 `factor`」。

> ⭐ **2026-09-13 全庫重算，上一版那句「上櫃是回推的」已解除，⛔ 而方向反過來了：**

```
減資事件 584 筆
  上櫃 tpex  281 筆  ⭐ **全部有 `factor_official`**，而且 `cum_factor` 用的就是它
  上市 twse  303 筆  ⛔ **全部沒有 `factor_official`** ⇒ 仍然是「官方參考價 ÷ 前收」回推的
                     年份平均分佈（2015~2026 每年 15~37 筆），⛔ 不是只有舊資料
```

⚠ 回推值與官方值的相對差（拿上櫃那 245 筆有雙欄的實測）：
**中位 1.2e-4、最大 7.4e-4**。單筆很小，⛔ 而 `cum_factor` 是**連乘**的。

⇒ 判準沒變，只是適用對象換了：
**看該列的 `factor_official` 欄有沒有值——⭐ 有值才是官方比例。**
⛔ 而現在「沒有值」＝ 它是上市的減資列。

⚠ 而上市那 303 筆**目前沒有第二來源可以驗**（上櫃那批是使用者用瀏覽器匯出櫃買
「減資恢復交易參考價」才有的）。⇒ 這是 `data/adj/` 現在最大的一格沒有外援。

---

## 六、回測一定要用 first_seen／last_seen

`data/meta/stocks.csv` 有 **256 檔已下市**的真實紀錄（例：0054 元大台商50 到 2022-05-26）。

回測到 2018 年時，universe 要用**當時真的存在的那批**，不是今天還活著的這批。
`data/stocks/` 已經含下市股票，**不要用「今天的清單」去過濾歷史**——那就是生存者偏差。

---

## 七、讀進來要驗，不要靜默相信

三個十秒內做得完、但能抓到真問題的檢查：

1. **列數**對照 `data/stocks/_index.csv`（欄位：`stock_id, rows, first, last`）
2. **日期集合**對照交易日曆 —— ⛔ **用 `data/meta/calendar_twse.csv`**
   ```python
   import csv, io
   with io.open("data/meta/calendar_twse.csv", encoding="utf-8") as f:
       cal = sorted(r["date"] for r in csv.DictReader(f) if r.get("date"))
   ```
   ⛔⛔ **這一條 2026-09-09 改過，舊版是錯的。**
   舊版教人讀 `data/universe/_coverage_backfill.csv` 與 `_coverage.csv`。
   **那兩個不是交易日曆，是「抓取覆蓋紀錄」**，而且：

   - 交易日曆的紀錄**分在三個檔**（`_coverage.csv`／`_coverage_backfill.csv`／
     `_coverage_daily.csv`），**要三個都 union 才完整**；
   - 每日管線現在只寫 **`_coverage_daily.csv`** ⇒ 只讀舊版那兩個的人，
     窗口會**停在 2026-09-03 而且愈差愈遠**。

   ⚠ 實測代價（2026-09-09，兩條線各踩一次）：
   K線線照舊版讀，算出「日曆缺最近 4 天」，於是他的
   「近 20／60 個交易日不可有缺漏」閘門**靜默往前平移**，
   **最近幾天完全不在檢查範圍內，而閘門仍然回報通過**。
   回測線沒踩到，因為它一直只讀 `calendar_twse.csv`。

   ⇒ **`calendar_twse.csv` 才是日曆**（來自 TWSE `FMTQIK` 大盤月報，
     與建日檔的個股端點不同條路，所以不是循環自證）。
     `_coverage*` 請只當「抓取覆蓋紀錄」用，⛔ 不要當日曆。
   ⇒ 自己那一側加一條斷言：**日曆最後一天 ≥ 你手上價格的最後一天**。
     少讀一個檔的時候它會當場紅，而不是安靜地平移。
     （我方同名斷言在 `calendar_audit.calendar_lag()`，每天進 `_last_run.md`。）
3. **價格合理性**：`low ≤ open,close ≤ high`、零價與負價、單日漲跌超出 ±11%
   （超出的多半是除權息或資料錯，要點名不要略過）

### ⛔⛔ 切「近 N 個交易日」的視窗，要**依交易日曆取**，不可以取最後 N 列

```python
# ⛔ 錯：rows[-20:]
# ✓ 對：先取日曆的最後 20 個交易日，再去對，缺的補 0（或明確標成缺）
win = cal[-20:]
vals = [by_date.get(d, 0.0) for d in win]     # 沒有那一列 ⇒ 補 0，不是跳過
```

**為什麼**：`data/universe/daily/` **只收當天有成交的證券**——
官方那一列存在（成交股數 0、價格欄 `--`），而我方解析時
`if not c: continue` **整列跳過**，所以檔案裡沒有那一天。

**全期實測（2015~2026，判準：T−1 有、T+1 有、T 沒有）**：

```
每年 9.3 ~ 15.8 筆／日，十二年沒有斷層
合計 **35,820 筆／1,236 檔**
```

⇒ 取最後 N 列的人，**視窗會比 N 個交易日長**，
而漏掉的**一律是零成交或低量日** ⇒ **均額系統性高估、波動系統性低估**，
⛔ **方向是單向的，不會互相抵銷**。

### ⛔ 用「精確日期」去 join `data/adj/` 會漏掉 183 筆

`data/adj/<代號>.csv` 有 **183 筆事件的日期不是交易日**（176 檔、12 個日期），
12 個日期**全部是颱風休市日**（2015-07-10、2016-07-08、2023-08-03、
2024-07-24、2024-07-25、2026-07-10 …）——官方把除權息排在那天，而那天休市。

⭐ **還原價本身是對的**，因為 `cum_factor` 的語意是
「適用於**該列日期之前**（不含當日）的價格」——那是 `date <` 查找，
而休市日**沒有任何價格**，所以邊界跟事件掛在哪一天完全一樣。

⛔ **但如果你問的是「這一天有沒有除權息事件」（精確日期 join），那 183 筆你會漏掉。**
⇒ 要判斷「某天有沒有公司行動」，用 `date <=` 的區間比對，不要用 `==`。

### ~~⛔⛔ 上櫃 ETF 完全沒有還原因子~~ ⇒ ⭐ **2026-09-13 已解除**

> **重算：上櫃 ETF 153 檔裡有除權息紀錄的 135 檔，`data/adj/` 現在
> **135 檔全部都有**（0 檔缺）。事件數重算是 **2,967** 次，不是 2,907。**
> ⇒ 下面整節是**解除前**的紀錄，留著是因為它記了「為什麼單筆很小卻不能忽略」。
> ⛔ 而**不要再拿它當現況**——契約寫著「沒有」而其實有，
> 下一個人會替它加上一段不必要的但書，⚠ 那跟寫著「有」而其實沒有一樣糟。

### ⛔⛔ **上櫃 ETF 完全沒有還原因子**（135 檔／2,907 次配息）

```
上市 ETF   偵測到 1,386 次配息｜有還原因子 1,386｜命中率 **100.00%**
上櫃 ETF   偵測到 2,907 次配息｜有還原因子     0｜命中率 ⛔ **0.00%**
           ⇒ 135 檔**連 `data/adj/<代號>.csv` 這個檔案都不存在**
           其中 125 檔是債券 ETF（代號 `B` 結尾）
```

**這不是「有檔沒事件」，是整類都沒有。** 而上市 ETF 是整類都有——界線分明。

#### ⚠ 單筆很小，累積很大——⛔ 只看單筆會讓人忽略它

```
單筆未還原跌幅：中位 **−0.90%**   ← 看起來可以忽略
但債券 ETF 是月配／季配，2018 起每檔中位 **26 次**：

  00773B 中信優先金融債    81 次｜單筆中位 −0.43%｜**全期累乘 −29.5%**
  00772B 中信高評級公司債   79 次｜單筆中位 −0.42%｜**全期累乘 −30.1%**
  00741B 富邦全球非投等債   47 次｜單筆中位 −0.73%｜**全期累乘 −35.9%**
  00740B 富邦全球投等債    47 次｜單筆中位 −0.76%｜**全期累乘 −29.3%**
```

⇒ **拿上櫃 ETF 的收盤價算長期報酬，會系統性低估約三成。**
⛔ **而且是單向的，不會互相抵銷**——每一次配息都往同一個方向扣。

#### ⇒ 怎麼用

- **短期（單日、數週）**：影響 ≤ 1%，多數用途可接受，但要知道它存在。
- ⛔ **長期報酬、累積績效、與其他標的比較**：**不可以直接用**。
  誰要用誰負責自己補（用 `data/universe/otcexright/` 或官方
  `tpex_exright_daily` 自行推算還原因子）。
- ⚠ **上市 ETF 不受影響**（100% 有因子），⛔ 但不要因此假設「ETF 都沒問題」——
  這條界線是**市場別**，不是證券種類。

⚠ 現況（2026-09-09）：**已知、未修**。市場情報分析線裁定「寫成已知缺陷、不要修」，
理由是它不在推薦母體（母體是 `kind == stock`）。⇒ **這一段就是那個「寫下來」。**

---

### ⚠ 興櫃的 `close` 曾經是 **0**（⭐ 2026-09-10 **寫入端與舊資料都修好了**）

興櫃無成交日的 `close` 是 `0`（2026-09 實測 6 檔；K線線 09-09 稽核在 4 個交易日內
量到 **34 檔／52 列**）。⛔ 拿它算報酬會得到 **−100% 或 +∞**。

⭐ **2026-09-10 修掉了寫入端**：`fetch.parse_openapi_daily` 的判零原本寫成 `if not c`，
而 `_num()` 回的是**字串**、`"0.00"` 是 truthy ⇒ 那 52 列是**帶著 0 寫進去的**。
（`data/universe/esb/` 逐月檔那一份早就用 `_isz()` 擋掉了——⚠ 又是同一段判斷抄兩份、
只修了一份。現在 `_isz` 只留一份，兩邊 import 同一支。）
⛔ 反向測試還照出一件更糟的：舊碼**連 `change` 都編了一個 `-12.00` 出來**
（用前一日均價減 0），而那一格看起來完全正常。

⇒ **從此興櫃無成交日寫成 `price_basis='無成交'`、價格欄空字串**，和上市／上櫃同一套。

#### ⛔⛔ 真正咬人的不是那個 0，是**從它算出來的 `change`**

K線線 2026-09-10 14:55：那幾天成交量都是 0，而 `change` 被編出
**−31.60%／−41.05%／−35.32%**。

> 一根 −31.6% 的長黑會**同時**觸發：跌破所有均線、長黑 K、單日停損、爆量下跌。
> ⛔ **它不是「一格數字不好看」，它是一個完整的、會通過所有型別檢查的假出場訊號。**

⚠ 我方全庫實測更糟：**61 列／36 檔／5 天，每一列的 `change` 都是編出來的**，
最極端 **−139.50**。⛔ 而那些列的 `price_basis` 寫著 `均價/額推算`
⇒ **照 `price_basis` 篩會篩進來。**

⭐ 通則（K線線抽的，值得記）：
**一個壞掉的來源欄，會沿著「用它算出來的欄位」擴散，而下游只看到後者。**
⇒ 擋壞資料要**連同衍生欄位一起擋**。

⭐ **舊資料已經修好了**（`fix_emerging_zero.py`，61 列 → 0 列，
`change` 一併作廢、`price_basis` 改成 `無成交`；`volume`／`amount` 照官方原文不動）。
⚠ 若在別處看到殘留，判準是 `market == 'emerging'` 且 `close` 轉 float 等於 0。興櫃還有兩件事見 `_data_audit.md` 的「四點五」節：
`close` 其實是**均價**（`price_basis` 欄標著「均價/額推算」），`open` 整欄空白。

---

⚠ 這一條對 MA、ATR、報酬序列、流動性門檻**全部適用**。
2026-09-09 實測：以「20 日均額 ≥ 5,000 萬」篩母體時，
兩種算法差 20 檔（全部是興櫃）；⛔ 但那是**今天**的數字，
**受影響的 1,236 檔裡任何一檔的視窗只要罩到它的洞，那一檔就會偏**。

**跨管線交叉驗證**才是最有份量的：同一檔若在兩條獨立路徑都有，逐欄比一次。
2026-09-04 實測 3017 的 `data/stocks/` 與 `data/history/` 重疊 405 天、逐欄完全相同。

---

### ⚠⚠ `stocks_per` 的兩個坑（2026-09-07 實測，8 檔抽驗一致）

這一層是 2026-09-07 才轉置出來的，來源端（TWSE `BWIBBU`／TPEx 的本益比表）
本身就不一致，轉置只是原樣搬過來、沒有加工。

**① `close` 欄不要當價格用。**

| 市場 | `close` 有值的區間 |
|---|---|
| 上市 | **2017-04-15 起**（之前 551 列空白）|
| 上櫃 | **全期空白**（2,845 列全空）|

價格一律讀 `data/stocks/`。這一欄留著只是為了與來源逐欄對得起來。

**② `fs_quarter`（財報年／季）上市與上櫃的格式不同，而且覆蓋範圍差很多。**

| 市場 | 格式 | 有值的區間 |
|---|---|---|
| 上市 | `115/2`（年**斜線**季）| 2017-04-15 起 |
| 上櫃 | `115Q2`（年 **Q** 季）| **2025-01-02 起**（之前 2,439 列全空）|

⛔ **只寫 `(\d+)/(\d)` 這一種解析式，上櫃會一列都 parse 不到**——
而且不會報錯，只會安靜地變成「這檔查不到財報期別」。
`tw-stock-analysis-report` 的「財報時效」那一節就是靠這個欄位判斷手上這期過期了沒。

**兩種都要收**：`^(\d{2,3})[/Q](\d)$`，或先 `.replace("Q", "/")` 再解析。
⚠ 上櫃 2025 年以前**真的沒有這個欄位**，那不是解析失敗，是來源就沒給——
兩者要分辨得出來，否則會把「來源沒有」誤記成「程式壞了」。

### ⛔⛔ `amount`（成交金額）**兩市場的定義不一樣**

市場情報分析線 2026-09-10 實測：

    上市（twse）  **寬**：含零股／盤後定價／鉅額交易
    上櫃（tpex）  **窄**：只有一般交易

⇒ ⛔ **兩市場的 `amount` 不可以直接相加，也不可以互相比較。**
跨市場的「成交值市佔」「金額排行」「量能比較」全部會被這一條汙染，
而且**看不出來**——兩邊都是數字、量級也相近。

⚠ 這也解釋了為什麼 Σ我方上市 `amount` ÷ 官方大盤成交金額穩定在 0.995 而不是 1.000：
分子分母的口徑本來就差一截。

#### ⭐⭐ 大盤那四個檔：`market_index` 有歷史，`market_amount` **沒有**，而且補不了

```
data/history/market_index.csv    date,close,change,change_pct
data/history/market_amount.csv   date,trade_value,trade_volume,transactions
data/history/market_breadth.csv  date,up,down,flat,limit_up,limit_down
data/history/market_inst.csv
```

四個都是 `fetch.py` 從 **2026-09-01** 起一天累積一列（⛔ 不是 2015 年起）。

⭐ 而 `market_index.csv` 的 `close` **回補得到**：TWSE `FMTQIK`（`calendar_audit.py`
每個月本來就會打一次）第 5 欄就是發行量加權股價指數，2015-01 起 140 個月
⇒ **0 次額外請求**。⚠ 而回補的列 `change`／`change_pct` 是**空的**
（FMTQIK 沒有 `change_pct`；`漲跌點數` 那一欄實測**不帶正負號**，
⛔ 存一個可能反向的值比留空更糟——`change` 從 `close` 自己減得出來）。

⛔⛔ **而同一張表的總量三欄不可以拿來補 `market_amount.csv`**（2026-09-01 實測）：

```
              我方（MI_INDEX type=IND）        FMTQIK
成交金額      1,090,449,046,456        1,187,571,567,117   ＋8.9%
成交股數          5,209,282,128           13,000,849,196   ⛔ **2.5 倍**
成交筆數              4,224,598                5,301,801   ＋25%
⭐ 指數              46,948.72                46,948.72   **逐位相同**
```

⇒ 兩條路的**口徑不同**（含不含鉅額／零股／盤後定價／外幣成交值），
⚠ 而差異**只落在總量那三欄**。⛔ 硬補的話 2015~2026-08 是一種口徑、
2026-09 起是另一種，**而接縫不會報錯**（主鍵是 date，兩段不重疊）。

⇒ ⭐ `calendar_audit --write-index` 的重疊日是**閘門**：
FMTQIK 與 MI_INDEX 兩條路算同一天的指數，對不上就一列都不寫、runlog 紅。
daily.yml 每天跑一次（`fetch.py` 排在前面 ⇒ 100% 重疊 ⇒ 純對照，不寫入）。

### ⛔⛔ `close` 可能是**空字串**（2026-09-10 起）——`price_basis == '無成交'`

十一年來 `data/universe/daily/` 只收「當天有成交價」的證券：
`backfill.parse_twse()` 有一行 `if not c: continue`，把官方確實發布、
但當天沒有成交價的列**整列丟掉**。規模至少 **67,446 筆／1,191 檔**
（`data/meta/_missing_rows.csv`，六張官方清單差集，⚠ 那是下界）。

使用者 2026-09-10 裁定「甲」：**照官方原文補回來。** 於是：

    price_basis == '無成交'  ⇒  close／open／high／low 是**空字串**
                                volume／amount **照官方原文**（⛔ 不一定是 0）
                                limit 一律空（沒有價格就沒有「停」）

⛔ **`volume=0` 不等於「沒有交易」。** 6904 伯鑫 2026-09-03／04／09 的成交仟元
分別是 2／8／2——那是**零股成交**。所以這些列的成交金額可能不是 0。

### ⚠ `無成交` 有**兩種成因**，而且對判讀的意義完全不同

    ① 官方那天公告**暫停交易**（根本不准交易）      ← 202 筆（0.30%）
    ② 冷門股當天沒有成交價（`parse_twse` 跳過 `--`）  ← 其餘 99.7%

⇒ 想分辨的話 **join `data/meta/suspend_twse.csv`**（`stock_id` ＋ `susp_date`）。
`data/meta/_missing_rows.csv` 的 `why` 欄已經幫你做好這個 join。

⛔ **為什麼不把「暫停交易」直接寫進 `price_basis`**：
那一欄的定義是「**行情端點當時說了什麼**」——`''` 是有收盤價、
`均價/額推算` 是興櫃、`無成交` 是那張表沒給價格。
把另一支端點（`TWTAWU`）的判斷混進來，日後就分不出
「這一格是行情表說的」還是「是某支公告端點說的」，
而**那支端點若當時錯了，也追不回來**。⇒ 歸因放在稽核層（`_missing_rows.csv`），
原始欄位只講它自己看到的事。

⚠ 而且比例上也不值得混：暫停交易只解釋 **千分之三**。

⚠ **讀價格前一定要處理這一格**，兩種都可以，⛔ 但不可以當它不存在：

    ① 要價格序列 → 濾掉 `price_basis == '無成交'` 的列
    ② 要交易日對齊 → 保留它，把 close 當「當天沒有成交價」

⭐ 用 `price_basis` 判，**不要用「close 是不是空的」判**：
那一欄是**正面標記**（興櫃是 `均價/額推算`，無成交是 `無成交`，
正常收盤價是空字串），而「close 空不空」只是它的副作用。

⚠ 這個改動是**故意**把一個靜默的偏誤換成一個會叫的失敗：
在此之前，「取最後 N 列」跨的交易日比 N 多（序列有洞，沒有人會發現）；
在此之後，`float(close)` 會炸。**後者才修得掉。**

### ⭐ 回補期間：「沒有那一列」有**兩個意思**，要先問這一天補到了沒

    A｜這一天這一檔**真的沒成交**  ← 市場事實，⛔ 不該擋
    B｜這一天**還沒被回補到**      ← 資料缺陷，該擋

⛔ **兩者在檔案上長得一模一樣**（都是「沒有那一列」）。

⇒ 判準：**那一天的日檔裡有沒有任何 `price_basis == '無成交'` 的列。**
有 ＝ 已用新語意重抓過 ⇒ 缺列就是 B；沒有 ⇒ **不可判定**，⛔ 不可以當成缺陷。

⇒ 已經算好了，每天重算、整份覆蓋（⛔ 不是台帳，沒有「上次跑到哪」的狀態）：

    data/meta/_notrade_days.csv      逐日：new_semantics 0/1、該日無成交列數（分市場）
    data/meta/_notrade_by_stock.csv  逐檔：多幾列、first／last ← **重算範圍就看這份**

⚠ 已知的偽陰性：某一天若全市場**真的**沒有任何一檔無成交，會被判成「還沒補」。
量過 2,848 個可比日裡 `missing=0` 的有 **0 天** ⇒ 實務上不會發生，
⛔ 但別寫成「不可能」——那是把觀察寫成通則。

### ⛔⛔ 而「濾掉無成交列」會生出一個**新的、更難發現的**靜默失敗

K線線 2026-09-10 13:00 提出，⭐ 這一條比上面那個偏誤更糟：

> 一檔冷門股的「近 20 個**交易日**」裡有 5 天無成交
> ⇒ 濾掉之後只剩 **15 根有價的 K**
> ⇒ **20MA 其實是 15 根算出來的，而列數看起來仍然是 20。**

⚠ 原來的偏誤方向單一、可以量（視窗跨的交易日比 N 多）；
這一個是「**視窗看起來剛好，但根數不夠**」——
⛔ 而 `rolling(20).mean()` 在濾過的序列上**不會抱怨**。

⇒ 斷言要寫成「**濾掉無成交之後仍有 N 根**」，不是「有 N 列」。
⇒ 不足時**標不適用**，⛔ 不可以用剩下的根數硬算——那等於偷偷把 20MA 換成 15MA。

⚠ 這一條對**冷門股**特別致命。K線線的 5,000 萬流動性下限會擋掉大部分，
但**追蹤中的持股與使用者主動查詢的個股不受那個下限保護**。

### ⛔⛔ 所以「濾掉無成交列」**不可以在讀檔那一層做**——那會連量一起濾掉

⭐ 這是我方從來沒有處理過的一種狀態：**有成交量、沒有成交價**（零股成交）。
K線線 2026-09-10 定的分層規則：

| 指標 | 無成交列 | 理由 |
|---|---|---|
| **均量（5／20／60 日）** | ⭐ **要納入** | 那些股數是真的成交，排除掉會**低估均量** ⇒ 反過來讓「爆量」門檻**變鬆** |
| **周轉率** | ⭐ 要納入 | 分子是股數，與價格無關 |
| **OBV** | ⛔ 不可納入 | 要比較今收與昨收，**沒有收盤價就沒有方向**，加或減都是編的 |
| **均線／扣抵／布林／ATR** | ⛔ 不可納入 | 全部建在收盤價上 |

⇒ **必須在每個指標各自的層級決定**。⛔ 一行 `dropna()` 一次做錯全部。

### ⭐ 反過來的好處：`data/stocks/` 的「洞」終於分得出兩種

    在此之前  少一列 ＝ **不知道為什麼少了**（只有一個意思）
    在此之後  少一列 ＝ 資料缺陷
              有一列但 price_basis='無成交' ＝ **市場事實**

⇒ 「連續缺 ≥ 5 個交易日 ⇒ 整檔標本方法不適用」那條判準**現在才真的可判**：
在此之前，一檔冷門股連續 5 天沒人買賣會被判成斷點，⛔ 那是純粹的誤殺。
⚠ 反過來的那一半也成立：補完之後剩下的「連續缺 5 天」**就真的是缺陷**
⇒ 偽陽性降、鑑別力升，**門檻不必動**。

### ⭐ `price_basis` 的三個值是**互斥且窮盡**的（回 K線線 Q2）

    ''            有收盤價（上市／上櫃正常交易日）
    '均價' / '均價/額推算'   興櫃：那一格是**加權均價**不是收盤價
    '無成交'      那張表**沒有給價格** ⇒ close/open/high/low 是空字串

⛔ **不會有「又是興櫃又是無成交」擠不進去的情形**，因為那兩件事不在同一個問題上：
`price_basis` 回答的是「**`close` 這一格是怎麼來的**」；無成交 ⇒ 根本沒有 `close`
⇒ 答案就是「沒有」。**「這一列是興櫃」由 `market` 欄回答**。

⇒ ⛔ **要挑興櫃的列，一律看 `market == 'emerging'`，不可以看 `price_basis` 是不是均價系**
——那樣會**漏掉興櫃的無成交日**。

### ⭐ `limit` 欄的**正向**用途：它是一個流動性旗標（2026-09-13 市場情報分析線裁）

⚠ 下面那一節在講它**不是**什麼。⛔ 而只寫「不是漲跌停」會讓人以為這一欄沒用。

`limit == 1` 真正測到的是「**這一天這一檔只成交在一個價位**」
⇒ ⭐ **那是一個當日流動性旗標，而且是對的。**

⚠ 而它正好裝到本專案現有的流動性下限（近 20 日均額 ≥ 5,000 萬）**擋不到**的那一格：

```
均額門檻   擋得掉「量太小」
⛔ 擋不掉  「有量、但全天只有一個價位」   ← ⭐ `limit == 1` 就是這一格
```

⇒ 後者在**進出場**上更致命：停損價根本掛不進去。

⛔ 而欄名不改（改名會讓同一個欄名前後兩種意思，比「名字取錯」更嚴重）。

### ⛔⛔ `limit` 欄**不是漲跌停旗標**——它是「一價到底」（2026-09-13 全庫實測）

K線分析線 2026-09-12 量到「對 tick 精確漲停價的召回只有 9~11%」。
⇒ 我方逐行看程式並全庫重算，**確認，而且比那更糟**。

#### 它實際上判的是什麼

```python
if o and h and l and c and o == h == l == c:      # ← 判準只有這一條
    lim = _lock_dir(chg)                          # change 的正負號 → up / down / flat
```

⇒ **`limit` ＝「開＝高＝低＝收」（整天只成交在一個價位）的方向。**
⛔ 它**從來沒有跟當天的漲跌停價比過**——程式裡根本沒有那個比較。

#### 全庫實測（2,848 個日檔／5,529,865 列，日檔最後一天 2026-09-09）

```
limit 非空          177,153 列（3.20%）｜up 66,121　down 55,267　flat 55,765
  其中 |漲跌幅| ≥ 9.5%（**可能**真的是漲跌停）   10,710 列
  其中 |漲跌幅| < 9.5%（⛔ **一定不是**漲跌停）  161,064 列
  其中 算不出（change 空）                        5,379 列
```

⇒ ⭐ **精確度的上限是 10,710 / 177,153 ＝ 6.0%**，
⚠ 而那還只是**上限**：「漲跌幅 ≥ 9.5%」不等於「打到停板」。

#### ⇒ 兩個方向都會錯，而且錯得不對稱

```
偽陽性（大宗）  冷門股整天只有一筆成交 ⇒ 開＝高＝低＝收 ⇒ 被標成 up/down
                ⛔ 而它離停板十萬八千里
偽陰性（大宗）  真的漲停但盤中有打開過 ⇒ 開≠高 ⇒ **一片空白**
                ⛔ 而「空白」看起來跟「那天沒有漲跌停」一模一樣
```

⇒ ⛔ **任何「今天有沒有漲停／買不買得到」的判準，都不可以建在這一欄上。**
⭐ 要漲跌停請**自己用價格算**（前收 × 漲跌幅 × tick 規則）——我方日檔的
`close`／`change` 足夠推出前收，K線分析線 2026-09-12 實測**不缺資料**。

#### ⚠ 那它還有什麼用

它唯一誠實回答的是那一句字面：**這一天這一檔只成交在一個價位。**
⇒ 拿來當**流動性／可成交性**的旗標是對的（那一天你只買得到那個價），
⛔ 拿來當漲跌停是錯的。⚠ 欄名取錯了，而改欄名會動到每一個日檔，
所以**留著名字、把定義寫死在這裡**。

### ⭐⭐ `valid_bar`：一根 K 棒**可不可以拿來算**（2026-09-12 起，`data/stocks/` 最後一欄）

K線分析線原本自己看 `price_basis` 判（「甲」）。⛔ 而那等於同一段判準散在每一條線裡
——每一條線判得不一樣的時候，**三邊都算得出數字，只是母體不同，沒有任何地方會說**。
⇒ 資料庫這邊把它物化成一欄（「乙」）。

    valid_bar = 1    有列 且 price_basis != '無成交' 且 close 非空
    valid_bar = 0    有列，但上面兩條有一條不成立

#### ⛔ 而「0」只有**一半**——另一半是**那一天根本沒有列**

    valid_bar = 0    有列，但那天沒有有效價
    ⛔ 缺日期        連列都沒有（回補還沒補到／那天沒成交而來源沒給列）

⚠ 一個不存在的列**沒有地方可以放 `valid_bar=0`**
⇒ ⭐ 算「連續 N 個交易日沒有有效 K 棒」（硬斷點）時，**兩種都要算進去**，
⛔ 只數 `valid_bar == 0` 的列會少算——而少算的方向是**把斷點說成沒有斷點**。

#### ⭐ 這一欄是**可反證**的：`price_basis` 與 `close` 原封不動留著

⛔ 衍生欄如果把來源欄蓋掉，它就變成**不可反證**的：算錯了也沒有東西可以對。
⇒ `valid_bar` 只在 `price_basis` 與 `close` **都還在表頭裡**的時候才發；
來源欄不見了就**大聲失敗、不發這一欄**，⛔ 不是「照樣發、只是沒得對照」。

#### ⛔⛔ 引用之前先問它是**哪一版、用哪一批日檔算的**

```python
import valid_bar
c, note = valid_bar.read_contract("data/stocks")
# c is None ⇒ ⛔ 這份個股庫**證明不了**它的 valid_bar 是新的（或它是加這一欄之前建的）
```

`data/stocks/_built.json` 裡那一塊長這樣：

    "valid_bar": {"version": "v1",
                  "rule": "有列 且 price_basis != 無成交 且 close 非空",
                  "from": {"files": …, "bytes": …, "last": "YYYY-MM-DD"}}

⚠ `from` 就是**產生它那一趟的日檔指紋**。⭐ 它存在的理由只有一個：
`valid_bar` 只要比 `data/universe/daily/` 早一趟跑就會跟日檔不一致，
⛔ **而它長得跟對的一模一樣**——沒有指紋的衍生欄，就是下一次的 `_db_status.md`。

### 融資融券的欄位（⚠ 有兩代表頭，2026-09-09 起）

    舊（9 欄）  date, stock_id, m_buy, m_sell, m_balance, m_limit, s_buy, s_sell, s_balance
    新（13 欄） …上面九欄不動… + m_prev, m_ret, s_prev, s_ret

`m_prev`＝前日融資餘額、`m_ret`＝現金償還、`s_prev`＝前日融券餘額、`s_ret`＝現券償還。

⛔ **舊日檔那四欄是空字串，不是 0。** 回補中的期間會同時存在兩代表頭；
`transpose.py` 取聯集當表頭、舊檔缺的欄補空字串，所以
`data/stocks_margin/<代號>.csv` 裡早期的列那四格會是空的。
⚠ **空字串＝那一天沒存這欄；`0`＝那一天真的是 0。判讀時要分得出來。**

⛔ **不要自己用「昨天的餘額」回推這四欄。** 量過了：

    現償 = 昨日餘額 + 買 − 賣 − 今日餘額
    ⇒ margin 4,574 列、otcmargin 3,205 列算出**負的現償**（＝代換一定錯，而且這只是下限）

假說「那些是除權息調整日」也被自己的資料推翻：落在該檔 adj 事件日的只有
2.95%／2.78%（負控 0.37%／0.41%）——關聯是真的，但只解釋得了 3%。

新欄一律**接在舊表頭後面**（讓舊表頭是新表頭的前綴），這是 `transpose.py`
在回補期間還合併得起來的前提；⛔ 以後加欄也要照這個規矩。

### ⭐ `last_price`（2026-09-10 起，日檔第 17 欄）——**只有興櫃有值**

    …前十六欄不動… + last_price

官方的「最後成交價」。⛔ 上市／上櫃一律空：它們的 `close` 本來就是最後撮合價，
再存一次等於同一個數字兩欄，⚠ **而兩欄以後一定會走岔**。

⚠ 興櫃的 `close` **仍然是加權均價**（`price_basis` 標著），⛔ 沒有被換掉。
理由：換掉的話 `close` 這一欄在時間軸上會**斷成兩段**
（補之前是均價、補之後是收盤價）⇒ **任何跨那個時點的長區間統計都會靜默錯掉，
而且沒有任何欄位標記它**。

⚠ 兩者**真的不同**：K線線 2026-09-09 量到 363 檔裡 **318 檔**兩者不一樣。
興櫃流動性極低，最後成交價可能由**一筆 1 股**的交易決定
⇒ ⛔ 拿它算報酬序列要自己知道在做什麼；均價才是這個市場比較有代表性的那個數。

⚠ 兩個範圍：
- **2026-09-10 之前的日檔沒有這一欄**（空字串）。
- ⛔ **這一欄不會回補**——興櫃的全市場歷史逐日檔我方本來就沒有來源
  （`data/universe/esb/` 是逐檔逐月，另一條路，⚠ 它存的也是均價）。
- 無成交日 `last_price` **是空的**：官方在那種列給 `0`，⛔ 我方不照抄。

### ⭐ 創新板：`data/universe/tib/<日期>.csv`（官方清單，2026-09-10 起）

    date, stock_id, name

⛔ **要判一檔是不是創新板，讀這一份，不要用名稱後綴。**
我方原本靠 `/(?:-|KY)創$/` 猜——2026-09-09 與官方清單**兩個方向都 0 差異**，
⚠ 但那是猜的：**改名就失效，而且不會有人發現**。

⚠ **存成逐日不是現況清單**，理由是 K線線那句判準：
「問的是它**現在**是什麼，還是它**那時候**是什麼。」圈歷史區間要的是那一天的成分。

#### ⛔⛔ 端點宣稱的下限是 2021-06-28，**而它實際上從 2022-09-22 才有資料**

```
2022-09-21 及之前   問得到，⭐ 而官方回的是「這天沒有資料」（⛔ 不是失敗、不是越界）
2022-09-22 起       有資料，而且連續
```

【實測 2026-09-12，⭐ **逐日問完，不是抽樣**】
2021-06-28 ~ 2022-09-21 這段交易日曆上有 **306 天**，
**306 天全部問過**（台帳 `_asked.json` 逐日可查）：
**有資料 0 天、失敗 0 天**——⭐ 端點是通的、參數是對的，它就是說那幾天沒有。
⚠ 越界才會**大聲失敗**（`stat:"查詢日期小於110年6月28日，請重新查詢!"`，
會算進失敗天數）⇒ ⛔ 這些天**不是越界**。

⇒ ⛔⛔ 而那段**不是「沒有創新板」**：創新板 **2021-07-20 開板**，
2021-07-30 就有股票在上面交易。⇒ 判準還是那一句：

    2021-07-20 ~ 2022-09-21   **我方取不到**（官方這條端點沒給）
    ⛔ 不可以寫成「那時候沒有創新板」，也不可以寫成「那一檔不是創新板」

⚠ 逐日問過哪幾天記在 `data/universe/tib/_asked.json`（「問到了、那天沒資料」的台帳）
⇒ 續跑會從**沒問過**的接著跑，⛔ 不會每晚重問同樣的前 30 天。
⭐ 而這段已經**問完了** ⇒ 以後派 `feed=tib` 不會再重打這 306 天，
⛔ 也不要因為「`data/universe/tib/` 那段是空的」就以為它沒跑過。


⚠ 官方那張表最後一列是「合計」（**代號欄空字串**），我方已丟掉。
⭐ 順帶記著它不是垃圾：那一列的成交金額
＝ `MI_INDEX` 大盤統計「14.創新板股票」，情報分析線驗過**差 0**。

### ⭐⭐ 借券賣出：`data/universe/sbl/` 與 `otcsbl/`（2026-09-10 起）

⛔ **要判空方壓力，不可以只看融券。**

    借券賣出是融券的 **77.5 倍**（2002 中鋼差 3,225 倍）
    缺口逐年惡化：2015 是 9.2 倍 → 2026 是 72.7 倍
    ⇒ **融券只佔空方的 1.4%**（2026 實測）

⇒ `券資比 = 融券餘額 ÷ 融資餘額` 的**分子只涵蓋空方的 1.4%**，
「券資比低 ＝ 空方壓力小」是拿那 1.4% 對整體下結論。
⚠ 偏誤方向**單一**：借券賣出越集中的股票（大型權值股、外資愛用），判讀越樂觀。
⭐ 而它**不會炸、不會缺值、不會有人抱怨**——券資比一直算得出來，只是量錯對象。

#### 欄位（15 欄，兩市場逐字相同）

    date, stock_id
    s_prev, s_sell, s_buy, s_ret, s_balance, s_limit     ← **融券**段
    sbl_prev, sbl_sell, sbl_return, sbl_adj, sbl_balance, sbl_limit  ← **借券賣出**段
    note

⭐ **`sbl_balance` 才是空方那個量。**
⛔ **`sbl_limit`（次一營業日可借券賣出限額）不是餘額，兩者不可互換。**

⚠ **`s_*` 那一段與 `data/universe/margin/` 的融券欄不是同一個口徑**
（這張表是「信用額度總量管制」視角）⇒ ⛔ 不可以互相取代。

#### ⚠ 三個一定要知道的

- **借券賣出股數含鉅額交易股數**（官方 notes 明文）⇒ 引用倍數時要標。
- 官方恆等式：`當日餘額 ＝ 前日餘額＋當日賣出−當日還券＋當日調整`
  ⇒ 我方逐列驗算，不符的列**丟掉並計數**（⛔ 不靜默寫入）。
- ⛔ **`TWT93U` 每日晚間二次更新（約 20:30 與 22:30 台北）**
  ⇒ 在那之間取到的是**不完整**的版本，⚠ 而它看起來完全正常。
  （我方兩個排程是 19:00 與 23:59，都不落在那個區間。）

#### ⚠ 符號集是**這張表自己的**，⛔ 與 `MI_MARGN` 不同

    X-停券／Y-未取得信用交易資格／V-不得借券交易且無借券餘額停止借券賣出
    %-信用額度分配／Z-借券賣出餘額已達總量控管標準或初次上市無賣出額度／!-停止買賣

⇒ **同一個 `X`，在 `MI_MARGN` 是「停止融券」，在這裡是「停券」。**

#### ⛔⛔ `note` 可能是**多個符號連在一起**——判準要用「包含」不是「等於」

2026-09-09 實測（1,301 列裡有註記的 117 列）：

    X 50｜XV 55｜V 3｜Y 4｜Z 1｜XV! 2｜V! 2

⚠ **`XV` 比單獨的 `X` 還多。**
⇒ ⛔ 寫 `note == 'X'` 會漏掉 **55 列**（而且是漏掉「同時停券又不得借券交易」的那一群
——⭐ **限制最嚴的那些**）。
⇒ 判準一律寫成 `'X' in note`。

#### ⭐ 落地當天的規模（2026-09-09，我方自己的資料）

    融券餘額合計       203,610,000 股
    借券賣出餘額合計  15,785,414,878 股
    ⇒ 借券是融券的 **77.5 倍**｜融券只佔空方 **1.27%**

⚠ 這個 77.5 與情報分析線 2026-09-10 獨立量到的**同一個數字**
（他們從官方端點、我方從落地後的資料）⇒ 兩條獨立路徑，同一個答案。
⚠ 官方 notes 明文「借券賣出股數含鉅額交易股數」⇒ **引用這個倍數時要標**。

### ⛔⛔ `note`（官方註記欄，2026-09-10 起第 14 欄）——**它會讓訊號方向相反**

    第三代（14 欄） …上面十三欄不動… + note

⛔ **這不是「補一個欄位」這種清潔工作。** K線線 2026-09-10 13:10 的表：

| 表面現象 | 意思 A（判讀規則原本假設的） | 意思 B（官方 `O`） |
|---|---|---|
| 融資餘額低、持續下降 | 槓桿已出清 ⇒ **賣壓風險低，偏正面** | 停止融資買進 ⇒ 股價波動過劇或股權過度集中，**偏負面** |

⇒ 一檔正在「停止融資買進」的股票，餘額**也會**低、**也會**持續下降
——因為新的買不進來。⭐ **那不是槓桿出清，是被官方掐住信用交易。**
⚠ 而它專打**飆股**：被停止融資的往往正是波動最大、也最需要看融資水位的那一群。

⇒ ⛔ **判「這檔能不能融資」一律看 `note`，不可以看「餘額是不是 0」。**
實證（情報分析線 00:50）：00671R 停止融資時餘額 **119,287 張**、1447 是 576、1516 是 1。

#### ⛔⛔ TWSE 的 `note` 講的是「**次一營業日**」，不是當天

官方原文（`MI_MARGN` 的 `notes`）：

    「備註欄係表明成交日【次一營業日】股票融資融券狀況」
    「符號說明： O：停止融資, X：停止融券, @：融資分配, %：融券分配, !：停止買賣」

⇒ 判「這檔**今天**能不能融資」要看 **前一交易日**那一列的 `note`。
⛔ 我方**照官方原文存在成交日那一列**，⚠ **偏移由讀取端處理**——
先移一天的話，就分不出「官方那天說的」與「我方推的」，而官方若改口也追不回來。

⚠ **TPEx（`margin/balance` 的「備註」欄）的時點語意未查證**，不可套用 TWSE 這條。
（⛔ 這正是「把觀察到的範圍寫成通則」那一族的錯——同一族不同端點各驗各的。）

#### ⛔⛔ 同一列的 `note` 與 `m_balance`，**講的是不同的日子**

K線線 2026-09-10 14:55 裁定，⭐ 這是「不移日」那個決定的直接後果：

    第 T 列： m_balance = T 當天的融資餘額      ← 講的是 T
             note      = T+1 起的信用交易限制  ← 講的是 T+1

⇒ ⛔ **任何人寫「篩掉 `note` 含 `O` 的列，剩下的餘額才拿來判讀」都是錯的**：
那會篩掉 T 這一天，⚠ **而 T 那天其實還可以融資買進**；真正該篩掉的是 T+1。
⚠ 差一天在單日看不出來，**在「餘額連續下降」這種趨勢判讀上會把轉折點挪一格**。

    問「D 這一天能不能融資買進」 → 讀 **D 的前一個交易日**那一列的 note。
    問「最新一列的 note 是什麼」 → 得到的是**明天**的狀態，不是今天。
    ⛔ 兩者不可混用。

⭐ 推論（K線線裁的）：一檔**今天首次**被標 `O` ⇒ **今天仍可融資買進**
⇒ **今天的餘額變化仍是正常的雙向**，明天起才變成只出不進。
⇒ 「餘額低且持續下降」要被判成「停止融資買進造成的」，
**至少要看到標記日之後的那幾天**，⛔ 不可以拿標記當天的餘額下結論。

⭐ **2026-09-10 18:00 解除：TPEx 的時點與上市相同，兩邊同規則。**
情報分析線取到官方逐字（TPEx 融資融券餘額表註）：

    「備註欄係表明成交日【次一營業日】股票融資融券狀況」

⇒ 上櫃**照樣套用**這個 T→T+1 偏移。
⚠ 而在這之前這裡寫的是【不可判定】——⛔ 那是對的，不是保守過頭：
**猜錯方向會把判讀整批挪一天，而且不會有人發現**；等到官方逐字才解除，
比先套上去再回頭改便宜得多。

#### ⛔ 符號集**不可跨表共用**

    MI_MARGN（上市）  ：O 停止融資／X 停止融券／@ 融資分配／% 融券分配／! 停止買賣
    TWT93U（上市借券）：X 停券／Y 未取得信用交易資格／V 不得借券交易且無借券餘額停止借券賣出
              ／% 信用額度分配／Z 借券賣出餘額已達總量控管或初次上市無賣出額度／! 停止買賣
    ⭐ TPEx margin/balance（上櫃）：上面那五個 ＋ **`* A B C D`**（上櫃專有）
              ／`*` **融券餘額占融資餘額 60% 以上**（⭐ 官方直接標出「券資比 ≥ 60%」，
                不必自己算——⚠ 但**上市那邊沒有這個符號**）
              ／A 股價波動過度劇烈／B 股權過度集中／C 成交量過度異常／D 監視第二次處置

#### ⛔⛔ 而上櫃那個 `*` —— **官方蓋章的數字，仍然可以是在量錯的對象**

K線線 2026-09-10 20:20 逐字：

> **官方旗標解決的是「我方沒存」，它不解決「這個量是不是我要的量」。**

`*` 的定義是**融券餘額 ÷ 融資餘額 ≥ 60%**。⚠ 那是「融券相對融資高」，
⛔ **不是「空方壓力大」**——借券賣出是融券的 **77.5 倍**、融券只佔空方 **1.27%**。
⇒ 我方存這個標記，契約裡也只寫「官方標記：融券／融資 ≥ 60%」，
⛔ 不寫任何跟「空方壓力」有關的字，也不放進任何否決條件。

⭐ 通則（我方這一側的說法）：**一個欄位存在，不代表它回答的是你要問的問題。**

⇒ **同一個 `X`，在 `MI_MARGN` 是「停止融券」，在 `TWT93U` 是「停券」。**
⇒ ⛔⛔ **兩市場的備註欄要分開解析，不可以共用符號表**——
⚠ 上櫃比上市**多四個字母**，拿上市那張表去讀上櫃，那四種會被讀成「沒有註記」。
⚠ 而官方同一份說明裡「數字」出現兩次、意思不同
（合計降低融資比率 vs 監視業務督導會報決議降低）⇒ ⛔ 數字型註記不可以只認一種。
⇒ 我方存的是**原文字元**，不翻譯 —— 翻譯等於把某一天的解讀寫死進資料。

⚠ **2026-09-10 之前的日檔這一欄是空字串**（那時候還沒存），
⛔ 空字串 ≠「那天沒有註記」。回補完成前，凡是要用 `note` 下結論的，
**先確認那一天的日檔有沒有這一欄**。

### ⭐ 上櫃減資：`data/meta/otc_reduce_history.csv` 多了官方換股比例（2026-09-10 起）

    …原有七欄不動… + shares_per_1000, cash_return, factor_official

⭐ 官方在 `revivt` 第 11 欄（`詳細資料`）就給了**換股比例本身**：

    每壹仟股換發新股票: 618.578 股
    每股退還股款: 0.00000000 元/股

⭐ 而它**自帶驗算**（情報分析線 287/287 全過，我方逐筆重跑）：

    恢復買賣參考價 ＝（最後交易日收盤價 − 每股退還股款）÷（每壹仟股換發新股 ÷ 1000）

⚠ **`factor` 與 `factor_official` 是兩個數字，不是同一個。**
`factor` 從官方**印到分為止**的參考價回推 ⇒ 帶四捨五入殘差；
`factor_official` 從原始比例算 ⇒ 沒有殘差。
⛔ **用哪一個是判準問題 ⇒ K線線裁定。** 這一支只把兩個都存下來並逐筆比，
⛔ **沒有**動 `data/adj/`（那個目錄的寫入者是 `adjust.py`／`otc_adj.py`）。

⚠ `cash_return` 是 **0** 與**讀不到**分得開：0 存 `0.00000000`，讀不到存空字串。
⛔ 兩者混用會讓上面那條恆等式「看起來過了」。

### ⭐⭐ `data/adj/` 多一欄 `factor_official`（2026-09-10 起）——**同一個事件兩個數字**

    date, factor, factor_official, cum_factor, pre_close, ref_price, kind, event

K線線 2026-09-10 20:20 裁定，逐字：

> **同一個公司行動可以有兩個正確的數字，因為它們回答的是兩個不同的問題。**
> 「持有人的財富怎麼變」用原始比例；「當天開盤從哪裡開始」用官方參考價。
> ⛔ 統一成一個看起來比較乾淨，**但那會讓其中一個問題被錯的數字回答。**

| 欄位 | 是什麼 | 誰該用 |
|---|---|---|
| `factor` | 參考價 ÷ 前收（⚠ 官方參考價**印到分** ⇒ 帶捨入殘差） | ⭐ **硬斷點偵測的門檻**（0.55／1.8 就是從參考價推出來的） |
| `factor_official` | 官方**換股比例**回推（無殘差）⚠ 目前只有上櫃減資有 | — |
| ⭐ `cum_factor` | 連乘，**有官方值就用官方值** | ⭐ **還原價一律用它** |

⚠ 為什麼還原要用官方比例：殘差是**系統性**的（一律往「印到分」那一格捨入）
⇒ ⛔ 在長序列**連乘**的還原價上**不會互相抵銷**。

#### ⛔ 兩個讀取端要知道的

- `factor_official` **空白 ≠ 0**：空白代表**那個事件沒有官方比例**
  （只有上櫃減資有）⇒ ⛔ 不可以拿 `factor` 填進去當它的值。
- ⚠ `cum_factor` 已經幫你選好了 ⇒ **不要自己在 `factor` 與 `factor_official`
  之間挑**；⛔ 挑錯的那一次不會報錯，只會讓那一檔的還原序列整段偏一點點。

### ⭐ 個股融資融券成數的**調整幅度**：`data/universe/marginratio/`（2026-09-10 起）

    date, stock_id, name, reason, adjust_from, restore_date,
    margin_cut, margin_cum, short_raise, short_cum

⛔⛔ **這不是「現行成數」，是調整幅度。**
官方基準是「最高融資比率 **60%**、最低融券保證金成數 **90%**」（⚠ 那是**上櫃**頁面的
逐字，上市那一半尚無出處 ⇒ ⛔ 先不要套過去）；個股實際成數要用
「基準 − 累計調整」推，⚠ **而那個彙總規則是判準，屬 K線線** ⇒ 我方只落地原始欄位。

#### ⛔ 三個會讀錯的地方（都在實測值裡看得到）

    ① **一檔多列**：471 列裡只有 **94 個相異代號**——一個「調整成數原因」一列
       ⇒ 主鍵是（日期, 代號, **原因**），⛔ 不是（日期, 代號）
    ② **值有 `累計：N` 前綴**：`margin_cut` 與 `margin_cum` 是**兩欄**
       ⇒ `累計：6` ⇒ 值 6、`margin_cum=1`；單次的 `6` ⇒ 值 6、`margin_cum=**0**`
       ⛔ 混成一個數字就分不出「這一次降 6」與「累計降 6」
    ③ **空值有兩種寫法**：空字串與**單一半形空白**
       ⇒ 我方一律存空字串；⛔ 而空字串 ≠ 0（0 是「不調整」的真值）

⚠ `reason` 的實測相異值（6 種）：股價波動過度劇烈／成交量過度異常／股權過度集中／
監視第二次處置／TDR兌回異常／（空）。⭐ 官方 `selectList` 另有「監視業務督導會報決議
處置增加調整成數」。

#### ⛔ 它的日期**只寫在 `hints` 裡**

回應**沒有 `date`、`title` 也不帶日期** ⇒ 通用的 `_same_day` 找不到自述日期
⇒ **一律放行**。⚠ 而不帶日期參數時它回的是**前一個營業日**。
⇒ 這一支的 parser **自己驗 `hints`**；⛔ 而 `hints` 沒有被加進通用判準——
別的端點的 `hints` 常寫著「資料自 104 年起提供」這種**與本次查詢無關**的日期，
⚠ 加進去會變成大規模誤擋。

### ⛔ 借券餘額的恆等式有一個**定義邊界**：該檔離開本市場那一天

官方恆等式（`notes` 明文）：`當日餘額 ＝ 前日餘額 ＋ 賣出 − 還券 ＋ 調整`。

⚠ **該檔離開本市場（轉上市／終止櫃買）的那一天，官方把餘額直接歸零**
——⛔ 不走「還券」也不走「調整」欄 ⇒ 恆等式當然不成立。

實例（情報分析線 2026-09-10 逐日全表驗算，20 天裡就這一筆）：

    2015-01-22　3416 融程電
    前日餘額 1,000　賣出 0　還券 0　調整 0　當日餘額 **0**　⇒ 差 1,000 股
    01-23 起這一檔**整個從上櫃表消失**（我方日檔：01-22 tpex、01-23 twse）

⭐ 這跟「`exDailyQ` 不含該檔轉上市之後」是**同一個形狀，第二次出現**。

⇒ 我方判準：不符時看該檔**次一交易日還在不在這張表上**
（不在 ⇒ 離開本市場，⛔ 不算瑕疵；還在 ⇒ 才是真的要查）。
⛔ **不可以靠備註欄判斷**——那一筆的備註是**空的**。

### ⚠ TPEx／TWSE 的民國年參數要**三位補零**

    chtm  date=098/06/01  ✅   ／ date=98/06/01  ⛔ 無效（會回今天）
    suspendListing 回傳值 090/01/20（⛔ 不是 90/01/20）

⇒ **民國 100 年以前的資料，兩位寫法一律不要用。**
⚠ 情報分析線先前說「`chtm` 的 `98/06/01` 靜默回今天」——
⭐ 那是**送錯參數**，不是端點的性質；`chtm` 的歷史至少回到 **2009**。

### ⚠ `fields` 的長度 **≠** `data` 每列的長度

`chtm` 的 `fields` 是 8 個名字，而 `data` 每列有 **10** 個元素
——末兩欄（公告連結、財務重點專區連結）**有值但沒有名字**。

⇒ 逐字比對的守衛要照 **`data` 的實際長度**寫；
⭐ 用欄名定位的取值不受影響（⛔ 用位置定位的會少看兩欄）。

### ⭐⭐ 上櫃分盤／變更交易／管理股票：`data/universe/chtm/`（2026-09-10 起）

上市只有 `TWT85U` 一欄 `**`（狀態，且要自己推）；⭐ **上櫃四個狀態各一欄**：

    date, stock_id, name, changed, split_auction, managed,
    match_cycle_min, halted, fin_watch

⭐ **`match_cycle_min` 是官方逐檔給的撮合循環時間**（實測值 `030` 與 `045`）。

⛔⛔ **「30 分鐘」不是常數，也不是全市場一致。**
官方 `TWT85U` 的 notes 自己就說「每 30 分鐘…**為原則**，並得視交易情形因應公告調整」。
⇒ 判讀裡要用撮合週期一律**逐檔讀這一欄**（K線線 2026-09-10 20:20 裁）。
⚠ 而 K線 skill 舊的那組數字（2／10／25／45／60 分）是**處置制度**那一套，
⛔ 不是變更交易這一套——**`30` 根本不在那組裡**。

⚠ **上市沒有同等來源**：只知「30 分鐘為原則、得調整」
⇒ 上市個股的撮合週期一律標【只知原則值，不知該檔實際值】。
⚠ 上市也**沒有「管理股票」這個制度**（櫃買特有的板別）。

#### ⛔⛔ 原始值是**全形 Ｙ**（U+FF39），我方存的是 1/0

寫 `== "Y"`（半形）會**一筆都不匹配，而且不報錯**——整欄變成「沒有任何一檔被標記」。
⇒ 我方存 `1`／`0`，判準是「**非空**」而不是等號比對。
⭐ 這是「分類欄要當**集合**讀、不要當**值**讀」的同一條
（`OX!`、`XV`、`**`、`*ABCD` 都是複合值）——
⛔ `==` 的偏誤不是隨機的，它**系統性地放行狀態最多、也就是最該被擋的那一批**。

⚠ `match_cycle_min` 存的是**原樣字串**（`"030"`），⛔ 不是整數：
轉成整數就分不出「沒有值」與「0 分鐘」。

⚠ 涵蓋：我方實測 `098/06/01` 回 38 列、且與今天那一份**逐位元組不同**
⇒ ⭐ 這支的歷史至少到 **2009**（我方只回補到母體起點 2015）。

### ⭐⭐ 變更交易（全額交割）：`data/universe/fulldelivery/`（2026-09-10 起）

⛔ **這一列在 K線線的前置閘門裡本來是「假裝有在擋」的**：閘門寫著「全額交割股」，
而我方全庫全文查「全額交割」**0 次** ⇒ 那一列過不過都一樣，
⚠ 而下游會以為過了關——**那比沒有擋更危險**。

    date, stock_id, name, split_auction

⭐ `split_auction` = 官方 `**` 標示。**官方 `notes` 逐字**（⛔ 不是轉述）：

> `**` 代表上市證券除應預先收足款券外，其交易採**分盤集合競價**方式，
> 該方式係採每 30 分鐘以人工管制之撮合終端機執行撮合作業一次為原則，
> 並得視交易情形因應公告調整撮合時間。

⇒ ⛔ 那是**狀態**，不是成因：**處置股也會分盤**。
⇒ 「這一檔是不是分盤集合競價」與「它為什麼被分盤」是**兩個問題、兩個名單**。

⚠ 而另一支 `BFIHBU` 的 notes 講的是這一群**還被禁掉什麼**：

> 變更交易有價證券**不得進行當日沖銷交易、融資融券交易、借券交易、平盤以下借券賣出**

⇒ ⛔ **「融資融券欄位是 0」至少有兩個成因**：
① 主管機關個別公告停止融資融券（`MI_MARGN` 備註欄，且那是**次一營業日**生效）；
② 被列為變更交易 ⇒ 連當沖與借券一起禁掉，**比 ① 嚴**。
⚠ 只查其中一個名單會誤判。

⚠ 涵蓋：`date=` 是**真的吃的**（實測 2015-01-05 → 23 列、2020-01-03 → 21）
⇒ 逐日回補中；⛔ 回補完成前「那一天沒有檔」≠「那一天沒有全額交割股」。

### ⭐⭐ 終止上市（下市）清單：`data/meta/delisted.csv`（2026-09-10 起）

⛔ **這一份解的是生存者偏誤，而生存者偏誤不會讓任何一格數字出錯。**

下市公司會從母體消失 ⇒ 母體只收「**現在還在的**」
⇒ 「2015 買、後來下市」那一段**整段不見**
⇒ ⭐ 回測績效**系統性偏樂觀**——錯在「該賠錢的標的根本沒被算進來」。

265 筆裡有 2888 新光金、2867 三商壽、2809 京城銀、3682 亞太電。

    delist_date, stock_id, name, asof      ← 涵蓋 2001-01-20 起

⭐ **它同時是 `stocks.csv` 的 `last_seen` 的外部判準**：
我方 `last_seen` 應該 ≤ 官方終止上市日；反過來代表我方寫進了不該有的列。
⛔ 但這一支**不碰** `stocks.csv`——那個檔的唯一寫入者是 `fetch.merge_stocks_meta`。

⚠ 官方有、我方 meta 沒有的那些，**多半是我方涵蓋期（2015）之前就下市的**
⇒ ⛔ 那不是缺陷，不要拿它當漏抓的證據。
⚠ 端點**不吃日期參數**（`yy` 帶了也回全部 265 筆，實測兩趟同數）
⇒ ⛔ 不要拿它驗年份，也不要以為可以逐年抓。

## 八、現況數字（2026-09-07）

**個股軸**

| | |
|---|---|
| 價格 | **3,029 檔、5,521,648 列、2015-01-05 ~ 2026-09-04**（含 256 檔已下市）|
| 三大法人 | **2,661 檔、4,787,894 列、2015-01-05 起**（上市＋上櫃合併）|
| **融資融券** | **2,426 檔、5,083,638 列、2015-01-05 ~ 2026-09-04**（上市＋上櫃合併，2026-09-07 轉置）。1,112 檔是全期 2,845 列 |
| **本益比／殖利率／股淨比** | **2,121 檔、4,919,397 列、同上區間**（上市＋上櫃合併，2026-09-07 轉置）。1,039 檔全期。⚠ `close` 與 `fs_quarter` 有坑，見第五節 |
| 還原因子 | **2,199 檔、19,385 個事件**（除權息 18,853 ＋ 減資 534）。上市官方、**上櫃 FinMind**。前收盤交叉核對 19,340 筆、**0 不符** |
| 興櫃（esb） | 3 檔，最長 2,844 列、2015 起 |
| 股本 | **2,463 檔**（ok 2,272／含特別股 27／mismatch 32／只有股數 131／無股數 1）。**2026-09-07 補上上櫃**：`capital` 空白從 1,000 檔降到 131 檔，其中 119 檔是 ETF／ETN／債券 ETF（端點本來就沒有公司基本資料），真正的一般個股只剩 8 檔（4154、4183、4950、5523、6129、6624、7757、8087），要走 `--finmind` |
| 月營收 | 當期 1,975 檔（上市 1,085／上櫃 890）。**歷史已回補到 2015-01**，在 `data/mops/revenue_hist/` |
| 財報 | 當期一般業 1,930 檔 ＋ 銀行 10／證券 10／保險 5。**歷史已回補到 2015**，在 `data/mops/fs_hist/`、`bs_hist/` |
| 產業別 | **1,984 檔**（上市 1,094／上櫃 890），34 個類股（2 個是母類股）。**在市的上市櫃 100% 有值**；興櫃與下市股沒有 |

**兩道稽核（2026-09-07）**

| 稽核 | 結果 |
|---|---|
| **交易日曆對獨立來源** | TWSE `FMTQIK`，141 個月全部問到、失敗 0。官方 **2,845 = 我方 2,845**，兩個方向 0 差異。獨立日曆存在 `data/meta/calendar_twse.csv` |

> **★ 交易日曆涵蓋上櫃嗎？全庫實測 0 天（2026-09-09）**
>
> 掃 2,847 個日檔，「出現在日檔、卻不在 `calendar_twse.csv` 裡」的日期是 **0 天**；
> 反方向（日曆有、日檔沒有）也是 **0 天**。週六 8 天兩邊完全一致。
> ⇒ **日曆可以當全市場用**，先前「只涵蓋上市」那條警語已解除。
>
> ★ 這不是循環自證：`backfill.py` 的日期是逐**平日**迭代（`wd < 5`），
> 與日曆無關。上櫃若有 TWSE 沒有的平日交易日，我們會抓到、寫出日檔、被這個檢查抓出來。
>
> ⚠ **殘餘限制（推論，未經實測）**：週六預設不抓（需 `--saturdays`），
> 故「上櫃單獨開的週六場」兩邊都看不到。台灣補行交易日為全市場公告，
> 據此推論不存在，**但沒有被證明**。
> 已知的 8 個補行交易日（2016-01-30、2016-06-04、2016-09-10、2017-02-18、
> 2017-06-03、2017-09-30、2018-03-31、2018-12-22）**是 FMTQIK 自己有的**——
> 日曆來自大盤層級端點、與逐日抓取無關，所以那 8 天不是靠 `--saturdays` 補進來的。
| **每日內容完整度** | 缺檔 **0** 天｜帳本 vs 實際檔案對不上 **0** 筆｜家數局部離群 **0** 天（三個市場）｜有列但 `close` 不是數字 **0** 列｜解析不了的列 **0** 列。逐日逐市場的明細在 `data/meta/_content_audit.csv`（5,692 列）|

⚠ 第二道的「逐檔缺漏 50,922 段」**不是缺陷**，見上方「日檔沒有那一列，不代表漏抓」。

**日期軸**（交易日 **2,845 天**，2015-01-05 ~ 2026-09-04）

| feed | 覆蓋 | 驗算 |
|---|---|---|
| `daily`（價格） | 2,845／2,845 | 失敗 0、缺市場 0 |
| `inst`（上市法人） | 2,845／2,845 | 外資＋投信＋自營＝合計，抽驗 0 不符 |
| `otcinst`（上櫃法人） | 2,845／2,845 | 同上，抽驗 36,512 列 0 不符 |
| `margin`（上市融資券） | 2,845／2,845 | 餘額恆等式**全期 0 丟棄** |
| `otcmargin`（上櫃融資券） | 2,845／2,845 | 餘額為負 0 列 |
| `per`（上市估值） | 2,845／2,845 | 本益比為 0：**0 列**（虧損留空）|
| `otcper`（上櫃估值） | 2,845／2,845 | 同上；**收盤價欄 100% 留空**（TPEx 該表沒有）|
| `exright`（除權息） | 事件型 | 前收盤交叉核對 11,712 查、**0 不符** |
| `reduce`（減資，上市） | 事件型 | **305 列、2015-01-23 ~ 2026-08-24**；逐年對過官方表 12 年全部相符 |
| `otcexright`（除權息，上櫃）| 事件型 | **7,124 列、890 檔、2015-01-21 ~ 2026-09-03**（FinMind）|
| `otcreduce`（減資，上櫃）| 事件型 | **232 列、166 檔、2015-01-30 ~ 2026-08-25**（FinMind）|

> `exright` 是**事件型**，沒有除權息的日子本來就沒有檔案——
> **不要把「那天沒有檔」讀成漏抓**。季節性可以拿來驗：6～8 月是 3～5 月的數十倍。

> `reduce` 的回補**逐年對過官方表**（2026-09-06）：
> 2015 **26**／2016 **33**／2017 **37**／2018 **34**／2019 **27**／2020 **28**／
> 2021 **27**／2022 **31**／2023 **26**／2024 **15**／2025 **17**／2026 **4**
> ——與 TWSE `reducation/TWTAUU` 逐年查詢的列數**全部相符**。
>
> ⚠ **減資表同一（代號,日期）會重複出現**：3536 於 2015-03-20 出現 3 次、
> 5906 於 2016-07-14 出現 2 次，四個價格欄一模一樣，只有「除權參考價」欄有差。
> `adjust.py` 會去重（判準是**因子是否相同**，不是「同一天只能有一筆」）。
> 不去重的話因子會被**連乘**——3536 的 2.0258 會變成 8.32，該檔 2015-03-20 之前
> 每一根 K 棒被灌大四倍，**而且不會有任何錯誤訊息**（每一列自己都是對的）。
> 除權息表沒有這個問題（11,729 列實測重複 0 組）。
>
> ⚠ **減資是事前公告表，但資料庫裡只有已發生的。**
> `reducation/TWTAUU` 在停止買賣期間就先公告參考價，所以當月的回應會帶未來日期的列
> （2026-09-06 那天就已經公告到 2026-09-29）。**那些列不會寫進資料庫。**
>
> 理由是還原因子的不變量：**最新價的 F = 1，現價不動**。
> 收下一筆還沒發生的減資，今天 1563 的收盤 66.00 會被還原成 84.66，
> 報告上的價位就跟看盤軟體對不起來了——而且不會有任何地方報錯。
> `feeds.py` 不寫未來的列，`adjust.py` 另有一道閘門擋掉日期在**交易日曆最後一天**
> 之後的事件（基準是資料到哪一天，**不是系統時鐘**——容器的時鐘曾經差過一天）。
>
> → 所以 `data/universe/reduce/` 的最大日期**不會超過交易日曆**。
> 要知道「接下來有哪些減資」得現查端點，資料庫不負責回答未來。

維護：`daily.yml` 每天抓完之後，依序寫入當日的上市／上櫃全市場法人，再跑 `transpose.py` 全量重建。
**全量重建是決定性的**——資料沒變就不會 commit。

---

## ⭐⭐ 官方自己給的公式（`formula` 鍵，2026-09-13 才開始留）

⛔ `_tables()` 只取 `title`／`fields`／`data`，⚠ 而 TWSE 有些表在同一個回應裡
給一個 `formula` 鍵——**我方一直丟掉它**（CLAUDE.md 第一點那一族）。

### 減資恢復買賣參考價（`reducation/TWTAUU`）

```
退還股款：            參考價 ＝（停止買賣前收盤價 − 息值 − 每股退還股款）/（減資換股率）
彌補虧損：            參考價 ＝（停止買賣前收盤價）/（減資換股率）
彌補虧損之減資並現金增資：
    恢復買賣參考價 ＝（停止買賣前收盤價）/（減資換股率）
    減資後現金增資除權參考價
        ＝（恢復買賣參考價 + 現金增資認購價 × 減資後現金增資配股率）
          /（1 + 減資後現金增資配股率）
```

⚠ 而我方 `reduce_shares_check.implied_cash()` 推的式子**少了兩件**：
① **息值**（「除息併案辦理減資」會同時扣現金股利）
② 退還金額是官方欄位「每股退還股款」，⛔ 不是我推的「面額 10 × 減資比率」。

### 除權除息預告表（`exRight/TWT48U`）

```
無償配股率   ＝（股東之盈餘及資本公積增資股數）/（參與除權之發行股數）
現金增資配股率 ＝（現金增資股數）/（參與除權之發行股數）
```

### ⛔ 而公式會變，而且變了不會有人講

⇒ `official_formula.py` 把看過的釘在 `KNOWN` 裡，每一趟抓取比一次，
四種狀態分開報：`same`／`changed`／`unknown`（有公式但我方沒記錄）／`absent`。

## ⭐ 交易所的漲跌停是**有年代的**：2015-06-01 由 7% 放寬為 10%

⇒ `price_limit.py`（唯一的一份實作）。漲停 = 參考價×(1+cap) **無條件捨去**到升降單位、
跌停 = 參考價×(1−cap) **無條件進位**；升降單位看**算出來的那個價**落在哪一檔。

⛔ 而跌停側**故意不收緊**：3536 誠創 2015-03-20 官方跌停 **12.15**、公式算 12.40，
⚠ 而我方那天的收盤就是 12.15（跌停鎖死）⇒ 對不上的是公式，不是我方的參考價。
⇒ 成因未知（⛔ 不知道就寫不知道）；等 TWTAUU 的官方漲跌停欄回補下來再用官方數字重判。

---

## ⭐⭐ `data/universe/exright/` 2026-09-13 從七欄變**十欄**

```
date, stock_id, pre_close, ref_price, value, kind, open_base,
⭐ limit_up, limit_down, ex_div_ref
```

| 欄 | 官方欄名 | 是什麼 |
|---|---|---|
| `limit_up`／`limit_down` | 漲停價格／跌停價格 | ⭐ 交易所**逐筆**給的，涵蓋自民國 92-05-05 |
| `ex_div_ref` | 減除股利參考價 | **不計現金增資稀釋**的價（⛔ 不是「只扣現金股利」，見下） |

### ⛔ 三件讀這幾欄之前一定要知道的

**⓪ 這兩欄只在【除權息事件表】裡有，⛔ 價格檔沒有。**

```
✅ data/universe/exright/<日>.csv　　1,759 個日檔｜11,751 列
　　　2015-01-05 ~ 2026-09-10｜limit_up／limit_down **100% 有值、沒有空段**
⛔ data/stocks/<code>.csv　　　　　沒有這兩欄，現在也不會有
⛔ data/universe/daily/<日>.csv　　同上
```

⚠ 官方只在「除權息／減資公告」那張表逐筆給漲跌停 ⇒ **只有除權息當天查得到**。
⛔ 不是除權息日的日子，官方逐筆漲跌停**根本不存在**（⚠ 不是「查不到」）——
⇒ 拿一個平常日去查會得到空白，⛔ 而把空白讀成「那天沒有漲跌停」就是偽陰性。

⚠ 而 `data/stocks/` 與 `data/universe/daily/` 有一個**名字很像的 `limit` 欄**，
⛔ **意思完全不同**（它是「一價到底」旗標，見上面 5.x 那一節）。
⭐ 兩者連檔案都不同——2026-09-14 就有一條線在價格檔裡找 `limit_up` 找不到。

**① `9,999.95` / `0.01` ＝「無漲跌幅限制」的旗標，⛔ 不是價格。**
（00714 群益道瓊美國地產 2020-01-16 就是這樣。）
⇒ 用 `price_limit.is_unlimited()` 判，⚠ 而那一筆是**沒有判準**，
⛔ 不可以算成「通過漲跌停檢查」——算了就是把母體灌水。

⛔⛔ 而**判準是比例，不是絕對值**（2026-09-14 訂正，代價是一筆被靜靜吃掉）：

```
第一版　`limit_up >= 9000 or limit_down <= 0.02`
⇒ 2059 川湖 2026-08-27（前收 14,510、官方漲停 15,900、跌停 13,015）
　 被判成「無漲跌幅限制」——⚠ 而那 15,900 就是正常的 +10%
⇒ ⛔ 那一筆被踢出母體，而報表上不會少任何東西
⚠ 而門檻是絕對值 ⇒ **前收 > 8,182 的每一筆都中**，股價漲上去它自己長大

現行　真的那一對　1.10/0.90 = 1.2222（7% 時代 1.151）
　　　哨兵那一對　9,999.95/0.01 = 999,995　⇒ 門檻 2.0，⭐ 不隨股價縮放
```

⭐ 全庫實測（11,751 列）：哨兵 **517 列**，⭐ 而**沒有任何一筆只有單邊**是哨兵。

**② 官方的漲停與跌停，基準可以不同。**
```
2353 宏碁 2015-01-05（含現金增資除權）
  漲停 22.80 ＝ ⌊減除股利參考價 21.35 × 1.07⌋
  跌停 19.55 ＝ ⌈除權息參考價   21.02 × 0.93⌉
```
⇒ 兩邊各用對自己**比較寬**的那個基準。
⛔ 所以「一個參考價 ± 一個百分比」結構上對不到每一格（實測漲停只中 96.2%）。

**③ `ex_div_ref` ＝「不計現金增資稀釋」的參考價。**

```
減除股利參考價 =（除權息前收盤價 − 息值）/（1 + **無償**配股率）
```

⛔⛔ 2026-09-14 訂正：我原本在這裡寫「**只扣現金股利、不調整配股**」——**那是錯的**。
它**有**調整配股，只是**只算無償那一半**，⛔ **不含現金增資的稀釋**。

⚠ 而 95.5% 的事件裡它**等於**除權息參考價 ⇒ 隨便挑一天去比兩種口徑，**比不出東西**：

```
xd == ref     11,227 筆（95.5%）  ⇒ 那次沒有現增 ⇒ 兩種口徑相同
xd == 前收       461 筆（ 3.9%）  ⇒ 純現增（無息、無無償配股）
⭐ ref < xd < 前收  63 筆（ 0.5%）⇒ 三個價都不同 ⇒ **只有這一族分得出口徑**
```

⇒ 要做「不計現增稀釋」的序列用這一欄，⛔ 不要自己從 `value` 拆
——`value` 是 `權值+息值` 的**合併**值，而官方 notes 自己寫著它 ＝ 前收 − 參考價
（⇒ 它是**導出值**，拆不開）。

### ⚠ 而「上市有沒有分開的權值／息值」這句否定，成立範圍是

```
【掃描範圍】TWSE mega menu 195 條、TPEx menu.json 485 條、
            TWSE OpenAPI 143、TPEx OpenAPI 225、sitemap 3,109、MOPS 20
【查詢用詞】除權／除息／權值／息值／認購／增資／股利／配股／配息…（19 個）
【適用規模】⛔ 對「回補歷史」不可用；⭐ 對「從今天起累積」可用
```
⇒ 分開的欄位（無償配股率／現金增資配股率／現金增資認購價／現金股利）在
`exRight/TWT48U`，⛔ 而它三個區間＋強制單一 `date` 共六發**回同樣的 68 列**
⇒ 它是**預告表**，沒有歷史。

---

## ⭐⭐ 集保股權分散（`data/tdcc/`）：「沒有歷史」要寫成「**這條路**沒有歷史」

```
✅ 有　　data/tdcc/<資料日>.csv　68,936 列／期｜每檔 17 級（人數／股數／佔比）
⚠ 現況　**只有 2 期**（2026-09-04、2026-09-11）——2026-09-08 才接的，每週累積
```

### ⛔ 而「沒有歷史」這句話要標**對哪一條路**（2026-09-14 訂正）

```
我方現在用的　opendata `getOD.ashx?id=1-5`
　　　　　　　⇒ **不吃日期參數、只回最新一週** ⇒ 對這條路，沒有歷史 ✅ 這句成立
　　　　　　　⭐ 2026-09-14 **實測過了**，⛔ 不再是「看起來不吃」（見下）
⭐ 而官方另有　查詢頁 `qryStockAjax`
　　　　　　　⇒ 查詢頁自己列了 **51 個週別**（約一年）⇒ **官方是有歷史的**
　　　　　　　⛔ 而我方取不到：每一發都回 2 bytes（token 重抓也沒用，
　　　　　　　　 可能還要 Cookie／Session）——`_tdcc_probe.txt` [7] 逐發記著
```

⇒ ⭐ **正確的寫法是「我方目前取不到歷史」，⛔ 不是「沒有歷史」。**
⚠ 兩者對讀者的意義完全不同：前者是一個**還沒解決的工程問題**，
⛔ 後者會讓人不再去試。

⚠ 而這一條是**我方自己的探針早就寫著的**（那一節逐字寫「⛔ 仍然不可以下
『沒有歷史』的結論：查詢頁確實列了 51 個週別，只是我方取不到」）
——⛔ 而我 2026-09-14 還是對外寫了「沒有歷史」。
⭐ 教訓就是 CLAUDE.md 三點 3.5：**寫否定句之前，先 grep 自己家。**

#### ⭐ 「不吃日期參數」的實測（`_tdcc_probe.txt` [8.5]，2026-09-14）

K線分析線指名要我方測（他們沒測）。⇒ 四個欄名 × 最舊那一週 `20250919`：

```
對照組（不帶任何參數）           相異資料日期 ['20260911']
date / dateVal / firDate / scaDate
  ⇒ 每一個都 2,363,628 bytes｜68,935 列｜['20260911']
  ⛔ 四個**逐位元相同** ⇒ 全是假參數（靜靜回最新一期）
```

⭐ **而這一格就是第二點①那條判準的用處**：四發都是 2.36 MB、68,935 列
⇒ 「有回列」「bytes 夠大」**四個都會過**。⛔ 只有「回應自己宣告的資料日期」揭穿它。

⚠ 而錨點取的是**最舊**那一週，⛔ 不是最新——問最新的話參數真假都會回
`20260911`（三點5：一個在所有口徑下都相同的錨點，跟沒有錨點一樣）。

#### ⇒ 而這句否定的**成立範圍**（三點3）

```
① 【查詢用詞】試的是 date／dateVal／firDate／scaDate 四個欄名，⛔ 不是「所有可能的參數」
② 【掃描範圍】opendata 那條路；查詢頁 `qryStockAjax` 是**另一條**（[7][13] 另有紀錄）
⭐ ③ 【適用規模】⛔ 這一條**沒有規模問題**——它不吃就是不吃，跟要幾檔無關
```

⚠ 而 `qryStock` 那條**有**規模問題，⇒ 它的「不可用」必須標規模：

```
全市場 51 週   4,051 檔 × 51 週 ≈ 206,601 次   ⇒ 不可行
追蹤 8 檔      8 檔 × 51 週     =     408 次   ⇒ ⭐ **量完全不是問題**
```

⇒ ⭐ 所以 `qryStock` 卡住的是**技術**（回 2 bytes，疑似要 JS／Session），
⛔ **不是量**。⚠ 兩者混寫成「這條路不可用」會擋掉一件它沒測過的事。

### ⚠ 而級距對照（第幾級 ＝ 幾張）仍然是【未驗】

來源的 CSV **只給代碼 1~17**，沒有級距文字；查詢頁上的級距是 JS 動態組的，
探針抓到的只有一個電話號碼樣的字串。⇒ ⛔ 「千張大戶」這種定義**現在仍然落不了地**。

---

## ⭐⭐ `market` 是**會變的**，⛔ 不是每一檔一個常數

實測（2026-09-13，全庫 3,032 檔）：**29 檔換過市場**。

```
tpex → twse   27 檔   （上櫃轉上市，這是主流）
twse → tpex    1 檔   （6423 億而得：2024-05 創新板 → 2026 已在上櫃）
emerging → tpex 1 檔
```

⇒ ⛔ **拿「最早那一列的 market」當這一檔的市場，會分錯 2,951 筆除權息事件。**
⚠ 而分錯的表現是**靜默**：它只是把一筆上櫃事件算成上市（或反過來），
⛔ 沒有任何地方會叫。

⇒ ⭐ 判準：**要問「這一檔在 ___ 那一天屬於哪個市場」，⛔ 不是「這一檔屬於哪個市場」。**
做法是掃 `data/stocks/<code>.csv` 到那一天為止的最後一個非空 `market`。

### ⛔ 而 `data/meta/stocks.csv` 的 `market` 欄**不能**拿來做這件事

```
語意 ＝ **`last_seen` 那一天**的市場別（每處理一天就覆蓋一次；`name` 欄同理）
⇒ ⛔ 不是「第一次見到的」，也不是「現在的」——是**我方最後一次看到它**的那一天的值
⇒ ⛔ 所以它不能拿來圈歷史母體，也不能回答「2019 年它在哪個市場」
```

⭐ 正確的那一格在**逐日檔**裡：`data/stocks/<code>.csv` 與
`data/universe/daily/<日>.csv` **每一列都有自己的 `market`**。

⚠ 這句話 `feeds.py` 的檔頭早就寫著——⛔ 而它寫在程式碼註解裡，
**讀資料庫的人看不到**。⇒ 這就是它現在也寫在這裡的理由。

⚠ 現況：`backtest/` 那邊全部是 `set_index("stock_id")["market"]` 這種**每檔一個值**的用法
（`research5/11/15/16`、`run.py`、`forward_and.py`、`breakpoint_scan.py`、`exright_gap.py`）
——⛔ 資料庫線只負責把這件事講出來，**怎麼處理是那條線的決定**。

### ⇒ 而這條是從一次**雙向比對**掉出來的

```
官方 TWSE `exright` 日檔事件            11,751 筆
我方 data/adj（事件日當天 market=twse）  11,751 筆
⭐ A 方向（官方有、我方沒有）：**0**
⭐ B 方向（我方有、官方沒有）：**0**
```

⚠ 而第一次跑的時候 B 方向是 **1 筆**（6423 2026-03-26）——
⛔ 那不是資料問題，是**我的市場判定取了最早那一列**。
⇒ ⭐ 單向比對根本不會發現這件事：A 方向從頭到尾都是 0。

【範圍】2015-01 ~ 2026-09、TWSE `exright` 一支端點、**實測**（⛔ 不是推定）。
⛔ 上櫃那一半（`otcexright`，來源是 FinMind）**沒有**官方逐筆表可以這樣比。

---

## ⛔ 上櫃**沒有**逐筆的官方漲跌停價格——而這句否定的成立範圍是

```
【掃描範圍】五份機器可讀目錄，合計 4,520 條（2026-09-14 01:26 台北實測，**5/5 全掃到**）
    TWSE mega menu   281（含中文 276）
    TPEx menu.json   762（含中文 759）
    TWSE OpenAPI     143（含中文 143）
    TPEx OpenAPI     225（含中文 225）
    TWSE sitemap   3,109（⚠ **含中文 0** ⇒ 中文詞對它結構上不命中，靠路徑詞）
【查詢用詞】漲停／跌停／漲跌幅／參考價／減資／換股／除權／除息／權值／息值…（19 個）
            ＋路徑詞 limit／fluctuation／reduction／exright…
【適用規模】對「逐筆事件的漲跌停」不可用；⭐ 而「首五日無漲跌幅」那一格是有的
```

命中的只有這四條，⛔ 沒有一條是逐筆的漲跌停價格：

```
TWSE  /exchangeReport/TWT88U            上市個股首五日無漲跌幅
TPEx  /tpex_ipo_no_limit                上櫃首五日無漲跌幅資訊
TPEx  /tpex_ceil_non_trading            上櫃**漲跌停未成交**資訊（⚠ 是名單，不是價格）
TPEx  /zh-tw/.../rules/fluctuation-limit.html  放寬漲跌幅度專區（說明頁）
```

### ⚠ 而這句否定還差一步才完整（`data-source-recon` §六②）

那份技能要求「名字疑似相符的頁面**都開過、都攔過它自己的請求**」才可以換站／下結論。
⛔ 我**沒有**開 `fluctuation-limit.html` 與 `no-limit.html` 攔請求。
⇒ 所以嚴格的寫法是：**「這五份目錄上沒有」**，⛔ 不是「TPEx 沒有」。

⇒ ⭐ 實際後果：`factor_limit_check` 的上櫃那一半只能靠推論，
而那條推論**對現金增資除權會誤報**（全庫漲停側 95.39%）。
⇒ 目前 2 筆「超出」都是上櫃 ⇒ ⛔ 標「未證實」。

---

## ⭐⭐⭐ `cum_factor` 系統性偏低的成因找到了：**參考價是無條件捨去到「分」**

### 一、直接量出來的事實（⛔ 不是推論）

官方 `exright` 11,751 列，把 `ref_price` 跟「未捨入的參考價」（＝`pre_close − value`，
而 `value` 官方給到**六位小數**）逐列比：

```
剛好相等            7,637（64.99%）
ref 比未捨入的**低** 4,114（35.01%）   最多低 0.0100
⛔ ref 比未捨入的高      **0**
```

⇒ ⭐ **官方參考價是無條件捨去到分**（若是四捨五入，高的那一格會有約一半）。
【範圍】2015-01 ~ 2026-09、TWSE `exright`、**實測 11,751/11,751**。

### 二、而它會**連乘**（CLAUDE.md 第二點：一筆假資料的半徑是整條序列）

我方 `factor = ref_price / pre_close` 用的是**捨去後**的 ref
⇒ 每一個事件都少一點點，而 `cum_factor` 是連乘 ⇒ 累積成單邊偏差。

```
受影響 920 檔（⚠ 上櫃沒有官方 `value`，不受影響）
  cum_factor 相對差：⛔ **920 / 920 全為負**
  中位 −3.87e-04｜最差 −2.90e-02（9105，25 個事件）｜2330 −5.76e-05（33 個事件）
```

⭐ 而 2330 的 **−5.76e-05** 跟 TradingView 線量到的 **−5.68e-05** 對得上
⇒ 那條「我方系統性偏低」的觀察，成因就是這個。

### ⇒ ⛔ 而我先前對它的兩次判讀都只對一半

```
第一次：「上市是截斷」          ⇒ 方向對，⛔ 而我說的是「最終價格被截斷」
第二次：「不是截斷，是固定比例」⇒ 現象對，⛔ 而我從「比例固定」推出「不是截斷」
```
⭐ 正解是**兩個都要**：**上游每一筆的參考價被捨去，經過連乘變成下游的固定比例。**
⚠ 「殘差是固定比例」**不能**用來否定「上游是截斷」——連乘會把前者變成後者。

### 三、⇒ 可以修，而且修法就在官方資料裡

```
現況：factor = ref_price / pre_close                （ref 只到分）
更準：factor = (pre_close − value) / pre_close      （value 到六位小數）
```
⛔ **而這是一個判準決定，不是施工**：它會動到 920 檔的整條還原序列（最大 2.9%），
⚠ 而別條線的回測掛在現況上。⇒ 由 K線線裁定要不要換、以及換的時點。
