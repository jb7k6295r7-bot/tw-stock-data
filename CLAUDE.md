# tw-stock-data — 動手之前一定要知道的事

⛔ 這一頁不是簡介。它記的是**已經付過代價**的規矩，每一條後面都有一次事故。

---

## ⭐⭐ 一、接新端點：**第一件事是把 `notes`／`hints`／`title` 印出來，再開始比對**

使用者 2026-09-10 定的。⛔ 這是**順序**，不是建議。

```python
import backfill as B
print("\n".join(B.describe_response(d, want={我送出去的參數})))
```

### 為什麼

我方 `_tables()` 只取 `title`／`fields`／`data`，其餘
（`stat`／`date`／`notes`／`hints`／`params`／`total`…）**整個丟掉，而且沒有紀錄**
——連「有這些鍵」都不知道。而那些鍵每一個都對應一件我方**手工做過**的事：

| 鍵 | 直接回答 | 我方原本怎麼做 |
|---|---|---|
| `total` | 官方說它給了幾列 | 另外造 N₁、Σamount÷大盤、六張清單差集（67,446 筆） |
| `params` | **我送的參數有沒有生效** | 在三支程式裡各寫一道「回應要講出我請求的那一天」 |
| `notes` | 符號說明／涵蓋起始年／單位 | 用推論，而且**沒有留下出處** |

⚠ `params` 會把收到的參數**回顯** ⇒ 被換掉就代表**那個參數是假的**。

已經做在 `feeds.py --probe` 的第一行（跳不過去）與 `keys_probe.py`。
完整順序見 `docs/NEW_ENDPOINT.md`。

---

## 二、⛔ 所有「靜默失敗」都長成 `stat:OK`

2026-09-09~10 一天之內踩到的四種，**每一種都是 200 ＋「成功」**：

```
① 靜靜回今天        參數格式錯／該用 POST 卻用 GET／查詢越界
② 靜靜回最新一期     MOPS t164sb03 的 year／season 四種組合回應完全相同
③ 回「查無資料頁」   hist.tpex 的 4,449 bytes ⇒ 32 個年份「全部命中」
④ 被 CDN 擋回 HTML   TWSE HTTP 428（只 try/catch JSON 會看成「解析失敗」）
```

⇒ 判準一律是「**這一批要自己講出它是哪一天／哪一期**」。
⛔ 不可以用「檔案夠大」「有回列」「`stat` 是 OK」當判準。

### ⭐ 而 2026-09-11 加一種：**端點的名字不是證據**

```
⑤ 名字叫 `history`、其實只有今年
   TPEx `openapi/v1/tpex_spendi_history`（「上櫃**歷史**暫停/恢復交易」）
   → 362 列、8 欄，看起來像一份歷史
   ⛔ 而 `Date` 欄的相異值只有 **1 種：民國 115（＝2026）×362**
   ⚠ 而且 181 檔裡混著權證（`CompanyName` 有「元大57購01」）
```

⚠ 這一族**頂層是陣列**（openapi.twse／tpex 那一家），
⛔ 沒有 `stat`／`total`／`notes` 可以問 ⇒ 第二點的「自己講出它是哪一期」它做不到。

⇒ ⭐ 落地：陣列型回應一律印**每個鍵的相異值分佈**（`delist_probe._spread()`）——
相異值只有一種的鍵（這裡的 `Date`）**當場就把涵蓋期間講出來了**；
相異值多的只印最小最大，⛔ 不洗版。

### ⭐⭐ 而 2026-09-11 再加一種：**官方表自己重打了一列**，而傷害會往前蔓延

```
⑦ TPEx `bulletin/revivt`（上櫃減資）283 列裡有一組
   6109 亞元：`2018-09-25` 與 `2020-09-25`
   ⛔ `last_close`／`ref_price`／原因／換股率／退還股款 **六個數字逐位相同**
   ⇒ 官方把 **1070925 打成 1090925**（283 列裡就這一組）
```

⚠ 它跟前六種不同的地方：**來源沒有壞、參數沒有錯、回應完全正常**——
⛔ 是那張表**自己**有一列是錯的。⇒ 「問回應」問不出來，只能**拿我方資料去驗**。

#### ⛔⛔ 而它的傷害**不只那一列**：`cum_factor` 是**連乘**的

```
data/adj/6109.csv 多一列 2020-09-25 f=1.01238095
⇒ ⛔ 2020-09-25 **之前每一列**的 cum_factor 全部多乘一次
   （2018-09-25 那列 0.96668147 → 0.96486184）
⚠ 而檔案看起來完全正常：欄位齊、數字連續、沒有任何地方會報錯
```

⇒ ⭐ **通則：凡是「連乘／累加」型的輸出，一筆假資料的半徑是整條序列，不是一列。**
⛔ 所以這種閘門要擋在**進入連乘之前**，不是事後報一個數字。

#### ⇒ 判準：**停止買賣型事件一定有停牌缺口**（`adjust.halting_event_is_real()`）

```
① 我方在事件日之前最後一筆收盤，就是事件日的**前一個交易日**（⇒ 沒有缺口）
② 而且官方的「停止買賣前收盤」跟我方那一天的收盤**對不上**
⇒ 兩條同時成立才判它不成立
```

⚠ **兩條缺一不可**：
只有②（有缺口、價格對不上）是我方價格／來源的問題 ⇒ **要報給人看，⛔ 不是自己吞掉**；
只有①（沒缺口、價格對得上）多半是官方把不停牌的種類也放進同一張表。
⛔ 而這條**只准對停止買賣型生效**——除權息本來就不停牌，
套上去會把任何一筆「前收對不上」的除權息**靜靜刪掉**（突變驗當場抓到）。

⭐ 而它是**算出來的**，⛔ 不是一份寫死的黑名單：
黑名單只認得已經發生過的那一筆，⚠ 而官方下次重打的會是別的代號。
（實測：283 筆官方上櫃減資裡**只判掉 1 筆**，就是那一筆。）

### ⭐⭐ 而 2026-09-14 再加一種：**同一張表裡，有的欄可以信、有的不行**

回測線問「本庫有沒有加權指數日線」。⇒ `calendar_audit.py` 逐月抓的 TWSE `FMTQIK`
裡就有，⚠ 而它只留首欄日期，其餘五欄整列丟掉 ⇒ 看起來是一個純賺的回補。

⛔ 而拿**同一天**跟我方既有的值對一次（2026-09-01）：

```
                我方（MI_INDEX type=IND）        FMTQIK
成交金額        1,090,449,046,456        1,187,571,567,117   ＋8.9%
成交股數            5,209,282,128           13,000,849,196   ⛔ **2.5 倍**
成交筆數                4,224,598                5,301,801   ＋25%
⭐ 發行量加權股價指數      46,948.72                46,948.72   **逐位相同**
```

⇒ ⭐ **這不是「FMTQIK 壞掉」**——它是對的，只是**口徑不同**（含不含鉅額、
零股、盤後定價、外幣成交值）。⛔ 而口徑差異**只落在總量那三欄**，指數那一欄不受影響。

#### ⭐⭐ 而 2026-09-16 把「是哪幾種」**量出來了**（⛔ 原本那一句是猜的）

`MI_INDEX?type=ALLBUT0999` 的**表 6「大盤統計資訊」**本來就逐類列著（15 類 ＋ 兩個合計），
⚠ 而我方 `_tables()` 只取需要的那幾張 ⇒ 這張一直沒被印出來。
⇒ 2026-09-09 逐位對一次：

```
我方 data/history/market_amount.csv  762,135,234,946｜4,494,280,166｜3,421,183
官方「**1.一般股票**」那一列        762,135,234,946｜4,494,280,166｜3,421,183
⇒ ⭐⭐ **逐位相同** ⇒ 我方取的是「一般股票」，⛔ 不是「總計(1~15)」
```

⇒ 而差額（總計 − 我方）**逐欄拆得乾乾淨淨**：

```
           差額             權證      ETF      TDR    創新板   四者合計
成交金額   53,841,443,162    5.8%    **85.0%**   0.4%    8.8%    99.9%
成交股數    4,763,800,054  **56.7%**   42.0%    0.5%    0.8%   100.0%
成交筆數          727,119   12.6%    **84.1%**   0.7%    2.6%    99.9%
```

⇒ ⭐ **而「佔比最大的是誰」逐欄不同**：金額與筆數是 ETF，股數是**權證**
——⚠ 因為權證單價低 ⇒ 吃掉很多股數、卻只佔一點金額。
⇒ ⛔ 所以「兩邊差幾 %」這個問題**沒有單一答案**，它是**每一欄各問一次**的問題
（也解釋了為什麼 FMTQIK 那三欄的差是 ＋8.9%／**2.5 倍**／＋25%，量級完全不同）。

⚠ 而這一段的掃描範圍是**一天**（2026-09-09）⇒ ⛔ 比例會隨當天行情變，
⭐ 但「我方 ＝ 一般股票那一列」是**逐位相同**，那一句跟比例無關。

⇒ ⛔ **「這個端點可不可信」問錯了問題。要問的是「這個【欄】可不可信」。**
⚠ 若照「它跟我方對得上 ⇒ 整張表都能用」去回補 `market_amount.csv`，
2015~2026-08 會是一種口徑、2026-09 起是另一種，⛔ 而接縫**不會報錯**：
`merge_history` 主鍵是 date，兩段日期根本不重疊。

#### ⭐⭐ 而 2026-09-16 是它的**鏡像**：同一族的**兩張表**，同名的欄是不同的量

上面那條是「同一張表裡，有的欄可以信」。⛔ 這一條是**跨表**：

```
                上市 FMSRFK（月）          上櫃 monthlyStock（月）
high／low       **盤中**最高／最低          **收市**（收盤）最高／最低
avg             **加權**平均（金額÷股數）    **收盤價的簡單平均**
成交量單位      股                          **仟股**
```

⚠ 而**欄名一模一樣**（都叫「最高價」「平均價」）。⭐ 兩邊各拿我方日檔驗過：
上市那張 3／3 對上**盤中**、上櫃那張對上**收盤**（6488 113/1：收市 603／盤中 604）。

⇒ ⛔ 併進同一張表 ＝ 一次做出**三道**靜默的定義接縫
（而主鍵是代號＋年月，兩段代號不重疊 ⇒ **不會報錯**）。
⇒ ⭐ 判準：**跨表合併之前，每一欄都要各自拿我方資料驗一次「它是哪一個量」**，
⛔ 不是看欄名，也不是看它們像不像。

⚠ 而同一族的**年**表又是另一種組合：兩邊的「盤中最高」都是盤中、
「收盤平均價」都是簡單平均 ⇒ ⛔ **年表可以合、月表不可以** ——
**同一族、同一個供應商，逐張表都要自己驗。**

#### ⭐⭐⭐ 而 2026-09-16 是第三種：**同一張表、同一欄，在【某一年】是另一個口徑**

前兩種是「同一張表裡有的欄可以信」與「跨表的同名欄是不同的量」。
⛔ 這一種的主詞換成**年**——⚠ 而它比前兩種更難看出來：那一欄**每一年都長得一樣**。

拿官方年表（`FMNPTK`）跟我方日檔加總逐格比（抽樣 506 檔、4,834 個 (檔,年)）：

```
民國年  可比   Δ股數≠0        Δ金額≠0
 104    209      9   4.3%      9   4.3%     ⭐ 兩欄的比例**一模一樣**
 105    214     16   7.5%     16   7.5%
  …                  …                …
 108    232     15   6.5%     15   6.5%
⭐⭐ 109  234      9   **3.8%**  166  **70.9%**   ⛔ 兩欄**當場分家**
 110    237     12   5.1%     12   5.1%
  …
 114    273     24   8.8%     24   8.8%
```

⇒ 其他 11 年：兩欄的不符率**逐年相等**（同一個成因同時動到兩欄）。
⛔ 而 109 一年：股數的不符率是**十二年最低**，金額卻是 70.9%。

⇒ 拆開來看，109 的 234 檔是**兩堆**：

```
9 檔    Δvol≠0 ⇒ ⭐ 9／9 **不是 1,000 的倍數** ⇒ 跟其他年一樣是**零股**
225 檔  Δvol == 0 **而且** Δtx == 0 ⇒ 同樣的成交
        ⭐ 其中 157 檔金額**我方多** 1~787 元（p50 8 元／全年）、68 檔逐位相同
        ⛔ 官方多的：**0 檔**
```

⇒ ⭐ 所以它是一個**只作用在金額、只作用在 109 年**的效應，
⚠ 而它跟零股那個效應**疊在同一格上**（2321／109 的隱含單價 1.06 元就是這樣來的
——⛔ 我一開始把那一格讀成「零股解釋不了」，其實是兩個效應相消）。

⇒ ⛔⛔ **成因我不知道，那就寫不知道。** 而**形狀**是量出來的：
⛔ 不是固定比例（相對超額跨三個量級）、⛔ 不是我方日檔有壞列
（「均價落在當日高低之外」的比例 2020 是 0.21%，比 2021 的 0.45% 還低）、
⛔ 不可能是多算／少算一天（Δvol 與 Δtx 都是 0）。

⇒ ⭐ **判準：一欄的可信度要【逐年】量一次，⛔ 不是量一次就當它整段都一樣。**
⚠ 而看得出來的方式只有一種：**把兩個相關的欄並排逐年列出來**
——它們平常同進同出，⭐ **分家的那一年就是口徑換掉的那一年**。

#### ⭐⭐ 而同一次比對還踩到：「這一列可不可比」要用**資料自己**，⛔ 不是另一張表

回測線排除**轉板年**用的是 `delisted.csv`「該年有 tpex 下市紀錄」。
⚠ 而**上櫃轉上市**在我方資料裡的表現是：`data/stocks/<sid>.csv` 同一年裡
`market` 欄從 `tpex` 變成 `twse`——⭐ **資料自己講得出來**。

```
漏掉的後果（我的抽樣）：
  「我方量比官方多」5 個 ⇒ 全部是轉板年（Δvol 都是 1,000 的整數倍 ＝ 整股）
  年均價「殘差」54 個 ⇒ 其中 4 個差到 4~17 元（⛔ 不是 ±0.01 那一族）
  ⭐ 只取 twse 那一段重算：3／4 **逐位相同**（1256/105 124.08、3563/108 349.66、
     5306/111 160.73），第 4 個回到 ±0.01
```

⇒ ⭐ 通則跟第二點那一族同源：**判準要問「這一列自己說了什麼」**，
⛔ 而「去另一張表查它有沒有被記一筆」多一層，⚠ 而那一層有它自己的涵蓋範圍
（`delisted.csv` 記的是**下市**，⛔ 轉上市不是下市）。

#### ⇒ 而 `notes` 自己也會變——**同一個 URL、同樣參數、24 分鐘內**

```
09:03Z  當日統計資訊含一般、零股、盤後定價，**不含鉅額**、拍賣、標購。／**不加計**外幣
09:27Z  當日統計資訊含大盤、零股、盤後定價**及鉅額交易**，不含拍賣、標購。／外幣**加入**成交金額
```

⚠ `title`／`date`／`stat`／`fields` 四個**完全相同**，只有 `notes` 換了，而且講的是**相反**的口徑。
⇒ ⛔ **`notes` 是說明文字，不是規格**——它可以用來提示「有口徑這回事」，
⛔ 不可以拿它當「這一欄含不含什麼」的依據。⭐ 唯一算數的是**拿兩條路的數字對一次**。

⇒ ⭐ 落地（`calendar_audit.write_index()`）：
**重疊的那幾天不是「跳過」，是【閘門】**——對不上就**一列都不寫**並且 runlog 紅。
⚠ 而它同時擋掉另一半：既有那幾列是 `fetch` 寫的、`change`／`change_pct` 有值，
FMTQIK 給不出 `change_pct` ⇒ 連同寫進去會把那兩欄**洗成空白**（四點六）。

---

## 三、⛔ 三個常犯的推論錯誤

1. **只比一個方向就宣告一致。**
   2026-09-10：只比「官方有的我方有沒有」⇒ 宣告某端點可當完整來源；
   補比反方向才發現它少 17 筆減資 ⇒ 照原結論做會**刪掉還原因子**。
2. **把觀察到的範圍寫成通則。**
   「我查過的那幾條端點沒有」**不等於**「官方沒有」——同一天被推翻兩次。
   同一族／同一站的不同端點，參數行為**互不相同**，每支都要自己實測。

### ⭐⭐⭐ 2.5 看到一條判準**會漏判**時，先問它**會不會誤判**（2026-09-16）

回測線用「Δvol **不是 1,000 的倍數** ⇒ 官方多出來的是零股」在標量差。
⇒ 我找到一個反例（1603／107：Δvol 剛好 ＝ 1,000、Δtx ＝ 4 ⇒ 每筆 250 股，
是零股湊成整數）⇒ ⛔ 我就寫信叫他們**把那條判準換掉**，改用
「Δvol ÷ Δtx < 1,000 ⇒ 必含零股」。

⛔⛔ **那個「換」字是錯的。** 那條判準**翻船的方向只有一個**：

```
整股類（普通／盤後定價／鉅額／拍賣／標購）**一律以「張」成交**
⇒ 它們的和**必定**是 1,000 的倍數
⇒ ⭐ 餘數 ≠ 0 ⇒ 裡面**至少有一筆不是整張** ⇒ **必含零股**
⇒ 它**只會漏判**（剛好湊成整數的那種），⛔ **不會誤判**
```

⇒ ⭐⭐ **一條只會漏、不會錯的判準，不該拿掉，該跟別的做「或」。**

⚠ 代價量得出來：他們照我的話換掉之後，全庫剩 **10 列**判不出來；
⭐ 改成「①不是千的倍數 **或** ②每筆<1,000」之後，**9 列當場判掉**，
⛔ 只剩 1 列（`6949／113`：681,000 股／1 筆，兩條都不成立）真的判不出來。

⇒ 判準：**「充分條件」與「必要條件」不可以混著丟。**
一條充分條件抓不到某些正例是**正常**的（它本來就不是必要條件）
——⛔ 而我把「它漏了一個」讀成「它是錯的」，然後叫人刪掉它。

⚠ 而這一族要連**前提**一起寫：①成立的前提是「除了零股，所有交易類型都以張為單位」
——⭐ 我查得到五種，⛔ 而我**沒有**逐條查官方規章、也沒窮舉「有沒有第六種」
⇒ 那一格的說明要寫出**是哪一條在判**，⛔ 不是只寫結論
（前提哪天不成立時，讀的人才看得出是哪一條開始誤判）。

### ⭐⭐ 3. 否定句要連同**適用規模**一起寫（2026-09-13）

我寫了一句：「分點沒有可落地的來源，**這條可以收了**」，
並附兩個理由：① 條款禁重製 ② 回補一年要 47.9 萬次請求打一個免費站台。

使用者一句話戳破：**「如果是要追蹤分析的一檔會有問題嗎？」**

```
全市場回補一年   1,954 檔 × 245 交易日 ≈ 478,730 次
追蹤一檔回補一年  1 檔 × 245 交易日   =     245 次
⭐ 而 ?day=365 一次請求就回整年       =       1 次
```

⇒ ⛔ **②是一個【規模】的理由，而我讓它縮放成了「任何規模都不行」。**
⚠ 而①（條款禁「重製」）**跟量無關** ⇒ 兩個理由的適用範圍**根本不同**，
⛔ 我把它們並排寫成「兩個都獨立成立」，看起來像「到處都成立」。

#### ⇒ 而同一個錯我在**同一天**犯了第二次，對象是官方端點

我把 `bsr.twse.com.tw`（官方券商買賣日報表）判成不可行，理由三個：
**有驗證碼／逐檔查／只有當日**。
⭐ 而那三個**全部是規模的函數**——對「追蹤幾檔」一個都不痛：

```
驗證碼   擋的是**自動化**。一檔一天輸一次，人做得到
逐檔查   你本來就只查一檔
只有當日 從今天開始一天記一次，一年後就有一年
```

⇒ ⛔ **判死的不是那條路，是「全市場自動化」那一格。**

#### ⇒ 判準

> **「這個來源不可用」必須寫成「對 ___ 規模不可用」。**
> 擋住一個來源的通常是**驗證碼、請求量、逐檔查**——⭐ 而那三個全是規模的函數。
> ⇒ 在全市場成立、在單檔不成立是**常態**，不是例外。
> ⛔ 沒有標規模的「不可用」，下一個人（包括寫它的人）會拿它去擋一件它沒測過的事。

⚠ 而它跟另外兩條是同一族，三條回答的是同一個問題：**一句否定，成立範圍是什麼**。

```
① 【掃描範圍】站層級／個股頁／全站選單逐條   ← 只掃了兩個站層級的頁就寫「這站沒有」
② 【查詢用詞】「查過了、沒有」要寫用什麼詞查 ← 同一份條款，換個關鍵字就翻出禁止條文
⭐ ③ 【適用規模】「不可用」是對哪一種規模說的  ← 本條
```

⚠ 三條分別由三條線各自獨立踩到 ⇒ **這不是誰粗心，是這個工作本身的形狀。**

---

### ⭐⭐⭐ 3.5 寫否定句之前，**先 grep 這個 repo**——同一句已經被推翻四次了（2026-09-14）

三點①②③講的是「掃描範圍／查詢用詞／適用規模」。⛔ **三條都在講外面。**
⚠ 而 2026-09-14 我寫下：

> 「上櫃除權息官方**只給當日**，補不回來。」

⇒ ⛔ `data/meta/otc_exright_history.csv` **早就在 repo 裡**：
13,106 列、**2008 年起**、來源櫃買公告區 `bulletin/exDailyQ`（一次請求回十一年半）。

⚠⚠ 而 `otc_exright_history.py` 的**檔頭第一句就是**：

> 「我對三個地方寫過『上櫃除權息的歷史官方來源：仍然沒有』……⛔ **那句話是錯的。**」

⇒ ⭐ **同一句否定句在這個 repo 裡第四次被推翻，而前三次的紀錄就寫在
我要動的那個檔的檔頭裡。⛔ 我沒讀它，就又寫了一次。**

#### ⇒ 判準：三點①②③要加第四欄【**自己家查過沒有**】

```
① 【掃描範圍】站層級／個股頁／全站選單逐條
② 【查詢用詞】「查過了、沒有」要寫用什麼詞查
③ 【適用規模】「不可用」是對哪一種規模說的
⭐ ④ 【自己家】`grep -rn "<那個東西>" .` ——⛔ 這一步**最便宜而且最常漏**
```

⚠ 為什麼最常漏：外面的東西我知道要去查，⛔ **而「我們自己有沒有」我以為我知道。**
⭐ 而這個 repo 有 200 多支程式與十幾份判準檔，**沒有人記得住全部**
——檔頭寫的教訓只有在**有人去讀**的時候才有用。

⇒ ⛔⛔ 而它有一個放大版：**要動某個檔之前，先把那個檔的檔頭讀完。**
⚠ 本次的前三次紀錄，就在我當天讀過三次的那個目錄裡的另一個檔。

### ⭐⭐ 4. 裁定之前**一律先掃一次收件夾**——「我手上這份」不等於「最新那份」（2026-09-13）

```
12:26  TradingView線 寄出「我 1203 那封請作廢，是我把 ADJ 開關讀錯了」
12:42  我寄信給 K線，內容建立在 1203 的前提上
12:48  我寄信給 TradingView線，**裁定** 1203 的 A/B/C
```

⛔ 那封撤回信在我兩封信之前 **16 分鐘**就在信箱裡了。
⚠ 我是拿**使用者轉貼給我的那一份**當最新狀態，直接裁下去 ⇒ **兩封信都要作廢**。

⭐ 而這件事真正的教訓不是「記得看信」，是：

> **一個對的推導，掛在一個過期的前提上，結論照樣是錯的。**
> ⚠ 而它讀起來跟對的**一模一樣**——⛔ 錯的地方不在任何一行推導裡。

⇒ 落地兩條：
- **要裁定／要下結論之前，先 `search_files` 掃一次收件夾**，
  ⛔ 不拿轉貼、不拿逐字稿、不拿記憶裡的版本當最新狀態。
- ⭐ 而對方**自己標出來的限制**要當成清單逐條核：
  這次 TradingView線 在 1203 §五就寫了「『TV 因子 = 4/3』嚴格說是**推算**，不是實測」
  ——**病根就在那三條裡的一條**。⚠ 他標了所以他沒錯，⛔ 我沒核所以我錯了。

⚠ 而它跟四點二是同一族，只是主詞從「綠燈」換成「我手上這份文件」：
**「它看起來完不完整」跟「它是不是最新的」是兩件事。**

#### ⭐ 而 2026-09-15 使用者定：**信處理完就自動移走，⛔ 不要等我說**

⇒ 歸檔規則（⚠ 比 K線分析線 1850 那條嚴一格，而嚴的理由是**我差一點做錯**）：

```
✅ 移走：我是**唯一**收件線（`件-資料庫線←…`，沒有「會同」）而且已答覆
✅ 移走：會同的信，但簽收標記**全部是 ✔**
⛔ 不移：會同的信而還有人是 ✖　⇒ ⭐ 只把**我自己那一格**簽成 ✔
⛔ 不移：標題開頭不是本線的（那是別人的收件匣）
```

⛔ 病根：我照「標題開頭是本線就移」把一封會同信移走了，
⚠ 而它標題上掛著 `〔簽收 資✖ K✖ 回✔〕`——**K線分析線還沒簽**
⇒ 我一移走就等於把它從還沒簽的人眼前拿掉。（已移回並簽成 `資✔`。）

⇒ ⭐ 通則：**「我處理完了」與「這封信可以收起來了」是兩件事**
——⛔ 一封會同信的壽命是**最後一個收件人**決定的，不是第一個。

### ⭐ 5. 要判「一個開關／參數管什麼」，**只准動那一個變數**（2026-09-13）

TradingView線 判那顆 `ADJ` 按鈕管什麼，用的是**同一檔、同一天、只切那顆開關**：

```
1563 2026-07-14   ADJ 關 85.999785     ADJ 開 80.17656722
1563 2026-08-26   ADJ 關 87.99978      ADJ 開 84.66645481
```

⇒ 兩個量都在，而且差別只可能是那顆開關。⛔ 換檔、換日期各量一次的話，
⚠ 差異可以被歸給任何一件事，而那正是我 1248 裁錯的方式。

#### ⛔⛔ 而那顆按鈕的**名字是錯的**

```
aria-label = "調整股息數據"      ← 只寫股息
實際上：**連退還股款一起扣**     ← 1563 那 2.50 元退還股款也被它扣掉
```

⇒ ⛔ **不要照名字推一個開關／欄位管什麼。**
⚠ 這跟 `_pick` 那條是同一族（`"除權息"` 命中的是「除權息**日期**」欄
⇒ `kind` 整欄存進日期）——**名字對得上不代表那是同一個東西。**

#### ⭐⭐ 而錨點要落在**會讓兩種口徑分岔**的地方

我給對方的判準是「附一格『還原因子必定 = 1』的日子當錨點」
（＝最後一次事件**之後**的任一天）。⛔ 而那正好是兩種口徑**必定相同**的那一格：
實測 1563 的 2026-09-11 在 ADJ 開與關**都回 76.00**。

⇒ ⭐ **一個在所有口徑下都相同的錨點，跟沒有錨點一樣。**
⇒ 錨點要取最後一次事件**之前**的一根——那一根兩種口徑會給不同的值。

### ⭐⭐ 6. **絕對誤差**與**相對誤差**看起來會一樣——而結論相反（2026-09-13）

TradingView 量到我方還原價比他們低，殘差 **全部落在 (0, 0.01)**。
⇒ 我讀成「我方的參考價被無條件捨去到分」，並算了一個「若全庫都截斷」的上界。

⛔ **錯。** 把**相對**殘差算出來：

```
5.77e-05、5.68e-05、5.68e-05、5.68e-05、5.68e-05   ← 五格到三位有效數字都一樣
```

⇒ 那是**固定比例**，⛔ 不是固定絕對量。
⚠ 它看起來像截斷，只因為那幾格的價格都在 100~106：`5.7e-05 × 106 ≈ 0.006`，
**剛好**落進 (0, 0.01)。

#### ⇒ 而量級給了算術上的反證

2330 在該日之後有 32 個事件。若每一筆 ref 真的都被截斷（真值 = ref + 0.005）：

```
ref + 0      ⇒ 離對方 +5.68e-05   ← 我方現況
ref + 0.005  ⇒ 離對方 −2.87e-04   ← 若全部截斷，**超過頭 5 倍**
```

⇒ ⭐ **「每一筆都截斷」被推翻。** 而**方向**仍然成立（13/13 同號，p = 2.4e-04）
⇒ 系統性偏低是真的，⛔ 成因不是截斷，而我不知道是什麼 ⇒ **那就寫不知道**。

#### ⇒ 判準

> **看到一串殘差落在同一個區間時，先問它是「絕對量固定」還是「比例固定」。**
> ⚠ 當那一批數字的量級接近（這次都在 100 附近），**兩者長得一模一樣**。
> ⛔ 而它們的成因與處置完全不同：前者是某一格的捨入，後者在**上游的連乘**。

⚠ 而這一條的代價是：我拿那個誤讀算了一個上界（0.142%）發給兩條線，⛔ 事後作廢。
⭐ 真正把它推翻的不是我重想一次，是**對方自己標出「我比的是別人信裡引述的值，
請你用 main 重算一次」**——照做之後，逐位的數字才讓相對誤差顯出形狀。

---

## 四、長時間的抓取：**先實測，而且測整條路**

使用者 2026-09-10 定的。⛔ 只測「抓不抓得到」不夠——
2026-09-09 那趟 2,846 天跑了 5 小時，**「回補」那一步是綠的**，掛在**最後的 push**。

⇒ 兩支長工 workflow 已經寫成：

```
第一段  抓一天 → **真的 push 一次**（推不上去就整趟紅掉，代價 5 秒）
第二段  逐年分批，**每批推一次**（任何失敗最多賠一年）
```

續跑判準一律用**資料自己**（`--need-col`／`--need-notrade`），
⛔ 不要另開台帳——台帳會跟資料不一致。

---

## ⭐⭐ 四點二、⛔ **中間那一步成功了，就被當成整件事成功了**

2026-09-10 一天之內四個 bug，**四個都是綠燈**，而且四個是同一個形狀。
⚠ 第四點（「測整條路」）講的就是這件事，⛔ 只是它被寫成「長工」的特例
——**它不是特例，是通則。**

| 我看到的證據 | 它真正證明的 | ⛔ 被當成 |
|---|---|---|
| `[fetch] 保留其他市場的 2395 列` ＋ `✓ 已推上 main` | 檔案在**工作區**寫對了 | 檔案**搬到 main 了** |
| 台帳檔還在、格式對、程式回 0 | 檔案**存在** | 內容**還在**（其實被洗成一列表頭） |
| `kind` 欄 13,105 列都有值 | 欄位**有值** | 欄位是**對的值**（其實全是日期） |
| 比對算出 C 類 14,497 筆 | **算得出數字** | 算的是**對的母體**（多算了全上市） |

⇒ 判準只有一句：**問「這個綠燈證明的是哪一件事」，然後問「它在錯的時候會不會也一樣綠」。**

### ⛔ 三個已經證實會騙人的「中間點」

```
① 寫進工作區  ≠ 進到 commit  ≠ 搬到 main
   （`push_data.sh` 問的是「本趟改了什麼」——
     ⛔ 而「分支與 main 本來就不同、本趟不必改它」它問不到）
② 檔案存在    ≠ 內容還在
   （累積型的檔被**整份取代**成空的：`save_done`、`write_days`、`_coverage_backfill.csv`
     ——⭐ 這一族已經三個位置了，見第四點六）
③ 欄位有值    ≠ 值是對的
   （`_pick` 是**包含**比對：`"除權息"` 命中的是「除權息**日期**」）
⭐ ④ 派工回 `queued`／`204` ≠ 那一趟會跑
   （GitHub 的 `concurrency` group **只排得下一個 pending**，
     ⛔ `cancel-in-progress: false` 不代表後來的會排隊等——
     再送一趟，**前一個還在等的會被直接 cancelled**。
     2026-09-10 實測：連送四趟，中間兩趟從來沒跑過，
     ⚠ 而 dispatch 四次都回 `204`，**沒有任何地方會說**。）
   ⇒ 派工一趟跑完再送下一趟，並**回頭確認它真的變成 `in_progress`**。
   ⛔⛔ **而「變成 in_progress」也只是中間點**（2026-09-11 付過代價）：
     run 107 我確認過 `in_progress`，⚠ 而它的 job conclusion 是 **failure**、
     全程 **10 秒**——一支判準檔的自檢 `exit 1`，`set -e` 把同一個 step 裡
     後面六行（含換供料本體）全掐死。⇒ 四檔的除權息因子沒落地，
     ⛔ 而我照著那個狀態寫信給 K線分析線，**那封信是錯的**。
   ⇒ ⭐ 派工之後要驗**三件**，缺一件就等於沒驗：
     ① `list_workflow_jobs` 的 **conclusion**（⛔ 不是 run 的 status）
     ② 那一趟**該造成的後果**（檔案裡真的多了那幾列嗎）
     ③ `_last_run.md` 有沒有多出它該寫的區塊
   ⚠ 而③有一個陷阱：`_last_run.md` 是**跨 workflow 累積**的
   ⇒ ⛔ 那一塊可能是**別趟**寫的。**一定要看區塊裡的時戳**——
   2026-09-11 我連續兩趟把昨晚 00:27 那一塊當成這一趟的結果讀，
   ⭐ 而真正的問題是：**那個 mode 根本沒有跑那支驗收判準**。
   ⇒ 順帶一條：**一個步驟要有能驗收自己的判準跟著跑**，
   ⛔ 否則它「做完了沒」只能靠別人的排程事後告訴你。
⭐ ⑤ run 的 conclusion 是 `success` ≠ **每一步**都成功
   （`continue-on-error: true`。2026-09-14 實測：`daily.yml` 有 **62 個**）
   ⚠ 而這 62 個裡有**兩種**，⛔ 而它們在 run 的畫面上長得一模一樣：

       抓資料那些  紅了會在 `_last_run.md` 留下 ✗（它們自己寫 runlog）⇒ 看得到
   ⛔  **13 支自測** 沒有 runlog 區塊 ⇒ 紅了**沒有任何地方會說**

   ⇒ 實際發生過：`selftest_adj_gap.py` 有一條斷言要求現場**一定要有
     未歸因的樣本**，⚠ 而未歸因收斂到 0 正是那一族的**目標**
     ⇒ ⛔ **目標達成的那一天它必然紅**，而它已經紅著（我是改別的東西才撞到）。
   ⇒ ⭐ 落地：`ci_step.py`（記下 rc、⛔ 一律 exit 0）
     ＋ `ci_report.py`（寫成 runlog 區塊 ⇒ 進 commit ⇒ 四條線讀得到）。
   ⚠ **可見性要由『資料』承擔，⛔ 不是由 log**——log 會捲掉，
     而捲掉的東西不算守門。
⭐ ⑥ **「推成功了」≠「東西搬過去了」**（2026-09-15，probe run 101）
   `push_data.sh` 印了 `✓ 已推上 main`，⚠ 而 main 上**只多了 `_last_run.md`**
   ——那一趟寫出來的 **22 份**探針輸出一份都沒到。
   ⇒ 病根：那一步是**一整批** `xargs git checkout "$DC" -- <23 個路徑>`，
     而其中一個在本趟被**刪掉**了 ⇒ `pathspec did not match`
     ⇒ ⛔ **整批失敗 ⇒ 另外 22 個一個都沒取出來**，
     ⚠ 而 `xargs` 的失敗**沒有人接** ⇒ 照樣 commit、push、印成功。
   ⇒ ⭐ 通則：**一個「一次處理一批」的命令，一個壞元素會毒死整批**
     ⇒ 逐一處理 ＋ 記下失敗 ＋ ⛔ **有失敗就不要往下走**。
   ⚠ 掃描範圍要標：自 2026-09-08 起可見的 **386 個** main 資料 commit 裡，
     `data/` 底下**零個刪除** ⇒ 「刪掉 main 上原本就有的檔」那一種從未觸發；
     ⛔ 而 `otc_adj.py --fresh` 會刪掉被追蹤的 `_otcadj_done.csv` ⇒ 那條路一直通著。
⭐ ⑧ **run 的 conclusion 是 `cancelled` ≠ 它什麼都沒做**（2026-09-16，probe run 125）
   ⚠ 這是⑤的**鏡像**：⑤說「success ≠ 每一步都成功」，這一條說
   「**cancelled ≠ 一步都沒成功**」。
   ```
   run 35033086139  conclusion: **cancelled**
   ⭐ 而逐步看：step 13 同步到 main ✓｜step 17 跑十四支探針 ✓
              step 21 Commit 回 repo ✓｜Complete job ✓   ← **每一步都 success**
   ⇒ 實測後果：main 上 `_tpex_probe.txt` 的時戳就是**那一趟**（06:57:46），
     而那一趟的 sync commit 也在 main 上
   ```
   ⚠ 沒有任何一趟新的 probe 去搶 concurrency group ⇒ ⛔ **不是**④那個原因；
   看起來是 job 跑完之後 runner 才被回收／取消。
   ⇒ ⭐ 危險在**反方向**：讀到 `cancelled` 就以為「沒跑到」⇒ 再派一趟
     ⇒ ⛔ 不是冪等的那一步會做**兩次**（例如累加型台帳、append 型 log）。
   ⇒ 判準還是那一句：**驗後果，⛔ 不驗 conclusion**
     ——而「後果」要看**輸出檔裡那一趟自己的時戳**（⭐ `probe_stamp()` 就是為這個而有的）。

⭐ ⑦ **「檔名在 workflow 裡」≠「它會被執行」**（2026-09-15）
   ```yaml
   run: python ci_step.py selftest_mops_history.py
     python selftest_revenue_complete.py        ← ⛔ 這是**續行**
   ```
   ⇒ YAML 折成**一個純量** ⇒ 第二支**一次都沒有被執行過**，
   ⚠ 而「每一支自測都有人跑」那道守門看的是**檔名有沒有出現在 workflow 文字裡**
   ⇒ ⛔ 它一直是綠的。
   ⇒ 兩個方向各一道：`selftest_workflows` 擋「單行 `run:` 有續行」、
     `ci_step.py` 自己**大聲拒絕多餘參數**（⛔ 靜靜忽略就是它藏那麼久的原因）。
```

### ⇒ 落地的做法：**斷言要驗終點，不是驗中間**

```
⛔ 不要斷言「寫檔成功」        ⇒ ⭐ 寫完**重讀**，斷言讀回來的內容
⛔ 不要斷言「這一步跑完了」    ⇒ ⭐ 斷言那一步**要造成的後果**發生了
⛔ 不要斷言「筆數 > 0」        ⇒ ⭐ 斷言那一批**自己講得出它是誰**（第二點）
```

⚠ 而反向那一半同樣重要：`esb_day_repair` 寫完之後**重讀日檔**、
斷言「其他市場一列都沒少」——⭐ 那條就是驗終點；
⛔ 若只斷言「write_universe_day 回了 363」，它照樣會綠。

## ⭐⭐ 四點五、⛔ **同一件事只准有一份實作**——這一族已經第六次了

2026-09-10 使用者：「同一族的錯（同一段邏輯抄兩份、只修一份）已經第三次了。」
⇒ 當天往下掃，**實際上是六次**：

```
① limit 平盤判跌停   fetch 修了、backfill.parse_twse 沒修
                     ⇒ 修好的天天跑、沒修的只有回補才跑
② 「甲」保留無成交列  backfill 修了、fetch 沒修
                     ⇒ 回補補回十一年、每日繼續挖新洞
③ 興櫃 0 價 `_isz`   esb 逐月檔有、fetch 的日檔沒有 ⇒ 52 列 close=0 寫進去
⭐ ④ backfill.parse_openapi ——**①②③ 三個全中**，而且它是興櫃回補走的那一條
   ⑤ write_day「保留沒抓的市場」＋「濾掉權證」只有回補那份有
     ⇒ 每日那份同一天跑第二次、某市場失敗 ⇒ 那個市場的列無聲消失
   ⑥ _kind／_tables／_num／_isz 各兩份（還沒走岔，⛔ 但「還沒」不是判準）
```

### ⛔ 原本寫在檔頭的理由是錯的

> 「刻意複製而不 import，讓它們各自獨立，改一邊不會意外弄壞另一邊。」

⚠ **六次沒有一次是那個方向。** 真正發生的一律是
**「改一邊，另一邊沒跟上，而且沒有人會發現」**——
⛔ 而兩份長得幾乎一樣的程式，**肉眼 review 看不出誰少了哪一行**。

### ⛔ 而往下掃是**十份**，其中一份會憑空造出一個交易日

```
⑦⑧ _iso／_pick     兩支歷史腳本各一份
⭐ ⑨ _same_day      **每日那支根本沒有**
   ⇒ today 是猜的（不知道國定假日與颱風停市）
   ＋ 候選清單最後一條 STOCK_DAY_ALL 沒有 date 參數、永遠回最近一個交易日
   ⇒ 把上一個交易日的行情寫進「今天」的日檔。stat=OK、數字全真、只是別天。
⑩ _post／_num／_is_dash   兩份
```

⇒ 判準：**同一件事只准有一份實作。** 另一邊用別名／import 指過去。
⛔ 「兩邊各留一份、記得同步」已經證明做不到。

### ⭐⭐ 而 2026-09-13 的第七次，加害者是**我自己，而且是在寫新檔的時候**

使用者說「那套全站掃描流程你還是試試看吧」。
⇒ 我照著 `docs/NEW_ENDPOINT.md` 第 −1 步**從頭寫了一支** `site_recon.py`，
⛔ 而 `site_inventory.py` 早就在做同一件事，⚠ 而且已經在 `probe.yml` 的清單裡跑。

⚠ 更貴的是**我寫的那份比較差**：我猜了兩站的首頁路徑，
⭐ 而既有那份用的是正確的選單端點（mega menu 195 條、menu.json 485 條）。

⇒ ⭐ **判準：寫一支新檔之前，先拿「它要做的那件事」去 grep 一次 repo。**
⛔ 前六次是「改一邊、另一邊沒跟上」，⚠ 這一次是**一開始就多了一份**
——而 `selftest_no_dup.py` 抓不到它：兩份的函式本體本來就不一樣。
⇒ 那道守門擋的是**飄移**，⛔ 擋不了**重造**。重造只能在動手之前自己擋。

### ⛔⛔ 而 2026-09-14 的第八次：**修好了一支，另一支沒跟上**——而它正在誤報

```
2026-09-11  otc_exright_check 修好：「未來事件」的基準要用**我方資料最後一天**
　　　　　　 ⛔ 不是「今天」——今天才除息的那筆會被報成「沒落地」
2026-09-14  otc_reduce_history **還在用 `today`**
　　　　　　 ⇒ 6129 普誠 2026-09-14（週一）的減資 vs 我方日檔最後一天 09-11（週五）
　　　　　　 ⇒ 判成「⛔ 未歸因缺口」⇒ runlog 寫「那一檔的還原序列是假報酬」
　　　　　　 ⚠ 而那一天**還沒收盤**
```

⚠ 兩支的那段程式**長得不一樣**（一支叫 `classify`、一支叫 `classify_gaps`）
⇒ ⛔ `selftest_no_dup.py` 抓不到：它比的是函式本體的 AST。
⭐ **同一個判準的兩份實作，只要外觀不同就躲得過那道守門**——
⇒ 這一族真正抓得到的只有**一件事**：把它收成一份（`adjust.last_data_day()`），
⛔ 而且在**兩支的自測裡各釘一條「它是共用的那一份」**。

#### ⛔ 而修這一條時，我第一版的斷言是**恆真的**

我在兩支下游自測裡寫的是「不傳 `upper` 的結果 == 傳 `last_data_day()` 的結果」
⇒ 把 `last_data_day()` 改成「回今天」的突變 **兩支都全綠**——因為兩邊一起變。
⇒ ⭐ **判準要釘在那一份實作自己身上**：拿一個沙箱日檔目錄，
答案必須是**檔名**（`2019-01-03`），⛔ 不是今天。突變 T4／T6／T7 才開始會紅。

### ⛔⛔ 而 2026-09-14 傍晚掃到**第九次**，這次是「已經有八份」——⚠ 還沒走岔，但語意已經分兩派

要替 `tdcc.py`（集保週檔）加一條「累積週數只能往上」的閘門之前，先 grep 一次
——⛔ **低水位檔已經有八個，而讀寫實作至少七份**：

```
_adj_gap_low        adj_gap.py              _delisted_low       delisted.py
_err_cut_low        （selftest_feed_days）   _factor_limit_low   factor_limit_check.py
_holiday_years_low  holiday.py              _missing_rows_low   missing_rows.py
_otc_exright_adjgap_low  otc_exright_history.py
_otc_reduce_gap_low      otc_reduce_history.py
```

⚠⚠ 而 `holiday.py` 的檔頭**自己就寫著**這一族最危險的地方：

> 「⛔ 跟 `_factor_limit_low.txt` 存**最低值**方向相反——**別照抄語意**。」

⇒ ⭐ **八個檔、兩種相反的語意（有的只准往上、有的只准往下）、七份各自的實作。**
⛔ 而 `selftest_no_dup.py` **抓不到**：七份的函式本體長得都不一樣
（讀的檔名不同、回傳的形狀不同）——正是第八次那條講的
「同一個判準的兩份實作，只要外觀不同就躲得過那道守門」。

⇒ ⛔ **所以那一刻正確的動作不是「再寫第九份」**，是停下來記這一條。
⭐ 收法：一份 `lowwater.py`，`direction` 當**必填參數**（`"up"`／`"down"`），
⛔ 不可以有預設值——預設值就是「照抄語意」那個坑的自動化版本。

⭐ 這一族的傷害方向已知——**低水位檔被寫小一次，那道閘門從此永遠綠**，
而畫面上是 ✓（2026-09-14 早上 `_holiday_years_low` 被自測寫成 2 就是實例）。

#### ✅ 已收（2026-09-14 傍晚）：`lowwater.py`，`direction` 必填且**沒有預設值**

八個呼叫點全部改走它；`read_low()`／`read_sched_low()` 留成那個方向的別名。
⭐ 而守門**不是**「記得寫 direction」，是 `selftest_lowwater.py` ⑨
**掃全 repo**：每個宣告低水位檔的程式都走這一份、⛔ 沒有人自己開檔讀寫、
⚠ 而且「**兩種方向都還在**」——⛔ 只剩一種就代表有人把語意抄平了。
（反向驗：塞一份自己讀寫回 `missing_rows.py`，那一節當場紅。）

⛔ 而 ⑨ 存在的理由就是本節開頭那句：`selftest_no_dup.py` 比的是**函式本體**
⇒ 這一族**躲得過**。⇒ ⭐ **躲得過那道守門的族，要自己帶一道。**

#### ⭐⭐⭐ 而收成一份的**同一天**，同一個形狀又出現一次 ⇒ 這是一條通則

`db_status.py` 的 B2 要量「這一欄補完了沒」。⇒ 而衍生層有**兩種**欄：

```
DENSE   每一列都該有值（`dealer_self`）        ⇒ 判準是 **100%**
SPARSE  本來就多半是空的（`note`：只有處置股）  ⇒ ⛔ 判準**不可以**是 100%
        實測 2330：183/2,850＝**6.4%**，而那是**對的**，不是缺口
```

⚠ 兩種欄在表上、在程式裡、在名字上**長得一模一樣**
⇒ ⛔ 把 B2 那把尺照抄給 B3，那一格**永遠到不了 ✅**。

⇒ ⭐⭐ **通則（一天之內第二次）**：

> **當一族東西有兩種相反的語意，那個「選哪一種」的參數必須是
> 【必填】，而且【不可以有預設值】。**
> ⛔ 預設值就是「照抄語意」那個坑的**自動化版本**：
> 不寫也會跑，而它會默默套上**多數派**那一種
> ⇒ 那一格從此用**錯的尺**在量，⚠ 而畫面上看不出來。

已經有兩個實例：`lowwater.direction`（`up`／`down`）、
`db_status._p_col_two(derived=)`（`DENSE`／`SPARSE`）。
⭐ 而兩條的自測都用 `inspect.signature` 直接釘「**沒有 default**」，
⛔ 不是釘「忘了傳會 TypeError」——後者在有人加上預設值之後照樣全綠。

### ⭐⭐ 而 2026-09-14 晚上是這一族的**變形**：不是兩份程式，是**兩道閘門**

```
transpose 那道  「各層的來源日檔都跟上 price（**容忍 1 個交易日**）」 ⇒ ✓ 正常
feeds 那道      「沒有『連問都問不到』的日子」                       ⇒ ✗ 有問題
```

⇒ 兩句在講**同一個事實**：TWSE 的融資融券（MI_MARGN）在**收盤後好幾個小時**才發，
而 daily.yml 排在 19:07 台北 ⇒ 當天那一發固定回「沒有符合條件的資料」。
⭐ 一道**早就知道**這件事並寫了容忍度，⛔ 另一道每個交易日判它失敗。

⇒ ⛔ **後果不是「有一道錯了」，是那一道【天天紅】** ⇒ 被學會忽略
⇒ ⚠ 而它一旦被忽略，**真的壞掉那天也沒有人會看**。

⇒ ⭐ 判準：**看到一個區塊是 ✗，先問「它上一次是 ✓ 是什麼時候」。**
⛔ 「今天紅了」與「它一直都紅」要分開——⚠ 而兩者在當下那一份報表上**一模一樣**。
（實測方式：翻 `_last_run.md` 在 main 上的歷史版本，看那一塊的狀態與時戳。）

#### ⇒ 而放寬一道閘門時，判準有三條，⛔ 缺一條都不可以放

```
① 放寬的**依據**要是資料，⛔ 不是推測
   （這次：`data/universe/margin/` 2,850 天**一個洞都沒有**
     ⇒ 「隔天一定補得回來」是量到的）
② ⛔ 不可以把那個**訊息**重新分類
   （`很抱歉，沒有符合條件的資料` 講不出它是哪一天：休市／越界／還沒發
     三種長得一模一樣 ⇒ 第二點。⇒ 它仍然算失敗，只有**判準**放行）
⭐ ③ 要回答「放行之後還有沒有人在守」，而且**把答案寫進原始碼**
   （這次有兩道：隔天 `day_is_open` 變 False ⇒ 立刻 ✗；
     transpose 落後 2 天就紅）
```

⚠ ③ 寫進原始碼的理由：寫在 commit 訊息裡，下一個人讀那段程式時看到的是
「這裡放行了一種失敗」——⛔ 而他沒有辦法知道那是**有守的**還是**被放掉的**。

#### ⭐⭐ 而選方向的判準是：**問那個量會不會回到 0**（2026-09-14 晚上）

```
DOWN 只能用在「**修得完**」的量 —— 它應該收斂到 0
⛔ **只會隨時間長大的量，一律不可以用 DOWN**
```

⚠ 實際踩到：`_missing_rows_low` 守的是漏列**總筆數**，
而每個交易日固定多 2~9 筆（`parse_twse()` 跳過 `--` 那個已知結構問題）：

```
11:07Z 那趟   6,799 筆 ／ 可比 2,850 天   ✓ 綠
16:22Z 那趟   6,807 筆 ／ 可比 2,851 天   ✗ 紅   ← 只差**一個新的可比日**
```

⇒ ⭐ 那不是「門檻訂太嚴」，是**量錯了東西**。
⛔ 而處置也不是換成 UP（那會讓它永遠綠），是**換一個會回到 0 的量**：
改成數「**既有的日子變差了幾天**」——新的日子多幾筆是新資訊，⛔ 不是退步。

⭐ 全庫九個照這條掃過一次：DOWN 六個都是修得完的量、UP 三個都是累積量，
⛔ 只有 `_missing_rows_low` 是「**累積量 × DOWN**」這個錯配。

⚠ 而它跟上面 `feeds:margin` 那條是同一族、**病根不同**：
那條是**容忍度**寫錯，這條是**量錯了東西**——⛔ 兩種的畫面都是「一道天天紅的閘門」。

#### ⇒ 順帶一條：**已知會產生錯誤結論的工具，要移走，⛔ 不是標註「別用」**

`_p_col`（只量回補那一層）改完之後沒有人用了。⛔ 而**留著**的話，
下一個人加新列時會挑到它——它比較短、比較好用
⇒ 那一列就回到 2026-09-14 那個 bug（回補跑完就翻 ✅，而下游一列都讀不到）。
⇒ ⭐ 刪掉，並在原地留一行字說**為什麼**它不在了。

### ⇒ 做成常駐守門（跟第六點五那條同一個道理）

    .githooks/pre-commit    動到任何 .py 就掃 → 有第二份就**不讓 commit**
    八支 workflow 的第二步   `python selftest_no_dup.py`

⛔ **兩個地方都要有**：hook 可以被 `--no-verify` 跳過，workflow 裡那道跳不掉；
而 workflow 裡那道要**推出去、按下去、跑起來**才叫，hook 在壞掉的那一刻就叫。

`selftest_no_dup.py` 比的是**函式本體的 AST**（⛔ 不是字面 ⇒ 改個變數名躲不掉），
先拿掉 docstring（各寫各的說明不算差異），⚠ 少於 3 個 statement 的不算
（⛔ 門檻太低會每天紅，然後被學會忽略）。
⚠ 白名單目前是空的，⛔ 要加之前先回答「為什麼這兩份不能收成一份」。

⚠ 而別名**可以被下一個人拆掉**（拆掉的當下一切正常）
⇒ 一定要有一節**逐格比對**的自測餵一整批不同形狀的回應
（`selftest_parse_daily.py` ⑨），⛔ 不是比「是不是同一個函式物件」。

---

## ⭐ 四點六、⛔ **累積型的檔，不可以整份覆蓋**（分支上的 `data/` 比 main 舊）

2026-09-10 實際發生的遺失：

```
07:15  一天實測（2026-09-09）推上 main ⇒ `_coverage_backfill.csv` 多一列
07:20  2015 那批開跑。它 checkout 的是**分支**，
       而分支上的 `data/` **沒有**那一列（那一列是推到 main 的，不是推到分支）
07:22  它把整個檔搬到 main ⇒ ⛔ **五分鐘前那一列被刪掉**
```

⚠ 而 `git diff` 看起來完全正常：**一加一減，像是「這一趟重算過」**。

⭐ `push_data.sh` 早就替 `_last_run.md` 做了逐區塊合併，理由一字不差
（「跨 workflow 累積 ⇒ ⛔ 不可以整份取本趟的」）——
⛔ **而累積型 CSV 一直是整檔覆蓋，同一個道理只做了一半。**

⇒ `merge_ledger.py`：具名清單 ＋ 主鍵，逐鍵合併（本趟的鍵覆蓋、其餘原封不動）。
⚠ **是具名清單，⛔ 不是「所有 CSV 都合併」**：`_missing_rows.csv` 那些是
每趟全量重算的，合併它們會把**已經修好的舊列復活**。

⚠ 判準裡最重要的一條：**合併後的列數不可以少於 main 那一份**
——⛔ 這支自己絕不可以變成刪東西的那個人。

### ⛔ 而這一族**不只 CSV**——2026-09-10 找到第三個位置

```
① `_coverage_backfill.csv` 等累積型 CSV   ⇒ `merge_ledger.py`（本節）
② `otc_adj.write_days`（一天一檔的事件）  ⇒ 檔頭自己記著「第一版是整檔覆寫」
⭐ ③ `otc_adj.save_done`（FinMind 續跑台帳）
   不帶 `--resume` 跑一趟 ⇒ `done` 是空集合 ⇒ **1,996 列整份洗成一列表頭**
   ⚠ 而檔案在、格式對、程式回 0；下一趟「已完成 0 個」⇒ 從頭重跑 1,942 發
   ⛔ 而那正是免費額度撐不過的跑法 ⇒ **永遠跑不完，每趟都像有在跑**
```

⇒ 判準：**任何「這一趟只知道自己那一部分」的寫入，一律是合併，不是取代。**
⚠ ②的檔頭早就寫著這個道理——⛔ 而③就在同一個檔案裡，**只做了一半**。

### ⭐ 而它有一個**鏡像**，2026-09-10 連續踩到兩次

本節到這裡講的都是「分支上的 `data/` 比 main **舊**」。
⛔ 另一半是：**分支上根本沒有那個檔**。

```
① `esb-repair` 那趟：`push_data.sh` 問「本趟改了什麼」
   ⇒ 分支比 main **新**、而本趟不必改它 ⇒ 那個檔**從來沒被搬過去**
② `otc-adj-codes` 補 6461 那趟：`data/meta/otc_reduce_history.csv`
   **只在 main 上**（判準檔是別的 mode 抓的、推到 main）
   ⇒ workflow checkout 分支 ⇒ 讀到的是「檔不存在」
   ⇒ ⛔ `official_factor` **整欄空白**，而那一趟**成功**、`data/adj/` 有更新、
     數字全對——只有 K線線裁定要用的那一欄靜靜空著
```

⚠ 兩次的共同形狀：**「這個檔在哪個 ref 上」跟「程式讀不讀得到它」是兩件事**，
⛔ 而讀不到的表現是**空值**，不是錯誤。

⇒ 落地兩條：
- workflow 裡凡是**讀** main 才有的判準檔，先 `git checkout origin/main -- <path>`
  ⛔⛔ **而 `<path>` 要是程式真的讀的那個，不是名字看起來像的那個**
  （2026-09-11 付過代價：我取了 main 的 `calendar_twse.csv`，
  ⚠ 而 `adjust.trading_days()` 讀的是 **`data/universe/daily/` 的檔名集合**
  ⇒ 修完再跑一趟，runlog 照樣寫「交易日曆最後一天 2026-09-09」，
  三筆除權息照樣被當成未來事件擋下。⇒ ⭐ **先 grep 那支程式讀哪裡**，⛔ 不要照檔名猜。）
- ⭐ 程式端**自己**要擋：讀不到判準檔就**大聲失敗**，⛔ 不是寫出一整欄空白
  （`otc_adj.check_official_table()`）。⚠ 而判準是「**對照表有沒有內容**」，
  ⛔ 不是 `os.path.exists`——一個空殼檔會讓後者靜靜放行。

### ⭐⭐ 而第三次（2026-09-11）不是檔，是**程式**：排程跑的是 `main`

```
③ `probe.yml` 的「自癒」：檔頭寫著「探針程式比輸出新 ⇒ 下一趟排程會自己重跑」
   00:25  我在**分支**上改好 `delist_probe.py`（加了雙向比對）
   00:41  排程觸發 probe.yml ⇒ ⛔ `head_branch: main`、跑的是**舊的**那一份
   ⇒ 它照樣成功、照樣寫出 `_delist_probe.txt`、照樣推上 main
   ⇒ ⛔ 而那個檔裡**沒有**雙向比對那一節——⚠ 而「沒有那一節」看起來
     跟「那一節查無結果」**一模一樣**
```

⚠ 共同形狀還是那一句，只是主詞換了：
**「這個東西在哪個 ref 上」跟「它會不會跑」是兩件事。**

⇒ 落地：
- ⛔ **`schedule` 與 `push` 觸發的一律是 `main`**（分支上的新步驟、新程式，排程看不到）
- ⇒ 要驗分支上剛加的東西，一定要 **`workflow_dispatch` 並指定分支**，
  ⛔ 不可以「等排程自己跑到」——它跑的是另一份程式。
- ⚠ 而**判準是輸出裡有沒有那一節**（我方那一節的標題），
  ⛔ 不是「這一趟成功了沒」——舊程式一樣會成功。

#### ⛔⛔ 而 2026-09-14 同一天踩了**三次**的是它的**下半場**：input 也在分支上

上面那條講「要驗分支上的**程式**，就指定分支派工」。
⚠ 而當那一步**讀 `data/`**（下一格那個會刪資料的例外）⇒ **只能在 main 上派**
⇒ ⛔ 那麼「我剛加的那個 workflow input」**也必須先在 main 上**。

```
18:48  派 backfill mode=inst need_col=dealer_self
       ⇒ ⛔ Unexpected inputs provided: ["need_col"]（我只加在分支）
19:57  派 feeds mode=calendar-audit cal_start=1990-01
       ⇒ ⛔ Unexpected inputs provided: ["cal_start","cal_end","cal_index_only"]
```

⇒ ⭐ **順序是死的，⛔ 不可以跳**：

```
① 在分支上改 workflow（加 input）
② 讓它**同步到 main**（任何一支 workflow 的「把程式同步到 main」那一步）
③ 才在 main 上派工
```

⚠ 而②**沒有捷徑**：`schedule`／`push` 跑的是 main 上**現有**那份，
⛔ 而我不能直接推 main。⇒ 只能派一支**不讀 `data/`** 的（例如 probe）去搬。

⭐ 這次是**大聲**失敗（API 直接回 `Unexpected inputs`），算運氣好——
⛔ 而它的近親會靜悄悄：若我當時改用一個 main 上**已經存在**的參數代打
（例如拿 `force` 代替 `need_col`），那會變成 2,850 天全期重抓 7.9 小時，
⚠ 而且**看起來完全正常**。

#### ⛔⛔ 而上面那條有一個**會刪資料**的例外，2026-09-11 差點踩下去

指定分支跑，拿到的是**分支的程式**（要的）＋**分支的 `data/`**（⛔ 不要的）。
而分支的 `data/` 永遠比 main 舊——所有資料都是 workflow 推到 **main** 的。

```
⇒ 只讀程式、不讀 data/ 的步驟（探針）      ⇒ ✅ 指定分支，沒問題
⇒ **讀 data/** 的步驟（transpose／各種
   衍生檔重建／比對／合併）                ⇒ ⛔⛔ 指定分支 = 拿過期的輸入重算，
                                            而輸出會**整份覆蓋 main 上較新的那份**
```

⚠ 具體差點發生的：`transpose.yml`（全量重建 `data/stocks/`，0.55 GB）。
在分支上跑 ⇒ 讀分支那份過期日檔 ⇒ 產出缺列的個股庫 ⇒ `push_data.sh` 判定「有變動」
⇒ **把 main 上補好的無成交列整批刪掉**，而且 `git diff` 看起來像「這一趟重算過」。

⇒ 判準一句：**問「這一步讀不讀 `data/`」。**
- 讀 ⇒ 要嘛 `git checkout origin/main -- <那些路徑>` 之後再跑，要嘛**在 main 上跑**
- ⛔ 不讀才可以放心指定分支

---

### ⭐⭐ 而第四次（2026-09-13）主詞換成 `backtest/`：**同步程式的那一步會蓋掉累積檔**

```
5a89c6ee3 forward: 前瞻紀錄（2026-09-13）   ← workflow 在 **main** 上寫的 02:42 區塊
8b1704a7b sync: 把分支的程式同步到 main    ← ⛔ runlog.md **−4 行**
```

「把程式同步到 main」搬的是「`data/` 以外的**一切**」，
⚠ 而 `backtest/forward/runlog.md` 是**累積型**、由 main 上的 `forward.yml` 寫。
⇒ 分支上那份永遠是舊的 ⇒ 搬過去就是把新的刪掉。
⛔ 而 `git diff` 看起來完全正常，⚠ 而那一趟是綠的。⭐ 是**別條線**發現的，不是我。

⚠ 而同一目錄的 `_runs.jsonl` 沒被蓋掉是**運氣**——只因為分支上根本沒有那個檔。

#### ⇒ 排除清單的判準：**不是「這是不是資料」，是「誰寫它」**

```
凡是由 workflow 在 **main** 上產生、而且**累積**的東西
⇒ 分支那份永遠是舊的 ⇒ 一律進 `sync_code.sh` 的 EXCLUDE_TREES
⚠ 而同一個目錄裡**別人寫的判準檔**（`forward/RULE.md`）要例外放行
  ⛔ 因為「同一個目錄」不是判準——判準是誰寫它
```

⇒ 兩層，⛔ 缺一層都會再犯：
① **分支上不留累積檔**（沒有那個檔，就沒有東西可以蓋過去）
② **排除清單**（有人日後把檔加回分支時擋住）

⚠ 而這一段本來在**八支 workflow 各抄一份、逐字相同** ⇒ 要加排除清單就得改八次
——⛔ 那是「改一邊、另一邊沒跟上」的完美條件。⇒ 已抽成 `sync_code.sh`（四點五）。

### ⛔⛔ 而 2026-09-16 我**自己動手**做出了同一個形狀：`git worktree` 共用 `.git/config`

probe run 129 的「同步到 main」紅了而我查不出原因 ⇒ 我想在本機重現，
於是開了一個 worktree、在裡面 `git remote set-url origin <本機 bare repo>`。

```
⛔⛔ 而 worktree 跟主 repo **共用同一份 `.git/config`**
⇒ 那一行改到的是 **/home/user/tw-stock-data 真的 origin**
⇒ 之後 `git push` 回 `fatal: Could not read from remote repository`
```

⭐ 這次是**大聲**失敗（那個 bare repo 已經被我 `rm -rf` 了）⇒ 算運氣好。
⛔ 而它的近親會安靜：**若那個 bare repo 還在**，`git push` 會**成功**、
`git ls-remote` 會回一個像樣的 sha、下一次 `git push` 會說 `Everything up-to-date`
——⚠ 而 GitHub 上一個 commit 都沒有。

⇒ ⭐ 兩條落地：
- **驗終點要用 `git ls-remote origin <ref>`**，⛔ 不是看本機的 remote-tracking
  （`origin/xxx`）——後者是**我方**的快取，被污染過就跟著錯。
  ⚠ 實際發生：`git push` 印 `Everything up-to-date`，而 `ls-remote` 顯示
  遠端停在三個 commit 以前。
- ⛔ **不要在 worktree 裡動 `remote`／`config`**。要隔離就用**另一個 clone**，
  ⚠ 而 clone 一份 0.55 GB 的 repo 很慢 ⇒ ⭐ 那個「慢」本身就是提示：
  **這個重現太貴 ⇒ 改用「再派一趟、當場抓 log」**（那才是這次真正有效的那一條）。

## 五、寫入資料前先問：**誰是這個目錄的唯一寫入者**

換供料是**換維護者**的決定，不是順手加一行。
兩支程式寫同一批檔＝後寫的贏、跟新舊無關，而且四個地方都顯示正常。

### ⭐⭐ 五點二、**同一種資料有兩條路 ⇒ 兩條路的涵蓋範圍不一樣**（2026-09-15 晚上）

策略線問：「`revenue_hist` 裡**一檔 KY 都沒有**，是甲／乙／丙？」

⇒ 拿**同一期、同樣兩個市場**把兩條路的代號集合做一次差集：

```
當期 feed   data/mops/revenue/2026-07.csv       （ajax_t21sc03）   1,976 檔
歷史面板    data/mops/revenue_hist/2026-07_*.csv（靜態 _0.html）   1,851 檔
⇒ 只有當期有、歷史沒有 **127 檔**，其中 **122 檔是 KY**
  ⚠ 另 5 檔裡有兩檔是 **-DR** ⇒ ⭐ 少的是「**外國發行**」這一整類
```

⛔ 而這件事**在任何一份報表上都看不出來**：
兩邊欄名一樣、格式一樣、列數都是幾千、**兩邊都不會報錯**。

⇒ ⭐ 判準：**一種資料只要有兩條路，就要定期拿同一期做一次代號差集。**
⛔ 「兩邊都抓得到」不是證據——⭐ 要問的是「**兩邊抓到的是不是同一批**」。
⚠ 而這一格已經這樣一年多了，⛔ 是**別條線問到**我才去量的。

#### ⇒ 而排除「是不是我方濾掉的」要用**列數**，⛔ 不是讀程式

```
探針解析 t21sc03_114_1_0.html                ⇒ **987 列**
我方 data/mops/revenue_hist/2025-01_twse.csv ⇒ **987 列**
⇒ ⭐ 逐列都留下來了 ⇒ 「我方解析濾掉」當場被排除
```

⚠ 而那張頁的註記寫著「跳過 33 列」——⛔ 那 33 列不是資料，是 68 張分業表的表頭。
⭐ 判準是**留下來的列數跟我方檔案逐位相同**，⛔ 不是「跳過幾列」。

### ⭐⭐ 五點三、⛔ **一筆不在清單上 ≠ 那一筆是 0**（2026-09-15 晚上）

回測線報「放棄組裡 83% 是**法人 feed 缺日**」。⇒ 量了兩件事：

```
① 「缺日」那個類別**不存在**：六條清單各 2,850 天，對日檔**一天都沒少**
② 而那份清單本來就只涵蓋當天的 77~85%（抽 10 天實測）
   ⇒ 他們看到的是「**那一檔那幾天不在清單上**」
```

⇒ ⚠ 而最順口的解釋是「那天三大法人沒買賣它」。⛔ **它沒通過驗證**：

```
otcinst 2022 上半年 85,518 列
  ⭐ foreign／trust／dealer **三欄全 0** 的有 5,557 列（**6.50%**）
⇒ ⛔ 如果 absent 真的等於 zero，那 6.5% 的全 0 列**沒有理由存在**
```

⇒ ⭐ **absent 與 zero 是兩件事，而我答不出 absent 代表什麼 ⇒ 那就寫不知道。**
⛔ 而把 absent 補成 0 會把兩者壓平——⚠ 那正是 K線分析線裁 (c) 時講的
「補的那個值落在數值域的正中央、而且是一個**有意義的值**
⇒『沒有資料』與『剛好是 0』**事後不可分辨**」。
⭐ 那 6.5% 就是那句話的實證。

---

## 六、⛔ 這個開發容器**對交易所一律 403**——而那是**我方閘道**擋的

```
kind: connect_rejected｜gateway answered 403 to CONNECT
```

⇒ 意思跟「交易所擋人」**完全相反**：

```
對方擋     ⇒ Actions 上也會擋
我方閘道擋  ⇒ Actions 上是通的
```

⛔ 所以在這裡跑正式抓取程式，得到的失敗**不是事實**。
`append_coverage()` 已經會擋（不寫假紀錄），
`runlog` 會在區塊上標「⚠ 這一塊不是 Actions 跑的」。

---

## ⭐⭐ 六點六、**對方的 TLS 設定壞掉，而瀏覽器會替它遮掩**（2026-09-13）

`otcsbl` 在 Actions 上第一發就掛，而錯誤訊息只留下前 50 個字元：

```
失敗(URLError: <urlopen error [SSL: CERTIFICATE_VERIFY_)
```

### ⇒ 第一件事：**錯誤訊息不可以砍尾巴**

SSL／憑證／逾時這一族，**可行動的部分永遠在後面**：

```
unable to get local issuer certificate  ⇒ 鏈不完整（對方漏送，或我方缺 CA）
certificate has expired                 ⇒ 對方憑證過期
hostname mismatch                       ⇒ 打錯站
```

⛔ 而前 50 個字元每一次都長得一樣。⇒ `feeds._why()`：太長就**中間**省略。
⚠ 補這條斷言時用 AST 掃「還有沒有 `err[...]` 的切片」，**當場多找出兩處**我不知道存在的。

### ⇒ 第二件：`unable to get local issuer` 有兩種成因，**處置相反**

```
① 我方 CA 太舊     ⇒ 換一份新的（certifi）
② 對方漏送中間憑證 ⇒ 我方要補鏈
```

⇒ 判準：**同一個網址用兩份 CA 各打一次，再拿一個「現在是通的」站當對照組。**

```
【實測】TPEx  系統 CA ✗　certifi ✗　對方送 **1 張**　Verify return code: 21
       TWSE  系統 CA ✅　certifi ✅　對方送 **3 張**　Verify return code: 0
       ⭐ 而兩者的葉憑證發行者**完全相同** ⇒ 只可能是②
```

⚠ 而「對方送了幾張」在 Python 3.12 問不到（`get_verified_chain` 是 3.13+）
⇒ 用 `openssl s_client -showcerts` 數。⛔ 第一版卡在這裡，而報告照樣產出、
只在那一格寫「無法判斷」——⚠ 那跟「判斷過、沒問題」在紙上很像。

### ⭐ 而瀏覽器為什麼看不出來

瀏覽器會照憑證裡的 **AIA** 自己去把中間憑證抓回來補上（AIA chasing），
⛔ 而 `urllib`／`curl` 不會。⇒ **「瀏覽器打得開」不是「程式抓得到」的證據。**

### ⛔⛔ 而補鏈**不是**把驗證關掉——這一條要寫死

補進來的憑證**自己不會產生信任**，它們只是「路」；鏈仍然要走到系統本來就信任的根。
⇒ 假的葉憑證仍然要有中間 CA 的**簽章**才驗得過。

```
✅ create_default_context() 之後 load_verify_locations()  ← **追加**，系統的根一張沒少
⛔ CERT_NONE／check_hostname = False                      ← **永遠不是修法**
```

⚠ 而**只補一張是不夠的**：那張中間憑證的發行者可能也不在信任庫裡
（這次就是——`TWCA CYBER Root CA` 不在，還要那張交叉憑證）。
⭐ ⇒ **照抄那個「現在是通的」站送的整組**，⛔ 不要憑 AIA 猜一張。
本機離線 `openssl verify -untrusted` 驗得過才算數。

### ⇒ 而這一族的自測，**反向那幾條才是主角**

補鏈最糟的壞法**不是「沒補到」**（那會抓不到資料，很吵），
⭐ 是**「順手把驗證關掉」**——⚠ 那會安靜地成功，成功得跟對的一模一樣。
⇒ `selftest_ca_chain.py` 盯 `verify_mode`／`check_hostname`／原始碼裡沒有 `CERT_NONE`
（⛔ 比 AST 不比字串——那四個字在說明文字裡也有一份），
並釘「最早到期那張少於 180 天就紅」。

---

## ⭐ 六點五、動到 workflow／`.sh` 之後，**commit 之前**一定要驗 shell 語法

    git config core.hooksPath .githooks     ← 新 session 進來第一件事

`.githooks/pre-commit` 會在動到 `.github/workflows/*.yml` 或 `.sh` 時
跑 `selftest_workflows.py`（8 支、117 個 `run` 區塊逐個 `bash -n`），不過就擋下 commit。

### ⛔ 這一條的價格是 1 小時 50 分鐘

run 34426373917：「回補」跑了 1h50m 是綠的，「Commit」1 秒失敗——
`syntax error: unexpected end of file`，我批次改 workflow 時把 closing `fi` 切掉了。

⚠ 而我當時「驗過」，驗的是 `yaml.safe_load()`。
⛔ **YAML 合法 ≠ 裡面的 shell 合法**——對 YAML 來說 `run:` 只是一個字串。

⚠ 而檢查排在 workflow 裡是**不夠的**：那是推出去、按下去、跑起來之後才叫，
壞掉的那一刻是在**本機 commit**。⇒ 兩個地方都要有。

### ⛔⛔ 而 2026-09-13 **反過來咬了一次**：YAML 那一半後來被拿掉了

上面那句「YAML 合法 ≠ shell 合法」讓我們做了 shell 那一半（`bash -n` 逐區塊）。
⚠ 而 **YAML 那一半因為 runner 上沒有 `yaml` 模組被拿掉** ⇒ **從此沒有任何地方驗它**。

```
run: bash push_data.sh "forward: 前瞻紀錄（…）"
                                ^^ 冒號＋空白
```

單行 `run:` 的值裡有 `: ` ⇒ YAML 把它讀成一個**對映** ⇒ **整份檔案解析失敗**。
⚠ 引號在 YAML 的**純量**裡沒有保護作用——它不是 shell。

#### ⛔⛔ 而 GitHub **照樣把它註冊成一支 workflow**

```
state: active、出現在 workflow 清單裡
⛔ 只是**名字顯示成檔案路徑**（`.github/workflows/forward.yml`）
⛔ 而 `workflow_dispatch` **靜靜不存在** ⇒ 派工才回
   「Workflow does not have 'workflow_dispatch' trigger」
⚠ 而排程那一半會不會跑？**沒有任何地方會說。**
```

⭐ 「名字變成檔案路徑」就是**解析壞掉的徵兆**，⛔ 而它長得只像「我忘了取名字」。

#### ⇒ 判準兩層，⛔ 缺第二層等於沒有

```
① 有 `yaml` 就真的 parse（本機 pre-commit 一定有）
   ＋ 斷言有 `name:`　＋ ⭐ 斷言有 `workflow_dispatch`
② ⭐ 零相依那一道（runner 上**跳不掉**）：
   單行 `run:`（⛔ 不是 `run: |`）的值含 `: ` 就一定要整串加引號
```

⚠ ②存在的理由就是這次的病根：**①在 runner 上會靜靜跳過**。
⛔ 一道「環境不對就跳過」的檢查，跟沒有那道檢查在紙上一模一樣。

#### ⛔⛔ 而我修它的時候，**同一個小時內**又付了第二次代價

我把「這台有沒有 `yaml`」寫成了一條**斷言**（`ck`）。
⇒ runner 上沒有 `yaml` ⇒ 那一步 `exit 1` ⇒ ⛔ **九支 workflow 的第二步全部當場紅**，
而「把程式同步到 main」排在它後面 ⇒ **那一趟什麼都沒搬**（probe run 52 實測）。

⇒ ⭐ 判準：**第二步的自測是「零相依」的，⛔ 這是硬規定不是習慣。**

```
選用套件在 ⇒ 多驗一層
選用套件不在 ⇒ ⛔ **大聲印出「這一層沒跑」**，但**不算失敗**
             ⚠ 環境缺套件不是**這個 repo** 壞掉
⛔ 不可以寫成斷言——它會把所有 workflow 的第二步變成紅的
```

⚠ 而「大聲印」那一行要寫成**不會被讀成『驗過了』**的樣子
——⛔ 一行 `skipped` 跟一行 `ok` 在捲動的 log 裡看起來一樣。

#### ⛔⛔ 而 2026-09-13 同一天犯了**第二次**，這次的變數不是套件，是 **ref**

我寫了一條「分支上**不可以**留 `backtest/forward/` 那四個累積檔」（四點六④的第①層）。
⚠ 而它斷言的是**工作區**，主詞卻是**分支**——⛔ 那四個檔在 **main 上本來就該在**
（forward.yml 就是在 main 上寫它們的）。

```
feeds run 135（在 main 上派工，要補三期殘缺的月營收）
  step 4「驗 workflow 自己」  ⛔ failure（通過 387｜失敗 4）
  step 8「把程式同步到 main」 skipped
  step 17「全市場月營收與財報」skipped
⇒ ⛔ 那三期一列都沒補到，⚠ 而其中一期是隔天前瞻要用的
```

⇒ ⭐ **通則（把「套件」換成任何『環境會變的東西』都成立）**：

> **一條在某個環境下【必然】不成立的斷言，等於把那個環境的整條線關掉。**
> 已經踩過的**三個**變數：**選用套件在不在**、**這一趟在哪個 ref 上**、
> ⭐ **這一趟是不是在 Actions 上**（2026-09-15 新增）。

⇒ 第三個的實例：我在 `selftest_probes` 寫

```python
ok3 = "不是 Actions" in probe_stamp()      # ⛔ 在 Actions 上必然不成立
```

⇒ probe run 101 的 step 12 當場 failure，⚠ 而它後面兩步是 `if: always()`
⇒ 整趟照樣跑完、照樣 commit，⛔ 而**沒有任何地方會說**。
⇒ ⭐ 處置不是「跳過」，是**兩種環境都自己造出來各驗一次**
（設 `GITHUB_ACTIONS` 為 `true`／`false`／不設，**三種都要綠**）。

⇒ ⛔⛔ 而查它的時候挖出第二個：「在不在 Actions 上」有**兩份判準**
（`runlog._block` 比 `== "true"`、`backfill.probe_stamp` 只看真假值）
⇒ `GITHUB_ACTIONS="false"` 在後者是**真** ⇒ 它會**拿掉那句「不可信」的警語**。
⇒ 收成 `runlog.on_actions()`，並加一道**掃 AST** 的守門：誰都不准自己讀那個環境變數。

⇒ 處置跟上面那格**一模一樣**：條件不成立就**大聲印「這一層沒跑」**，⛔ 不算失敗。

⛔⛔ **而跳過的同時一定要問：這一層跳掉之後，還有沒有人在守？**
本例的①在 main 上跳過 ⇒ ②「排除清單真的擋得住那四個檔」就成了**唯一**的守衛
⇒ ⭐ **②必須在每一個 ref 上都跑**。⚠ 反向驗要**兩個 ref 各跑一次**：
把排除清單拿掉，分支紅 5 條、main 也要紅 5 條——⛔ 修之前 main 是 **0**。

⭐ 而 `workflow_dispatch` 是**每一支**都要有，理由是四點六③：
**排程與 push 觸發的一律是 `main`** ⇒ 要驗分支上剛加的東西**只能**指定分支派工。
⚠ 沒有它，分支上的新步驟永遠只能「等排程跑到」——而那跑的是另一份程式。

---

## 七、測試：**沒證明過會失敗的測試，不算測試**

- 每一條新斷言都要證明它**會紅也會綠**。
- 假回應要**照真回應的形狀**做——假的比真的簡單，等於那段沒測。
- 回報「某群 0 筆」時，必須附上「該判準在該群抓到的正例數」。
  （曾經有一條偵測器在上市的正例數是 0／9,597，而那個 0 被讀成「上市乾淨」。）

### ⛔⛔ 而「突變驗」本身有一個會讓它整個失效的陷阱：`__pycache__`

2026-09-11 實際發生：

```
突變 M1（交換兩個 if 的順序）  ⇒ 測試**還是綠**
突變 M5（`s <= b and e >= a` 改成 `a <= s and e <= b`）⇒ 測試**還是綠**
⇒ 我據此認定「這兩條斷言是假的」，⭐ 而且動手去改掉其中一條**其實是對的**斷言
```

成因：Python 的 `.pyc` 只用 **mtime ＋ 檔案大小**判斷要不要重編。
⚠ 而上面兩種突變都是**同尺寸**的（交換順序、換比較方向），
⛔ 同一秒內改完又立刻跑 ⇒ `.pyc` 被判定still fresh ⇒ **跑的是突變前的程式**。

⚠ 這個失效的方向是**最糟的那一種**：
突變後還是綠 ⇒ 結論是「這條斷言沒用」⇒ **有人會去刪掉一條其實有用的斷言**。

### ⛔⛔ 第二個陷阱：**突變的「紅」不可以只數 `✗`**

2026-09-11：拿掉 `runlog.now_tpe` 之後 `grep -c "^  ✗"` 回 **0**，
⚠ 我差一點讀成「這條斷言沒用」——⛔ 而它其實**炸掉了**：
崩潰吐的是 traceback，**不會印 `✗`**。

⇒ ⭐ 判準是 **`rc != 0` 或輸出裡有 `✗`**，⛔ 不是只數 `✗`。
⚠ 而更好的做法是**讓它印 `✗`**：斷言那條路若會丟例外，就 `try/except` 接住再判——
⛔ 不接住的話整支測試當場中斷，**後面一條都不會跑**
（接住之後同一個突變從「紅 0」變成「紅 5」）。

### ⛔⛔ 第三個：**測了判準、沒測「呼叫點」與「預設值」**——這一族已經兩次

```
① otc_exright_check   測了 `classify()`，⛔ 沒測 `main()` 傳什麼基準進去
                      ⇒ 把基準改回 `today` 的突變**沒抓到**
⭐ ② feeds.month_is_open(ym, today=None)
      18 條斷言**每一條都傳了 `today=`**
      ⇒ ⛔ 預設值那條路**一次都沒被走過**，而那是 Actions 上唯一會走的那條
      ⇒ `runlog.now_tpe` 根本不存在 ⇒ **AttributeError 掛在 Actions 上**，
        ⚠ 而自測 18 條全綠
```

⇒ 判準兩條：
- **有預設值的參數，一定要有一條不傳它的斷言。**
- **測完純函式，再掃一次原始碼**確認呼叫點真的那樣叫（`otc_exright_check` 那支就是這樣補的）。

### ⛔⛔ 第四個：**突變根本沒套上去**，而它看起來跟「沒抓到」一模一樣

2026-09-11：我把錨點字串抄錯一個字（少了「的」）⇒ 取代了 **0 次**
⇒ 跑的是原版 ⇒ 當然全綠 ⇒ ⛔ 又看起來像「這條斷言沒用」。

⇒ ⭐ **三種結果一定要分開**，⛔ 混成「綠/紅」兩種就是上面那個誤判：

```
✅ 抓到          斷言有用
⛔ 沒抓到        ⇒ 這條斷言可能是假的（⚠ 先確認不是下面那一種）
⛔⛔ 沒套上去    ⇒ **我的錨點錯了**，跟斷言好不好無關
```

### ⛔⛔ 第五個：**baseline 自己是紅的** ⇒ 每一個突變都會回「✅ 抓到」（2026-09-14）

`mutate.py` 判「紅」＝ `rc != 0` 或有 `✗`。⛔ 而它**不驗未突變的那一版是不是綠的**。

```
自測有 1 條沒過（低水位檔被自己寫壞成 2，斷言要 6）
⇒ AB1~AB4 四個突變**全部**回「✅ 抓到」
⇒ ⛔ 我據此宣告「四條斷言都有效」，⚠ 而其中兩條（AB2／AB4）**根本不存在**
   ——修好 baseline 之後重跑，那兩個當場變成「⛔ 全綠」
```

⇒ ⭐ 判準：**跑突變之前，先讓 baseline 全綠**，⛔ 「只有一條沒過、不影響」是假的
——那一條會把**每一個**突變染紅。
⚠ 它跟第四個（沒套上去）是鏡像：那個是假的紅**不夠**，這個是假的紅**太多**，
⛔ 而兩個的畫面都寫著「✅ 抓到」。

#### ⇒ 而那條 baseline 是怎麼紅的：**沙箱導走漏了一個**

`selftest_holiday.py` 把 `H.OUT`／`H.SCHED_CSV`／`runlog.PATH` 都導進 tempdir，
⛔ 而當天新加的 `H.SCHED_LOW` 沒導 ⇒ 拿 3 列假資料跑一趟，
就把 repo 真的 `_holiday_years_low.txt` 從 `6` **寫成 `2`**。
⚠ 而它是「只往上寫」的檔 ⇒ 從此那道閘門永遠綠，而畫面上是 ✓。

⇒ ⭐ 判準不是「記得導走」（漏一個是常態），是
**每一個被導走的路徑都要有一條「★ 沒有動到 repo 真的 ___」的斷言**
——前兩個坑就是它抓到的，⛔ 而第三個檔加進來時沒有跟著加那一條。

##### ⛔⛔ 而當天下午又漏了一個，成因不是忘記，是**那個路徑根本不是旋鈕**

我把 `mops.OUT_DIR` 導到沙箱、試跑一次期別自癒 ⇒ 檔寫在沙箱（對的），
⛔ 而 log **1,782 行寫進 repo 真的 `data/mops/_changes.log`**。

```
CHANGES = os.path.join(OUT_DIR, "_changes.log")   ← **import 當下**就算好了
⇒ 之後再改 OUT_DIR，CHANGES 不會跟著動
⚠ 而它是 append ⇒ 不會報錯、不會覆蓋，只是靜靜長出 1,782 行假「財報更正」
```

⇒ ⭐ **一件事要導走卻需要動兩個旋鈕，那就是遲早會漏掉一個的旋鈕。**
⛔ 處置不是「記得兩個都導」（那正是上一格已經否決的說法），是
**收成一個**：路徑寫成**呼叫當下**才算的函式（`mops.changes_path()`），
⚠ 而自測裡那兩行 `M.CHANGES = ...` 要一起拿掉——⛔ 留著的話，
「只導一個也會對」這件事**一次都沒有被走過**（第三個陷阱：沒測預設那條路）。

⚠ 而 `selftest_mops` 的「★ repo 的 `_changes.log` 逐位元沒變」**本來就在**，
⛔ 它沒抓到我，只因為**我不是在跑自測**——我是在 shell 裡手動導一次來試跑。
⇒ ⭐ 那條斷言擋得住「自測污染 repo」，⛔ 擋不住「人手動污染 repo」；
**真正把它變成擋得住的，是把兩個旋鈕收成一個。**

### ⛔⛔ 第六個：**假回應少了一種形狀** ⇒ 那個「錯誤處理」在真回應上自己炸掉（2026-09-14）

我早上替 `IncompleteRead` 寫了一段「訊息要自己講出它是傳輸被切斷」的處理，
自測三條、突變三個全中。⚠ 而那三條的假回應**每一個都帶了 `expected`**。

```
真回應少了一種：chunked 斷在「下一塊的長度」那一行
　⇒ Python 丟 `IncompleteRead(b'')`，**expected 是 None**
　⇒ f"{None:,}" ⇒ TypeError ⇒ ⛔ **那段錯誤處理自己炸掉**
```

#### ⛔⛔ 而它比「沒有那段處理」更糟

原本那個例外會被最後的 `except Exception` 接住 ⇒ 轉成錯誤字串 ⇒ **重試**。
⚠ 我加的 `except http.client.IncompleteRead` **把它攔下來**，然後在裡面炸掉
⇒ 例外從函式裡逃出去 ⇒ ⛔ 不重試、`feeds.py` 當場結束。

```
【實測代價】feeds run 143（otcmargin 回補 --need-col note）
  十二年**每一年**都只跑了 8~9 天就掛掉 ⇒ 2,850 天只補到 86 天
  ⛔ 而每一年都照樣 `push` 了、每一批都寫了 runlog ⇒ **看起來一直有在跑**
  ⚠ 我是去查「為什麼 30 分鐘才 3%」才發現整趟早就紅了
```

⇒ ⭐ 兩條判準：

```
① 一個 `except` 分支**本身**也是程式 ⇒ 它也要有「它自己會不會炸」的斷言
　 ⛔ 尤其是把原本會被接住的例外**攔下來**的那種：它把安全網換掉了
② 假回應要照真回應的形狀做**每一種**——⚠ 而「有沒有某個屬性」也是形狀的一部分
　 （`IncompleteRead` 有帶 expected 與不帶 expected 是**兩種**）
```

⚠ 而補這一條時又踩到一個小的：斷言寫 `"56,326" in err` **抓不到**
「已讀那一格被拿掉」的突變——因為同一個數字在後面「各次讀到」那一段還有一份。
⇒ ⭐ 斷言要比**帶標籤的整串**（`"已讀 56,326"`），⛔ 不是比一個裸數字。

### ⛔⛔ 第七個：**一道閘門達成目標的那一天，它的自測就紅了**（2026-09-14）

`selftest_adj_gap.py` 的 [2] 要驗「未歸因**高於**歷史最低值 ⇒ 斷言變 ✗」。
⚠ 而它是拿**現場資料**造那個情境的：要造得出來，現場至少得有 2 筆未歸因。

```
⭐ 而 `_adj_gap_low.txt` 已經是 **0**——未歸因收斂到 0 正是這一族的**目標**
⇒ ⛔ **目標達成的那一天，這條斷言必然失敗。**
⚠ 實測同一天：main 上 3 筆 ⇒ 綠；分支上 0 筆 ⇒ 紅。同一支程式、同一天。
```

#### ⛔⛔ 而 `continue-on-error: true` 讓它**沒有任何地方會說**

daily.yml 裡那一步是 `continue-on-error: true`（理由正當：一支離線自測
不該賠掉整趟抓取）。⇒ 它紅著，而 run 的 conclusion 是 **success**。
⚠ 而「這支自測紅了」與「這支自測沒被寫進 workflow」在畫面上**一模一樣**：
兩種都是綠的一趟，兩種都沒有人被告知。

⇒ ⭐ **判準：一條「要有壞樣本才驗得到」的斷言，它的壽命等於那個壞樣本的壽命。**
⛔ 而我們每天在做的事，就是**把壞樣本清掉**。

⇒ 處置跟六點五那條一模一樣（條件不成立就大聲印「這一層沒跑」、不算失敗），
⛔ 而**跳掉的同時一定要問：這一層跳掉之後還有沒有人在守？**
本例有，而且比原本強：`selftest_lowwater.py` ②⑦ 拿**合成**數字驗同一條判準
（水位 10／本輪 12 ⇒ down 必紅）⇒ ⭐ **不依賴現場有幾筆**，每個 ref 都會跑。

⇒ ⭐⭐ 而這就是**寫這種斷言時該有的形狀**：
**判準本身拿合成數字驗（環境無關），現場資料只用來驗「呼叫點真的那樣叫」。**
⛔ 拿現場資料去驗判準，等於把那條斷言的壽命綁在「這個 bug 還沒修好」上。

---

### ⛔⛔ 第八個：**斷言比原始碼字串** ⇒ 那幾個字在**說明註解裡也有一份**（2026-09-15，一天三次）

```python
ck("三次都推不上去要 exit 4", "exit 4" in src)      # ⛔ 比原始碼
```
⇒ 把 `exit 4` 改成 `exit 0` 的突變**照樣綠**：
同一個檔的註解裡就寫著「⭐ 三次都不成才 exit 4」。

⚠ 同一天同一個形狀**三次**，三次的解法都不同：

```
① 掃 `GITHUB_ACTIONS`（說明文字裡有）          ⇒ 改比 **AST**
② 掃 `ci_report.py`（註解裡有）                ⇒ 改比「真的有一行 `run:` 在跑它」
⭐ ③ 掃 `exit 4`（註解裡有）                    ⇒ 改比**行為**（rc 與輸出）
```

⇒ ⭐ 判準就是四點二那一句，只是主詞換成斷言自己：
**斷言要驗終點。** ⛔ 而「原始碼長什麼樣」從來不是終點。
⚠ 而這一族在本 repo 特別容易中：**我們的註解本來就會引用那段程式碼**。

#### ⇒ 而要驗到那一格，**沙箱也得先做對**

⛔ 把 origin 指到不存在的路徑**驗不到** `sync_code.sh` 的 push 失敗：
那會走進「fetch 失敗 ⇒ 跳過同步」那條**正當**出口（回 0）。
⇒ ⭐ 要 **fetch 成功、push 失敗** ⇒ 在 bare repo 放一個一律拒絕的 `pre-receive` hook。
⚠ 「斷言沒抓到」與「我根本沒造出那個情境」長得一模一樣（第四個陷阱的變形）。

### ⛔⛔ 第十個：**兩邊的數字都對，而它們不是同一個量**（2026-09-15 晚上）

市場情報分析線給了 23 個對帳錨點（集保 2330，七個欄位）。⇒ 跑完：

```
總人數／總股數／股數_L12to15／股數_L13to15   ⭐ 四個整數欄 **23 週逐位相符**
⛔ 比例%_L12to15／比例%_L13to15               **0 ／ 23**
```

⇒ ⛔ 而那**不是誰算錯**：

```
他們 2019-06-28 寫 93.6874
24,293,492,882 ÷ 25,930,380,458 × 100 = 93.687375   ⇒ ⭐ 逐位吻合
⇒ 他們的比例是【從股數推導】的（四位小數）
⇒ 我方的是【官方 pct 欄逐級相加】，而官方只給**兩位小數**
   ⇒ 四個兩位小數加起來做不出四位小數 ⇒ 我方必定是 93.66，⭐ **而它也沒有錯**
```

⚠ 它比「計數沒寫定義」那一族更難看出來：
**兩邊都叫「比例%」、都在 0~100、都很接近** ⇒ ⛔ **差 0.02 讀起來就像四捨五入。**
⭐ 真正把它定住的是**算一次除法**——⛔ 不是再比一次、也不是加一位精度。

⇒ 判準：**跨線交換一個【推導欄】時，欄名要帶推導方式**
（`pct_derived` ＝ 分子÷分母　vs　`pct_official_sum` ＝ 官方欄逐級相加）。
⛔ 只叫「比例%」的欄，對不上時**沒有辦法判斷是誰錯**。

#### ⇒ 而同一次對帳裡，「少一週」那個差**也不是定義問題**

371 vs 370，差集恰好一個 `2023-10-20`，⭐ 而我方是**故意不收**的（那份來源被截斷）。
⚠ 而反過來的意思是：**他們那份的那一週少了一千多檔**，
⛔ 而 2330 在檔案前段 ⇒ **用 2330 那條對帳線看不出來**。

⇒ ⭐ 通則：**一個「抽樣檢查點」對得上，證明的是那幾個點，⛔ 不是那一批。**
⚠ 而他們自己在附件裡就寫了這句（「⛔ 抓不到：非檢查點那 348 週的數值單獨錯」）
——⭐ 而真的踩到的那一週就在那 348 週裡。

#### ⇒ 順帶一條：**「相鄰兩筆差 > N」不是缺漏偵測器**

K線建議用「相鄰週日期差 > 7 天」掃集保。⇒ 實測 **85 個**，
⛔ 而其中絕大多數是**同一件事的兩半**：

```
2019-08-02 → 2019-08-08　6 天
2019-08-08 → 2019-08-16　8 天     ← 6 ＋ 8 ＝ 14 ⇒ **一週都沒少**，只是那一週提前了
```

⇒ ⛔ 「> N 天」把**快照日位移**跟**真的少一筆**混成同一堆，
⚠ 而位移正是這份資料的常態（星期分佈：五 325、四 22、六 13、三 9、二 1）。
⇒ ⭐ 改成**折成週期單位再看連不連續**（ISO 週）：85 → **7**，而那 7 個每一個都真的缺。

⭐⭐ 而它是**獨立**驗過的：這個掃描自己冒出 `2023-10-16` 那一週，
⛔ 我是**先掃完才去讀對方附件**的，而他們多出來的那一天正是 `2023-10-20`
——⚠ 兩條互不相干的路指到同一週 ⇒ 那不是巧合，是證據。

### ⛔⛔ 第九個：**守門自己的「母體」被判準悄悄縮小**（2026-09-15）

我早上加了一道「每一支寫探針輸出的程式都要叫 `probe_stamp()`」，
報表寫著 **「17 支全部都叫 ✓」**。

⭐ 而母體其實是 **20 支**——判準是一條正規式：

```
OUT\s*=\s*os\.path\.join\([^)]*"meta"[^)]*"_[A-Za-z0-9_]+\.txt"
```

⚠ `[^)]*` **跨不過括號** ⇒ 寫成 `os.path.join(os.path.dirname(...), "data", "meta", …)`
的那三支掃不到 ⇒ ⛔ 而**那三支正好就是沒有叫的那三支**。

⇒ ⭐ 這是第七點那句套在**守門自己**身上：
「回報某群 0 筆時，必須附上該判準在該群抓到的正例數。」
⛔ 而這次比 0 筆更難看出來：**不是 0 筆，是母體被判準縮小了**
——「20 支裡有 3 支沒叫」與「17 支全部都叫」在紙上一模一樣。

⇒ 落地兩條：
- 母體的偵測改比 **AST**（⛔ 正規式跨不過巢狀結構）
- ⭐ **母體大小自己要是一道斷言**（`n >= 20`）
  ——⚠ 而它只在母體**縮小**時才會紅：把門檻改回 17 的突變**抓不到**，
  ⛔ 那不是「這條斷言沒用」，是那個突變什麼都沒改變（第四個陷阱）。

### ⛔⛔ 第十一個：**照說明拆，⛔ 不是照回來的東西拆**（2026-09-15 深夜）

```python
"""t163sb04／sb05 → ([(kind, caption, 表頭, 列)], 編碼)。"""   ← docstring 寫 **4** 個
got.append((kind, how, _clean(cap)[-40:], header, body))       ← 實際回 **5** 個
```

⇒ 我照 docstring 寫下游：`for _k, _cap, hdr, rows in tables:`
⇒ probe 112 `ValueError: too many values to unpack (expected 4)`。

⚠ 而代價不是「那一節沒印出來」：那支探針**整支 rc=1**
⇒ 守門在 `data/meta/_mops_probe.txt` 補一行 ✗「這一份是上一次成功的內容」
⇒ ⛔ 財報倖存者那一節**從頭到尾沒落地**，⚠ 而那個檔看起來完全正常。

⇒ ⭐ 判準：**一個回傳形狀，要從回來的東西量，⛔ 不是從說明讀。**
⚠ 而這個 repo 特別容易中：我們的 docstring 寫得長、活得久，
⛔ 而回傳形狀改過之後**沒有任何地方會要求那一行跟著改**。
⇒ 落地是一條**驗終點**的斷言（餵真形狀的假回應、比 `len(t) == 5`），
⛔ 不是「記得同步 docstring」。

### ⇒ 落地：**不要再手寫那個 shell 函式**，用 `mutate.py`

```
python3 mutate.py --file adjust.py --test selftest_adjust_official.py \
    --label "M1 拿掉停牌缺口那一半" --old '舊字串' --new '新字串'
```

它把上面四個陷阱都收進去（改檔前後與還原後各清一次 `__pycache__`、
「紅」＝ `rc != 0` **或**有 `✗`、錨點不是剛好 N 次就回 **2** 並明講
「這不是斷言沒用」、`finally` 保證還原）。
⚠ 它自己也有自測（`selftest_mutate.py`，17 條、突變 6 次全中），
⭐ 因為**它是判斷「斷言有沒有用」的那把尺**——
⛔ 尺壞掉的方向只有一種：它說「沒抓到」⇒ **有人會去刪掉一條其實有用的斷言**。

⚠ 而手寫 shell 版的四個陷阱**每一個我今天都踩過**，
⛔ 所以這一節的結論不是「記得清 cache」，是**同一件事只准有一份實作**（四點五）。
⚠ 而同一個道理也適用於**還原**：還原後不清 cache，下一支測試會讀到突變版。
⭐ 清掉之後上面那五次突變**全部命中**（M1 也證明了順序真的有意義）。

---

## ⭐⭐⭐ 七點五、**一個「跟鄰居比」的判準，對「所有鄰居一起缺同一段」完全免疫**

2026-09-15：KY 月營收要回補 ⇒ 每一期都得重抓一次。
⚠ 而 `feeds.yml` 那一步寫死 `--fill`，`--fill` 走 `revenue_complete()`，
它的判準是「這一期的列數 vs **鄰近期別**有沒有掉一半」。

```
⛔ 而 KY 是**每一期同時缺**的（`rev_url()` 本來只打 `_0`）
⇒ 鄰居也一樣少 ⇒ 280 期**全部**判成已完成
⇒ 派下去會印「0 期檔、0 個失敗」——⭐ 一趟全綠，而那一段從頭到尾沒被抓
```

⚠ 實測：2015-01／2020-06／2026-07 六格 `complete=True`，
⛔ 而 2026-07_twse 的 `-KY`／`-DR` 是 **0 列**——同一期的**當期 feed 有 124 檔**。

⇒ ⭐ **這不是「那個判準壞了」**：它的判準是對的，量的也是對的東西。
⛔ 它的盲點是**結構性**的：比對的基準本身也缺同一段。

⚠ 而它跟 K線分析線 2035 ④ 是**同一族**：
「內部一致性檢查對**整批同倍數縮放**完全免疫（1~15 合計 == 17 在雙倍下仍成立）」
⇒ ⭐ **兩條的解法都只有一個：外部錨點。**
（這次的外部錨點是**另一條路**的當期 feed ⇒ 五點二那條的正面用法。）

### ⇒ 處置要兩層，⛔ 缺一層都會再犯

```
① 一顆**按得到**的開關（這一次要用的）
   ⛔ 不是去放寬那個判準——它沒有判錯
② ⭐ 一道**之後永遠有人守**的判準：「鄰居有、而我沒有 ⇒ 我少了那一段」
```

⛔ 而②**不可以**寫成「每一期都必須有 KY」——那會犯五點三（absent ≠ zero）：
真的沒有外國企業的期別會變成**永遠補不完**。
⚠ 所以②在「全部都還沒補」的當下**一律放行**（鄰居也是 0）
⇒ ⭐ 它守的是**第一次之後**，而第一次只能靠①。

### ⭐⭐ 而同一族的第三個：**「整批失敗＝故障」在收斂的終局必然誤判**（2026-09-16）

`official_stats` 的閘門是「本趟有沒有任何一檔成功」，理由正當：
端點整個掛掉時每一檔都會失敗 ⇒ 照樣累加 miss 台帳的話，**兩趟就把全庫判死**。

⛔ 而它對**另一種**全失敗完全免疫：

```
母體 1,158（⭐ 只有上市——這個端點不涵蓋上櫃興櫃）｜已完成 1,054｜本趟 104
⇒ 104 檔**全部失敗** ⇒ 判成「端點側」⇒ ⛔ 一個字都不記 miss
⇒ 那 104 檔永遠停在「還沒問過」⇒ 下一趟再問同一批、再全失敗
⇒ ⭐ **這道閘門從此每一趟都紅**，而剩下的那一批永遠不會收斂
```

⚠ 兩種全失敗在 runlog 上**長得一模一樣**：同樣 104 行
`stat='很抱歉，沒有符合條件的資料!'`（第二點：那句話講不出它是哪一種）。

⇒ ⭐ 解法跟本節開頭那條**一模一樣：外部錨點**——
拿一組「**還在母體裡**而且**以前答得出來**」的代號（`pool ∩ done`，⛔ 不是寫死的清單）
在**同一趟**問一次：它答得出來 ⇒ 端點是好的 ⇒ 全失敗是那一批自己的性質。

⚠ 而「沒有對照組可用」是**第三種**回答（`None`），⛔ 不可以跟 `False` 混在一起
——那是六點五那條：條件不成立就**大聲印出「這一層沒跑」**並退回舊判準，⛔ 不算失敗。

⇒ ⭐ 通則：**一個以「全部 X」為故障判準的閘門，在「剩下的剛好全部 X」時必然誤判。**
⛔ 而那個終局不是例外，是**續跑型工作的正常結束方式**。

⇒ ⚠ 順帶記一個當場踩到的：假檔的**表頭順序**跟真檔不同
（名稱在第 2 欄 vs 真檔第 1 欄）⇒ 只數列數時怎麼排都對，
⛔ 而讀名稱欄的判準會**永遠是 0**——**欄的順序也是形狀的一部分**（第七點）。

---
