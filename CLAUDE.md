# tw-stock-data — 動手之前一定要知道的事

⛔ 這一頁不是簡介。它記的是**已經付過代價**的規矩，每一條後面都有一次事故。

---

## ⭐⭐ 一、接新端點：**第一件事是把 `notes`／`hints`／`title` 印出來，再開始比對**

使用者 2026-09-10 定的。⛔ 這是**順序**，不是建議。

```python
import backfill as B
print("\n".join(B.describe_response(d, want={我送出去的參數})))
```

### 為什麼

我方 `_tables()` 只取 `title`／`fields`／`data`，其餘
（`stat`／`date`／`notes`／`hints`／`params`／`total`…）**整個丟掉，而且沒有紀錄**
——連「有這些鍵」都不知道。而那些鍵每一個都對應一件我方**手工做過**的事：

| 鍵 | 直接回答 | 我方原本怎麼做 |
|---|---|---|
| `total` | 官方說它給了幾列 | 另外造 N₁、Σamount÷大盤、六張清單差集（67,446 筆） |
| `params` | **我送的參數有沒有生效** | 在三支程式裡各寫一道「回應要講出我請求的那一天」 |
| `notes` | 符號說明／涵蓋起始年／單位 | 用推論，而且**沒有留下出處** |

⚠ `params` 會把收到的參數**回顯** ⇒ 被換掉就代表**那個參數是假的**。

已經做在 `feeds.py --probe` 的第一行（跳不過去）與 `keys_probe.py`。
完整順序見 `docs/NEW_ENDPOINT.md`。

---

## 二、⛔ 所有「靜默失敗」都長成 `stat:OK`

2026-09-09~10 一天之內踩到的四種，**每一種都是 200 ＋「成功」**：

```
① 靜靜回今天        參數格式錯／該用 POST 卻用 GET／查詢越界
② 靜靜回最新一期     MOPS t164sb03 的 year／season 四種組合回應完全相同
③ 回「查無資料頁」   hist.tpex 的 4,449 bytes ⇒ 32 個年份「全部命中」
④ 被 CDN 擋回 HTML   TWSE HTTP 428（只 try/catch JSON 會看成「解析失敗」）
```

⇒ 判準一律是「**這一批要自己講出它是哪一天／哪一期**」。
⛔ 不可以用「檔案夠大」「有回列」「`stat` 是 OK」當判準。

### ⭐ 而 2026-09-11 加一種：**端點的名字不是證據**

```
⑤ 名字叫 `history`、其實只有今年
   TPEx `openapi/v1/tpex_spendi_history`（「上櫃**歷史**暫停/恢復交易」）
   → 362 列、8 欄，看起來像一份歷史
   ⛔ 而 `Date` 欄的相異值只有 **1 種：民國 115（＝2026）×362**
   ⚠ 而且 181 檔裡混著權證（`CompanyName` 有「元大57購01」）
```

⚠ 這一族**頂層是陣列**（openapi.twse／tpex 那一家），
⛔ 沒有 `stat`／`total`／`notes` 可以問 ⇒ 第二點的「自己講出它是哪一期」它做不到。

⇒ ⭐ 落地：陣列型回應一律印**每個鍵的相異值分佈**（`delist_probe._spread()`）——
相異值只有一種的鍵（這裡的 `Date`）**當場就把涵蓋期間講出來了**；
相異值多的只印最小最大，⛔ 不洗版。

### ⭐⭐ 而 2026-09-11 再加一種：**官方表自己重打了一列**，而傷害會往前蔓延

```
⑦ TPEx `bulletin/revivt`（上櫃減資）283 列裡有一組
   6109 亞元：`2018-09-25` 與 `2020-09-25`
   ⛔ `last_close`／`ref_price`／原因／換股率／退還股款 **六個數字逐位相同**
   ⇒ 官方把 **1070925 打成 1090925**（283 列裡就這一組）
```

⚠ 它跟前六種不同的地方：**來源沒有壞、參數沒有錯、回應完全正常**——
⛔ 是那張表**自己**有一列是錯的。⇒ 「問回應」問不出來，只能**拿我方資料去驗**。

#### ⛔⛔ 而它的傷害**不只那一列**：`cum_factor` 是**連乘**的

```
data/adj/6109.csv 多一列 2020-09-25 f=1.01238095
⇒ ⛔ 2020-09-25 **之前每一列**的 cum_factor 全部多乘一次
   （2018-09-25 那列 0.96668147 → 0.96486184）
⚠ 而檔案看起來完全正常：欄位齊、數字連續、沒有任何地方會報錯
```

⇒ ⭐ **通則：凡是「連乘／累加」型的輸出，一筆假資料的半徑是整條序列，不是一列。**
⛔ 所以這種閘門要擋在**進入連乘之前**，不是事後報一個數字。

#### ⇒ 判準：**停止買賣型事件一定有停牌缺口**（`adjust.halting_event_is_real()`）

```
① 我方在事件日之前最後一筆收盤，就是事件日的**前一個交易日**（⇒ 沒有缺口）
② 而且官方的「停止買賣前收盤」跟我方那一天的收盤**對不上**
⇒ 兩條同時成立才判它不成立
```

⚠ **兩條缺一不可**：
只有②（有缺口、價格對不上）是我方價格／來源的問題 ⇒ **要報給人看，⛔ 不是自己吞掉**；
只有①（沒缺口、價格對得上）多半是官方把不停牌的種類也放進同一張表。
⛔ 而這條**只准對停止買賣型生效**——除權息本來就不停牌，
套上去會把任何一筆「前收對不上」的除權息**靜靜刪掉**（突變驗當場抓到）。

⭐ 而它是**算出來的**，⛔ 不是一份寫死的黑名單：
黑名單只認得已經發生過的那一筆，⚠ 而官方下次重打的會是別的代號。
（實測：283 筆官方上櫃減資裡**只判掉 1 筆**，就是那一筆。）

---

## 三、⛔ 兩個常犯的推論錯誤

1. **只比一個方向就宣告一致。**
   2026-09-10：只比「官方有的我方有沒有」⇒ 宣告某端點可當完整來源；
   補比反方向才發現它少 17 筆減資 ⇒ 照原結論做會**刪掉還原因子**。
2. **把觀察到的範圍寫成通則。**
   「我查過的那幾條端點沒有」**不等於**「官方沒有」——同一天被推翻兩次。
   同一族／同一站的不同端點，參數行為**互不相同**，每支都要自己實測。

---

## 四、長時間的抓取：**先實測，而且測整條路**

使用者 2026-09-10 定的。⛔ 只測「抓不抓得到」不夠——
2026-09-09 那趟 2,846 天跑了 5 小時，**「回補」那一步是綠的**，掛在**最後的 push**。

⇒ 兩支長工 workflow 已經寫成：

```
第一段  抓一天 → **真的 push 一次**（推不上去就整趟紅掉，代價 5 秒）
第二段  逐年分批，**每批推一次**（任何失敗最多賠一年）
```

續跑判準一律用**資料自己**（`--need-col`／`--need-notrade`），
⛔ 不要另開台帳——台帳會跟資料不一致。

---

## ⭐⭐ 四點二、⛔ **中間那一步成功了，就被當成整件事成功了**

2026-09-10 一天之內四個 bug，**四個都是綠燈**，而且四個是同一個形狀。
⚠ 第四點（「測整條路」）講的就是這件事，⛔ 只是它被寫成「長工」的特例
——**它不是特例，是通則。**

| 我看到的證據 | 它真正證明的 | ⛔ 被當成 |
|---|---|---|
| `[fetch] 保留其他市場的 2395 列` ＋ `✓ 已推上 main` | 檔案在**工作區**寫對了 | 檔案**搬到 main 了** |
| 台帳檔還在、格式對、程式回 0 | 檔案**存在** | 內容**還在**（其實被洗成一列表頭） |
| `kind` 欄 13,105 列都有值 | 欄位**有值** | 欄位是**對的值**（其實全是日期） |
| 比對算出 C 類 14,497 筆 | **算得出數字** | 算的是**對的母體**（多算了全上市） |

⇒ 判準只有一句：**問「這個綠燈證明的是哪一件事」，然後問「它在錯的時候會不會也一樣綠」。**

### ⛔ 三個已經證實會騙人的「中間點」

```
① 寫進工作區  ≠ 進到 commit  ≠ 搬到 main
   （`push_data.sh` 問的是「本趟改了什麼」——
     ⛔ 而「分支與 main 本來就不同、本趟不必改它」它問不到）
② 檔案存在    ≠ 內容還在
   （累積型的檔被**整份取代**成空的：`save_done`、`write_days`、`_coverage_backfill.csv`
     ——⭐ 這一族已經三個位置了，見第四點六）
③ 欄位有值    ≠ 值是對的
   （`_pick` 是**包含**比對：`"除權息"` 命中的是「除權息**日期**」）
⭐ ④ 派工回 `queued`／`204` ≠ 那一趟會跑
   （GitHub 的 `concurrency` group **只排得下一個 pending**，
     ⛔ `cancel-in-progress: false` 不代表後來的會排隊等——
     再送一趟，**前一個還在等的會被直接 cancelled**。
     2026-09-10 實測：連送四趟，中間兩趟從來沒跑過，
     ⚠ 而 dispatch 四次都回 `204`，**沒有任何地方會說**。）
   ⇒ 派工一趟跑完再送下一趟，並**回頭確認它真的變成 `in_progress`**。
   ⛔⛔ **而「變成 in_progress」也只是中間點**（2026-09-11 付過代價）：
     run 107 我確認過 `in_progress`，⚠ 而它的 job conclusion 是 **failure**、
     全程 **10 秒**——一支判準檔的自檢 `exit 1`，`set -e` 把同一個 step 裡
     後面六行（含換供料本體）全掐死。⇒ 四檔的除權息因子沒落地，
     ⛔ 而我照著那個狀態寫信給 K線分析線，**那封信是錯的**。
   ⇒ ⭐ 派工之後要驗**三件**，缺一件就等於沒驗：
     ① `list_workflow_jobs` 的 **conclusion**（⛔ 不是 run 的 status）
     ② 那一趟**該造成的後果**（檔案裡真的多了那幾列嗎）
     ③ `_last_run.md` 有沒有多出它該寫的區塊
   ⚠ 而③有一個陷阱：`_last_run.md` 是**跨 workflow 累積**的
   ⇒ ⛔ 那一塊可能是**別趟**寫的。**一定要看區塊裡的時戳**——
   2026-09-11 我連續兩趟把昨晚 00:27 那一塊當成這一趟的結果讀，
   ⭐ 而真正的問題是：**那個 mode 根本沒有跑那支驗收判準**。
   ⇒ 順帶一條：**一個步驟要有能驗收自己的判準跟著跑**，
   ⛔ 否則它「做完了沒」只能靠別人的排程事後告訴你。
```

### ⇒ 落地的做法：**斷言要驗終點，不是驗中間**

```
⛔ 不要斷言「寫檔成功」        ⇒ ⭐ 寫完**重讀**，斷言讀回來的內容
⛔ 不要斷言「這一步跑完了」    ⇒ ⭐ 斷言那一步**要造成的後果**發生了
⛔ 不要斷言「筆數 > 0」        ⇒ ⭐ 斷言那一批**自己講得出它是誰**（第二點）
```

⚠ 而反向那一半同樣重要：`esb_day_repair` 寫完之後**重讀日檔**、
斷言「其他市場一列都沒少」——⭐ 那條就是驗終點；
⛔ 若只斷言「write_universe_day 回了 363」，它照樣會綠。

## ⭐⭐ 四點五、⛔ **同一件事只准有一份實作**——這一族已經第六次了

2026-09-10 使用者：「同一族的錯（同一段邏輯抄兩份、只修一份）已經第三次了。」
⇒ 當天往下掃，**實際上是六次**：

```
① limit 平盤判跌停   fetch 修了、backfill.parse_twse 沒修
                     ⇒ 修好的天天跑、沒修的只有回補才跑
② 「甲」保留無成交列  backfill 修了、fetch 沒修
                     ⇒ 回補補回十一年、每日繼續挖新洞
③ 興櫃 0 價 `_isz`   esb 逐月檔有、fetch 的日檔沒有 ⇒ 52 列 close=0 寫進去
⭐ ④ backfill.parse_openapi ——**①②③ 三個全中**，而且它是興櫃回補走的那一條
   ⑤ write_day「保留沒抓的市場」＋「濾掉權證」只有回補那份有
     ⇒ 每日那份同一天跑第二次、某市場失敗 ⇒ 那個市場的列無聲消失
   ⑥ _kind／_tables／_num／_isz 各兩份（還沒走岔，⛔ 但「還沒」不是判準）
```

### ⛔ 原本寫在檔頭的理由是錯的

> 「刻意複製而不 import，讓它們各自獨立，改一邊不會意外弄壞另一邊。」

⚠ **六次沒有一次是那個方向。** 真正發生的一律是
**「改一邊，另一邊沒跟上，而且沒有人會發現」**——
⛔ 而兩份長得幾乎一樣的程式，**肉眼 review 看不出誰少了哪一行**。

### ⛔ 而往下掃是**十份**，其中一份會憑空造出一個交易日

```
⑦⑧ _iso／_pick     兩支歷史腳本各一份
⭐ ⑨ _same_day      **每日那支根本沒有**
   ⇒ today 是猜的（不知道國定假日與颱風停市）
   ＋ 候選清單最後一條 STOCK_DAY_ALL 沒有 date 參數、永遠回最近一個交易日
   ⇒ 把上一個交易日的行情寫進「今天」的日檔。stat=OK、數字全真、只是別天。
⑩ _post／_num／_is_dash   兩份
```

⇒ 判準：**同一件事只准有一份實作。** 另一邊用別名／import 指過去。
⛔ 「兩邊各留一份、記得同步」已經證明做不到。

### ⇒ 做成常駐守門（跟第六點五那條同一個道理）

    .githooks/pre-commit    動到任何 .py 就掃 → 有第二份就**不讓 commit**
    八支 workflow 的第二步   `python selftest_no_dup.py`

⛔ **兩個地方都要有**：hook 可以被 `--no-verify` 跳過，workflow 裡那道跳不掉；
而 workflow 裡那道要**推出去、按下去、跑起來**才叫，hook 在壞掉的那一刻就叫。

`selftest_no_dup.py` 比的是**函式本體的 AST**（⛔ 不是字面 ⇒ 改個變數名躲不掉），
先拿掉 docstring（各寫各的說明不算差異），⚠ 少於 3 個 statement 的不算
（⛔ 門檻太低會每天紅，然後被學會忽略）。
⚠ 白名單目前是空的，⛔ 要加之前先回答「為什麼這兩份不能收成一份」。

⚠ 而別名**可以被下一個人拆掉**（拆掉的當下一切正常）
⇒ 一定要有一節**逐格比對**的自測餵一整批不同形狀的回應
（`selftest_parse_daily.py` ⑨），⛔ 不是比「是不是同一個函式物件」。

---

## ⭐ 四點六、⛔ **累積型的檔，不可以整份覆蓋**（分支上的 `data/` 比 main 舊）

2026-09-10 實際發生的遺失：

```
07:15  一天實測（2026-09-09）推上 main ⇒ `_coverage_backfill.csv` 多一列
07:20  2015 那批開跑。它 checkout 的是**分支**，
       而分支上的 `data/` **沒有**那一列（那一列是推到 main 的，不是推到分支）
07:22  它把整個檔搬到 main ⇒ ⛔ **五分鐘前那一列被刪掉**
```

⚠ 而 `git diff` 看起來完全正常：**一加一減，像是「這一趟重算過」**。

⭐ `push_data.sh` 早就替 `_last_run.md` 做了逐區塊合併，理由一字不差
（「跨 workflow 累積 ⇒ ⛔ 不可以整份取本趟的」）——
⛔ **而累積型 CSV 一直是整檔覆蓋，同一個道理只做了一半。**

⇒ `merge_ledger.py`：具名清單 ＋ 主鍵，逐鍵合併（本趟的鍵覆蓋、其餘原封不動）。
⚠ **是具名清單，⛔ 不是「所有 CSV 都合併」**：`_missing_rows.csv` 那些是
每趟全量重算的，合併它們會把**已經修好的舊列復活**。

⚠ 判準裡最重要的一條：**合併後的列數不可以少於 main 那一份**
——⛔ 這支自己絕不可以變成刪東西的那個人。

### ⛔ 而這一族**不只 CSV**——2026-09-10 找到第三個位置

```
① `_coverage_backfill.csv` 等累積型 CSV   ⇒ `merge_ledger.py`（本節）
② `otc_adj.write_days`（一天一檔的事件）  ⇒ 檔頭自己記著「第一版是整檔覆寫」
⭐ ③ `otc_adj.save_done`（FinMind 續跑台帳）
   不帶 `--resume` 跑一趟 ⇒ `done` 是空集合 ⇒ **1,996 列整份洗成一列表頭**
   ⚠ 而檔案在、格式對、程式回 0；下一趟「已完成 0 個」⇒ 從頭重跑 1,942 發
   ⛔ 而那正是免費額度撐不過的跑法 ⇒ **永遠跑不完，每趟都像有在跑**
```

⇒ 判準：**任何「這一趟只知道自己那一部分」的寫入，一律是合併，不是取代。**
⚠ ②的檔頭早就寫著這個道理——⛔ 而③就在同一個檔案裡，**只做了一半**。

### ⭐ 而它有一個**鏡像**，2026-09-10 連續踩到兩次

本節到這裡講的都是「分支上的 `data/` 比 main **舊**」。
⛔ 另一半是：**分支上根本沒有那個檔**。

```
① `esb-repair` 那趟：`push_data.sh` 問「本趟改了什麼」
   ⇒ 分支比 main **新**、而本趟不必改它 ⇒ 那個檔**從來沒被搬過去**
② `otc-adj-codes` 補 6461 那趟：`data/meta/otc_reduce_history.csv`
   **只在 main 上**（判準檔是別的 mode 抓的、推到 main）
   ⇒ workflow checkout 分支 ⇒ 讀到的是「檔不存在」
   ⇒ ⛔ `official_factor` **整欄空白**，而那一趟**成功**、`data/adj/` 有更新、
     數字全對——只有 K線線裁定要用的那一欄靜靜空著
```

⚠ 兩次的共同形狀：**「這個檔在哪個 ref 上」跟「程式讀不讀得到它」是兩件事**，
⛔ 而讀不到的表現是**空值**，不是錯誤。

⇒ 落地兩條：
- workflow 裡凡是**讀** main 才有的判準檔，先 `git checkout origin/main -- <path>`
  ⛔⛔ **而 `<path>` 要是程式真的讀的那個，不是名字看起來像的那個**
  （2026-09-11 付過代價：我取了 main 的 `calendar_twse.csv`，
  ⚠ 而 `adjust.trading_days()` 讀的是 **`data/universe/daily/` 的檔名集合**
  ⇒ 修完再跑一趟，runlog 照樣寫「交易日曆最後一天 2026-09-09」，
  三筆除權息照樣被當成未來事件擋下。⇒ ⭐ **先 grep 那支程式讀哪裡**，⛔ 不要照檔名猜。）
- ⭐ 程式端**自己**要擋：讀不到判準檔就**大聲失敗**，⛔ 不是寫出一整欄空白
  （`otc_adj.check_official_table()`）。⚠ 而判準是「**對照表有沒有內容**」，
  ⛔ 不是 `os.path.exists`——一個空殼檔會讓後者靜靜放行。

### ⭐⭐ 而第三次（2026-09-11）不是檔，是**程式**：排程跑的是 `main`

```
③ `probe.yml` 的「自癒」：檔頭寫著「探針程式比輸出新 ⇒ 下一趟排程會自己重跑」
   00:25  我在**分支**上改好 `delist_probe.py`（加了雙向比對）
   00:41  排程觸發 probe.yml ⇒ ⛔ `head_branch: main`、跑的是**舊的**那一份
   ⇒ 它照樣成功、照樣寫出 `_delist_probe.txt`、照樣推上 main
   ⇒ ⛔ 而那個檔裡**沒有**雙向比對那一節——⚠ 而「沒有那一節」看起來
     跟「那一節查無結果」**一模一樣**
```

⚠ 共同形狀還是那一句，只是主詞換了：
**「這個東西在哪個 ref 上」跟「它會不會跑」是兩件事。**

⇒ 落地：
- ⛔ **`schedule` 與 `push` 觸發的一律是 `main`**（分支上的新步驟、新程式，排程看不到）
- ⇒ 要驗分支上剛加的東西，一定要 **`workflow_dispatch` 並指定分支**，
  ⛔ 不可以「等排程自己跑到」——它跑的是另一份程式。
- ⚠ 而**判準是輸出裡有沒有那一節**（我方那一節的標題），
  ⛔ 不是「這一趟成功了沒」——舊程式一樣會成功。

#### ⛔⛔ 而上面那條有一個**會刪資料**的例外，2026-09-11 差點踩下去

指定分支跑，拿到的是**分支的程式**（要的）＋**分支的 `data/`**（⛔ 不要的）。
而分支的 `data/` 永遠比 main 舊——所有資料都是 workflow 推到 **main** 的。

```
⇒ 只讀程式、不讀 data/ 的步驟（探針）      ⇒ ✅ 指定分支，沒問題
⇒ **讀 data/** 的步驟（transpose／各種
   衍生檔重建／比對／合併）                ⇒ ⛔⛔ 指定分支 = 拿過期的輸入重算，
                                            而輸出會**整份覆蓋 main 上較新的那份**
```

⚠ 具體差點發生的：`transpose.yml`（全量重建 `data/stocks/`，0.55 GB）。
在分支上跑 ⇒ 讀分支那份過期日檔 ⇒ 產出缺列的個股庫 ⇒ `push_data.sh` 判定「有變動」
⇒ **把 main 上補好的無成交列整批刪掉**，而且 `git diff` 看起來像「這一趟重算過」。

⇒ 判準一句：**問「這一步讀不讀 `data/`」。**
- 讀 ⇒ 要嘛 `git checkout origin/main -- <那些路徑>` 之後再跑，要嘛**在 main 上跑**
- ⛔ 不讀才可以放心指定分支

---

## 五、寫入資料前先問：**誰是這個目錄的唯一寫入者**

換供料是**換維護者**的決定，不是順手加一行。
兩支程式寫同一批檔＝後寫的贏、跟新舊無關，而且四個地方都顯示正常。

---

## 六、⛔ 這個開發容器**對交易所一律 403**——而那是**我方閘道**擋的

```
kind: connect_rejected｜gateway answered 403 to CONNECT
```

⇒ 意思跟「交易所擋人」**完全相反**：

```
對方擋     ⇒ Actions 上也會擋
我方閘道擋  ⇒ Actions 上是通的
```

⛔ 所以在這裡跑正式抓取程式，得到的失敗**不是事實**。
`append_coverage()` 已經會擋（不寫假紀錄），
`runlog` 會在區塊上標「⚠ 這一塊不是 Actions 跑的」。

---

## ⭐ 六點五、動到 workflow／`.sh` 之後，**commit 之前**一定要驗 shell 語法

    git config core.hooksPath .githooks     ← 新 session 進來第一件事

`.githooks/pre-commit` 會在動到 `.github/workflows/*.yml` 或 `.sh` 時
跑 `selftest_workflows.py`（8 支、117 個 `run` 區塊逐個 `bash -n`），不過就擋下 commit。

### ⛔ 這一條的價格是 1 小時 50 分鐘

run 34426373917：「回補」跑了 1h50m 是綠的，「Commit」1 秒失敗——
`syntax error: unexpected end of file`，我批次改 workflow 時把 closing `fi` 切掉了。

⚠ 而我當時「驗過」，驗的是 `yaml.safe_load()`。
⛔ **YAML 合法 ≠ 裡面的 shell 合法**——對 YAML 來說 `run:` 只是一個字串。

⚠ 而檢查排在 workflow 裡是**不夠的**：那是推出去、按下去、跑起來之後才叫，
壞掉的那一刻是在**本機 commit**。⇒ 兩個地方都要有。

---

## 七、測試：**沒證明過會失敗的測試，不算測試**

- 每一條新斷言都要證明它**會紅也會綠**。
- 假回應要**照真回應的形狀**做——假的比真的簡單，等於那段沒測。
- 回報「某群 0 筆」時，必須附上「該判準在該群抓到的正例數」。
  （曾經有一條偵測器在上市的正例數是 0／9,597，而那個 0 被讀成「上市乾淨」。）

### ⛔⛔ 而「突變驗」本身有一個會讓它整個失效的陷阱：`__pycache__`

2026-09-11 實際發生：

```
突變 M1（交換兩個 if 的順序）  ⇒ 測試**還是綠**
突變 M5（`s <= b and e >= a` 改成 `a <= s and e <= b`）⇒ 測試**還是綠**
⇒ 我據此認定「這兩條斷言是假的」，⭐ 而且動手去改掉其中一條**其實是對的**斷言
```

成因：Python 的 `.pyc` 只用 **mtime ＋ 檔案大小**判斷要不要重編。
⚠ 而上面兩種突變都是**同尺寸**的（交換順序、換比較方向），
⛔ 同一秒內改完又立刻跑 ⇒ `.pyc` 被判定still fresh ⇒ **跑的是突變前的程式**。

⚠ 這個失效的方向是**最糟的那一種**：
突變後還是綠 ⇒ 結論是「這條斷言沒用」⇒ **有人會去刪掉一條其實有用的斷言**。

### ⛔⛔ 第二個陷阱：**突變的「紅」不可以只數 `✗`**

2026-09-11：拿掉 `runlog.now_tpe` 之後 `grep -c "^  ✗"` 回 **0**，
⚠ 我差一點讀成「這條斷言沒用」——⛔ 而它其實**炸掉了**：
崩潰吐的是 traceback，**不會印 `✗`**。

⇒ ⭐ 判準是 **`rc != 0` 或輸出裡有 `✗`**，⛔ 不是只數 `✗`。
⚠ 而更好的做法是**讓它印 `✗`**：斷言那條路若會丟例外，就 `try/except` 接住再判——
⛔ 不接住的話整支測試當場中斷，**後面一條都不會跑**
（接住之後同一個突變從「紅 0」變成「紅 5」）。

### ⛔⛔ 第三個：**測了判準、沒測「呼叫點」與「預設值」**——這一族已經兩次

```
① otc_exright_check   測了 `classify()`，⛔ 沒測 `main()` 傳什麼基準進去
                      ⇒ 把基準改回 `today` 的突變**沒抓到**
⭐ ② feeds.month_is_open(ym, today=None)
      18 條斷言**每一條都傳了 `today=`**
      ⇒ ⛔ 預設值那條路**一次都沒被走過**，而那是 Actions 上唯一會走的那條
      ⇒ `runlog.now_tpe` 根本不存在 ⇒ **AttributeError 掛在 Actions 上**，
        ⚠ 而自測 18 條全綠
```

⇒ 判準兩條：
- **有預設值的參數，一定要有一條不傳它的斷言。**
- **測完純函式，再掃一次原始碼**確認呼叫點真的那樣叫（`otc_exright_check` 那支就是這樣補的）。

### ⛔⛔ 第四個：**突變根本沒套上去**，而它看起來跟「沒抓到」一模一樣

2026-09-11：我把錨點字串抄錯一個字（少了「的」）⇒ 取代了 **0 次**
⇒ 跑的是原版 ⇒ 當然全綠 ⇒ ⛔ 又看起來像「這條斷言沒用」。

⇒ ⭐ **三種結果一定要分開**，⛔ 混成「綠/紅」兩種就是上面那個誤判：

```
✅ 抓到          斷言有用
⛔ 沒抓到        ⇒ 這條斷言可能是假的（⚠ 先確認不是下面那一種）
⛔⛔ 沒套上去    ⇒ **我的錨點錯了**，跟斷言好不好無關
```

### ⇒ 落地：**不要再手寫那個 shell 函式**，用 `mutate.py`

```
python3 mutate.py --file adjust.py --test selftest_adjust_official.py \
    --label "M1 拿掉停牌缺口那一半" --old '舊字串' --new '新字串'
```

它把上面四個陷阱都收進去（改檔前後與還原後各清一次 `__pycache__`、
「紅」＝ `rc != 0` **或**有 `✗`、錨點不是剛好 N 次就回 **2** 並明講
「這不是斷言沒用」、`finally` 保證還原）。
⚠ 它自己也有自測（`selftest_mutate.py`，17 條、突變 6 次全中），
⭐ 因為**它是判斷「斷言有沒有用」的那把尺**——
⛔ 尺壞掉的方向只有一種：它說「沒抓到」⇒ **有人會去刪掉一條其實有用的斷言**。

⚠ 而手寫 shell 版的四個陷阱**每一個我今天都踩過**，
⛔ 所以這一節的結論不是「記得清 cache」，是**同一件事只准有一份實作**（四點五）。
⚠ 而同一個道理也適用於**還原**：還原後不清 cache，下一支測試會讀到突變版。
⭐ 清掉之後上面那五次突變**全部命中**（M1 也證明了順序真的有意義）。
