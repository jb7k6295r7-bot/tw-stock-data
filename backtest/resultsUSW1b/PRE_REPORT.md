# USREG-W1b pre：候選盤點與讀法計數（⛔ 未跑引擎、未讀任何持有期報酬）⇒ ⛔ 停在兩個停下條件

| 欄 | 值 |
|---|---|
| 判準 | 美股登錄 seq2 §五＋seq3 §四＋seq4 §二§三＋seq5 §一～§三；裁定 seq168 §三、seq176 §二、seq182、seq214 §三；台股 W1 ＝ P12 (S1, C1, T1)（researchp12／researchAFC） |
| 資料 | us-stock-data `0043f97d974df78ffe5626623aed2caecfd4a5b8`；判定窗 2016-01-04～2026-08-31；量測日 126 個（每月第一個交易日，最後一個 2026-04-01：xpos 須在日曆內）；可用 712 檔 |
| 程式 | `backtest/researchUSW1b.py --pre`（讀法 Z1～Z10 寫在開頭）；逐月面板與訊號表在 `~/us_work/usw1b/`（repo 外），sha 見 `pre_work_sha.csv` |

## 一、閘（fixture 全過）

```
FW1 季營收旗標：8 季遞增新高／d 前一天只有 7 季⇒不足／容差 0.5e-4 過（精確不過）、2e-4 不過／相距 141 天⇒缺季、140 天過／value＝0⇒當缺／52-53 週兩季同曆季⇒會計季版過、曆季逐字版不過／d 之後才可用的一季不影響 d
FW2 窗內年化／回落手算相符；標籤三種（合格／另列／條件一等號不過＝不合格）
FW3 ma_stack／ma60_up 向量式＝逐點手算（台股 p4_features 同式）
```

## 二、候選與訊號（股-月）

```
在指數且有效 K 棒 ≥ 120 根：58,911
營收判定帳：非新高 35,653、新高 19,526、不足8季 1,403、缺季（相距>140天） 1,176、不適用（無營收列） 882、value≤0當缺 271
value ≤ 0（當缺）：100 列、29 檔｜不適用 19 檔（名單見 pre_summary.json；SIVB、FRC、SBNY 在內 ⇒ 倒閉銀行依構造不可能被選到）
候選（∧ 營收新高）19,526｜訊號（∧ ¬ma_stack ∧ ma60_up）6,714｜訊號表剔除 {'xpos≥日曆': 0, '開盤不可用': 0, '收盤不可用': 0}
每月訊號數：125 個月、零訊號 6 個月、中位 50（p10 20.4／p90 91.4）、≥ 8 檔的月份 94.4%
第一個進場日 2016-06-02（面板最早 2015-12-01 ⇒ 要湊滿 120 根有效 K 棒 ⇒ 窗首約 5 個月依構造空手）
描述臂（seq5 ①）曆季逐字會被踢出：營收新高股-月 1,298（77 檔），其中也是訊號 445 ⇒ 訊號數 會計季版 6,714／曆季逐字版 6,269
Z4 流動性：在指數且 bars_ok 的股-月 amt20 最小 3,984,112 美元 ＞ 新台幣 5,000 萬換算上限 2,000,000 美元 ⇒ 此閘對候選沒有作用（照全過）
```

## 三、⛔ 停下條件（登錄與 seq214 都沒寫、而且改動候選集合）

```
① Z5 營收新高的比較：台股 rev_hi24 用對稱容差（≥ 最大值 − |最大值|×1e-4）；登錄只寫「創近 8 季新高」
   ⇒ 容差版比精確 ≥ 版多 3 個訊號股-月（最新一季比前 7 季最大值低 0.003%～0.009%）
   選項：(a) 照台股先例用容差　(b) 照登錄字面用精確 ≥
② Z7 轉接層硬斷點：特徵回看窗（量測日往回 120 根）跨到來源接縫的訊號 2 筆（IR 接縫 2020-03-02 之後的兩個量測月；持有期跨斷點 0 筆）
   ⚠ IR 接縫前後是兩家不同公司（yahoo:TT → yahoo:IR），均線跨接縫算 ⇒ ma_stack／ma60_up 混了兩家的價格
   選項：(a) 剔除這類訊號（同 USREG-M／X「範圍內有斷點 ⇒ 剔除」、轉接層「⛔ 不跨斷點」）　(b) 保留（台股 W1 沒有斷點剔除）
⇒ 兩處都只動 2～3 個股-月（訊號表 6,714 筆），⛔ 本支沒選、⛔ 未跑引擎
```

## 四、後續：兩處讀法已定案（協調者轉達）

```
① 營收新高 ⇒ (a) 容差（台股 rev_hi24 先例；W1b 是 W1 的移植、口徑一致）；敏感度：精確 ≥ 版另跑
② 跨轉接層斷點 ⇒ (a) 剔除（同 USREG-M／X、轉接層「不跨斷點」）；敏感度：保留版另跑
⇒ 本體已照此跑完，見 BODY_REPORT.md
```
