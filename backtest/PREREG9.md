# PREREG9：研究十一長版（妖股／飆股相似度計分）＋ 研究十二 創新高拆解 ＋ 第七項 多頭排列持有期相依

事前登錄。K線分析 2026-09-11 23:25／23:40、09-12 00:20／00:35 四封交辦七項；本檔跑前 commit，跑完只追加「更正／追加」，不改本文。程式 `backtest/research11.py`，輸出 `backtest/results11/`。

## 〇、母體、期間、成本

- 母體：`data.load_universe()`（`kind == stock`、`market ∈ {twse, tpex}`，興櫃、ETF、DR 不在），2015-01-05～2026-09-09（併 main 04a329eea 的資料）。**主表不設流動性閘門**（與 K線分析短版對得上），流動性當變數報：近 20 日成交金額均值 ≥ 1,000 萬／2,000 萬／5,000 萬三格。
- 價格：`data.load_stock()` 還原價；訊號在**有成交日**的序列上算（「近 N 個交易日」＝近 N 根有效 K 棒）。無成交列、洞為 NaN。
- 進場：訊號日次一有效 K 棒的開盤。成本來回 0.585%；滑價當變數：0／0.3／0.5／1.0%（來回）。
- 硬斷點與公司行動排除：訊號日前 20 根與持有期（到出場日）內，若含 (a) `data.breakpoints`＋`applies()` 的斷點 T、(b) 相鄰兩根有效 K 棒之間交易日數 −1 ≥ 5 的洞、(c) 「因子與價格接不起來」的還原事件（事件日收盤 ÷（前收 × factor）超出 [0.895, 1.105]；就是稽核 F2 那類），整筆剔除。⚠ 一般除權息不剔除，因為價格已還原。
- 上市前 5 個交易日無漲跌幅限制，不算訊號；C5 需要 250 根有效 K 棒，不足者該日不算訊號（K線分析 09-11「視窗內有效 K 棒不足該視窗不可用」）。

## 一、研究十一：相似度計分

訊號日 t（有效 K 棒），五條各 1 分：
- C1 `close_t / close_{t−20} − 1 ≥ 30%`
- C2 近 20 根（t−19～t）**漲停**根數 ≥ 3。⛔ 漲停不用資料庫 `limit` 欄（召回 9～22%），用價格自算：未還原收盤 ＝ 前一有效 K 棒未還原收盤 ×(1＋限幅) 依升降單位（<10 元 0.01、<50 元 0.05、<100 元 0.1、<500 元 0.5、<1000 元 1、其餘 5）向下取整；限幅 2015-06-01 前 7%、之後 10%；還原事件日當天不判（基準價變了）。跌停對稱（向上取整）。
- C3 `amount_t ≥ 3 × mean(amount_{t−20}～amount_{t−1})`
- C4 `close_t > MA100_t`
- C5 `close_t = max(close_{t−249}～close_t)`
- 主分析 **score ≥ 3**；4／5 與 5/5 另報（5/5 樣本不足不下結論）。去重：同檔訊號後 20 根內不再取。
- ⭐ 量當變數：主表另拆「C3 成立 vs 不成立」兩組報 H20／H60／H120（K線分析 00:35）。

出場八條，全部報、不挑：H5、H10、H20、H60、H120（固定持有 N 根有效 K 棒後收盤出）；A2、A3（Wilder-14 ATR 追蹤：`ATR_t = (13·ATR_{t−1} + TR_t)/14`，種子為前 14 根 TR 算術平均，需 ≥ 114 根；停損線 ＝ 進場後最高收盤 − k×ATR，只在創收盤新高時上調；收盤 < 停損線 → 次一有效 K 棒開盤出；上限 120 根）；M5（收盤 < MA5 → 次開盤出；上限 120）；LD（進場後第一根跌停 → 次開盤出；上限 120）。

每格報：n、平均、**中位數**、勝率、**最壞單筆**、p10、平均持有根數、月分群 95% CI（訊號月）、對同持有期母體基準的超額（基準 ＝ 所有通過排除條件的股票日、同進場同持有、扣成本）。

## 二、七項

1. **樣本外**：2015-01～2020-12（A）、2021-01～2026-09（B）兩段各報全表；另做「A 段選 27 格裡 H120 超額最高的一格 → 報它在 B 段」的驗證（事前寫死：選法只看 A 段 H120 超額）。
2. **滑價**：0.3／0.5／1.0% 三檔全表重算；報「滑價多大時 H120 超額歸零」（線性內插）。
3. **配對停損**：同一筆訊號兩種出場的配對差：A2−H10、A3−H20、M5−H5、LD−H120；平均、月分群 CI。LD 拆「真的跌停出場」與「抱到上限」兩群，各對同一批的 H120。
4. **組合層**：N ∈ {5, 10, 20} 等權槽、訊號多於空槽時隨機挑、200 種子；規則 H20、H60、H120、LD；**逐日按收盤市值算權益**（不是階梯），報年化中位數與 p10～p90、最大回落中位數與 p10～p90、槽位使用率。同期訊號的相關性由「同一天只能進空槽數」自然帶入。
5. **27 格**：C1 ∈ {20, 30, 40%} × C3 ∈ {×2, ×3, ×5} × 去重 ∈ {10, 20, 40 根}，score ≥ 3，每格報 n 與 H20／H60／H120 超額、中位數；**全報不挑**。
6. **創新高拆解**（基準組 B ＝ 當日漲幅 ≥ 7% 且收盤 > MA60，量不當前提）：(c) **先算**重疊率：B 內「120 根新高」與「250 根新高」同一批訊號日的重疊 ＝ |P120 ∧ P250| ÷ |P120| 與 ÷ |P250|；研究十一 3/5 訊號中 C5 成立者與 P120 的重疊；(a) 回看窗 60／120／250／500 根，各報「創 N 根新高 vs 未創」的 H20／H60／H120 差與 CI，四格全報；(b) 連續版：`close ÷ max(close 近 N 根) − 1` 在 B 內十等分，各報 H20／H60／H120 平均與中位（N＝120 與 250 各一組）；(d) 全部分 A／B 兩段。
7. **多頭排列持有期相依**：B 內 `close > MA5 > MA20 > MA60` 有／無，H20／H60／H120 差與 CI，A／B 兩段各報。⚠ 這是均線代理，不與研究十的橫斷面動能互相引用。

## 三、判定用語（K線分析 09-09 11:05 兩層）

統計層：月分群 CI 不含 0 ＝ 測得出；含 0 ＝ 測不出。可執行層：扣成本與滑價後剩 X pp／不可執行。⛔ 只報平均不報中位數與最壞單筆的格子不算完成；5/5 層不下結論；不事後挑格。空方無；「某群 0 筆」附該判準在該群抓到的正例數。

## 四、事前預期（寫下來免得事後解讀）

- K線分析短版基準（3/5、無閘門、9,066 筆）：H120 +13.22%、超額 +3.74 pp、LD − H120 = −2.11 pp。長版主表若對不上 ±1 pp 以內，先找定義差（漲停自算 vs `limit` 欄是已知的一個）。
- 預期 A 段與 B 段超額同號但幅度不同；滑價 1% 會吃掉 H20 大半、H120 小半；LD 配對差為負；27 格中 C1 越高 n 越少、超額越高；創新高四個回看窗方向一致但 500 根 n 小；多頭排列只有 H120 那格站得住。任一與預期不符都照報。

## 五、不做的事

不做分數排序、不加新條件、不改 C1～C5 的定義、不做參數最佳化（27 格是掃描不是選擇）。

## 追加一（2026-09-12 23:40，跑完之後寫；本文未改）

1. **A 段實際是 2016-01～2020-12**：C5 要 250 根有效 K 棒，2015 全年暖機、沒有訊號。判定不受影響（A／B 仍是兩段互斥的樣本外）。
2. **組合層報表第一版用了近似**（權益 ＝ 現金 ＋ 持倉以進場金額計，出場才結算），不是本文寫的逐日收盤市值。改成逐日市值（`simulate_mtm`：進場價 ＝ 進場日開盤還原價、市值 ＝ 收盤 ÷ 進場開盤）之後重出報表。⚠ 老實講：改法在跑的時候就決定了，但第一版數字在改完前已經看過；兩版年化中位差 ≤ 0.4 pp、最大回落差 ≤ 1.2 pp，判定沒有因此變。
3. **跑完後加的三樣東西**（都是加、不是換）：(a) 主表多一欄「超額 月配對 95% CI」（每筆減同月同持有期母體基準再做月分群），比本文寫的「月分群 CI ＋ 基準當常數」嚴，判定改用它；(b) 5/5 層的表（本文寫「另報」但程式第一版沒印）；(c) 組合層加一行 0050 還原價買進持有同窗年化與最大回落當可執行替代。(a)(c) 是事後加的比較嚴的判準，不是放寬。
4. **可執行層的對照沒有寫在本文**：本文的「基準」是等權股票日母體；0050 是市值加權指數。兩者不同、事後補的，結論裡分開講。
5. 訊號頻率（K線分析 09-09 裁定 PREREG 必填，本文漏了）：每月中位 166 筆、最多 568 筆（2021-04）、每年 1,512～3,055 筆。
