# 營飆停損六族（裁定 seq200）＋營飆時停九格（裁定 seq201）＋ⓓ 補股池：停損線建構器與 fixture

回測線，2026-09-26（台北）。⛔ 沒跑任何判定格、⛔ 沒讀年化／回落；引擎只跑手造小例（引擎＝git HEAD 的 research11.py，blob e237a0f8b0）。

- 建構器：`backtest/yfstop_lines.py`（新檔；⛔ 未改 listexit_lines.py、research11.py）
- fixture：`backtest/selftest_yfstop.py fixtures | real | pool`
- 輸出：`resultsYfStop/lines_fixtures.json`（47 條，0 失敗）、`lines_real.json`（25 條，0 失敗）、`lines_pool.json`（5 條，0 失敗）、
  `lines_stats.json`、`lines_pool_stats.json`、逐列摘要 `lines_<族>.json`、池 `pool_S_nodedup.csv.gz`／`pool_and_nodedup.csv.gz`

## 一、讀法（登錄字面／登錄沒寫的，逐條）

| # | 項 | 本件做法 | 性質 |
|---|---|---|---|
| 1 | 比較方向 | P15「≤」、T20「≤」⇒ `stop_line_le=True`；A3（同 S2「＜」）、M20、M50「＜」、F（PREREGD5「還原收盤 < 停損」）⇒ `False`；時停 R0「≤」⇒ True、R10「＜」⇒ False、NH 線＝必觸發 ⇒ False | 登錄字面 |
| 2 | 引擎時序 | levels[d] 只用 ≤ d 的收盤；引擎在 t 開盤比 closes[t−1] 與 levels[t−1−start] ⇒ t 開盤整檔出（research11 docstring＋stop_line 段逐字核對） | 引擎既有 |
| 3 | P15 的 ep | 引擎進場價（開盤，無效改收盤）；線 ＝ ep×85/100 | ＝ S1 同一條 |
| 4 | T20 起點 | 進場日【收盤】（登錄字面）；⛔ 不是 ep（引擎 P7 的 stop=("trail") 從 ep 起算，是另一條路） | 登錄字面 |
| 5 | A3 | ＝ listexit_lines.s2_lines(mult=3, high_start="entry_close") 原樣（起點甲、Wilder 14、嚴格新高才上調、只升不降） | 登錄「同 S2」 |
| 6 | 均線含不含當根 | 含該根自己的收盤（研究十一 M5 同寫法 c[j] ＜ ma5[j]）；決策日 t 只用 t−1 以前 | 登錄沒寫；依委派指示與既有寫法 |
| 7 | 均線的根與停牌 | 只數有效 K 棒（load_bars traded）；停牌日線沿用前一根；收盤取引擎 closes（float32，與引擎比較同一個數） | 登錄「有效 K 棒」 |
| 8 | ⭐ 均線族：進場日收盤已在均線下 | **未選**，建構器 `arm` 必填，三種都建好：`state`（字面「收盤＜均線」是狀態 ⇒ 次日開盤就出）、`cross_sig`（讀成「跌破」，訊號日當前一根）、`cross_entry`（讀成「跌破」，進場後先站上才算） | ⚠ 待裁定 |
| 9 | F | 直接呼叫 stop_fractal.stop_line（a68eb28840）；日曆對應照 researchD5.build_lines（真資料 1,688 條與 D5 原函式逐位相同） | PREREGD5 原樣 |
| 10 | 浮點寫法 | 比例線一律 x×pct/100（真值可表示時恰好）；證明：(1−0.15)×ep 與 ep×85/100 在可表示時等價（真資料 0 列不同；旁查 S1 的 0.9×ep 也 0 列不同）；但 ep×1.10 會偏大一個 ulp（例 50×1.10＝55.00000000000001）⇒ R10「剛好漲 10%」會誤判成沒反應 ⇒ R10 必須用 ep×110/100 | 本件發現 |
| 11 | 時停「第 d 根」 | 第 1 根＝進場日（有效 K 棒 idx[k+1]）；線只放在第 d 根的日曆位置，其餘 NaN；第 d 根之後停牌 ⇒ 引擎 sl_pending 延到下一個有效開盤（手算 H2 證明：日曆 27 停牌 ⇒ 28 開盤出） | 登錄＋seq201 |
| 12 | ⭐ NH 的 +∞ | 引擎只比 `np.isfinite(線)` 的線 ⇒ 登錄字面 +∞ **永遠不觸發**（手算 H9 證明）⇒ 用 float64 最大有限值 BIG | 本件發現 |
| 13 | NH 事件 | 第 m 根（m ≥ 2）收盤嚴格大於第 1..m−1 根所有收盤；第 1 根不算；窗＝第 max(2,d−19)..d 根（seq201 ②）；與前一根比較或平高點都不算 | seq201 字面 |
| 14 | ⓓ 池 | 研究十一主格 30\|3 去掉 20 根去重（GRID 暫換成 30\|3\|0 呼叫原函式）⇒ and_flags ⇒ t−1 大盤閘 ⇒ 主窗；「未持有」由引擎排除 | seq200 ⓓ |

## 二、fixture

- 手算 29 條全過：T0 比較方向表；P1–P2（P15 線、開盤 NaN、收盤恰＝線 ⇒ 次日開盤出）；T1–T2（T20 停牌不算、起點＝進場日收盤、恰＝線觸發）；A1；M1–M8（n＝3 含停牌手算、三種 arm、arm 沒給報錯、平盤「＜」不觸發）；F1–F2（棘輪不退、s+2 才生效、stop_fractal 原 fixture）；H1–H9（第 20 根位置、R0 次日／停牌延後、R10 恰好 10%、NH 四例、NH 引擎出場、+∞ 不觸發）；L1 無前視（60 個隨機世界 × 全部族）
- 突變體 18 個全被抓到：六族比較方向各翻一次、T20 會退、均線含下一根（前視）、均線不含當根、均線數日曆根、d 差一根、NH 把第 1 根算事件、NH 平高點算事件、R0 嚴格、R10 用 ×1.10、NH 用 +∞、F 棘輪會退、F 確認提早一根
- （移除一個等價突變：P15 用 (0.85)×ep ⇒ 已證明與本式相同）
- 真資料：全部 1,688 條（2,036 列中 xpos_H120＜0 的 348 列不建線）與獨立迴圈版逐點相同（rtol 1e-12）；T20／A3／F 無任何一條下降；時停九格與獨立版逐點相同

## 三、真資料：略過數與停損距離（1,688 條；只看價格與線，⛔ 沒跑引擎）

停損距離 dist0 ＝ 1 − 進場日收盤那條線（引擎第一次比的線）／ep

| 族 | 略過 | dist0 p10／中位／p90 | 線 ≥ ep | 進場隔天就觸發 | 價格層曾觸發 | 觸發距進場根數 p10／中位／p90 |
|---|---|---|---|---|---|---|
| P15 | 0 | 15.0／15.0／15.0% | 0 | 0（0.00%） | 53.9% | 5／25／86 |
| T20 | 0 | 15.5／20.4／24.3% | 0 | 0（0.00%） | 82.6% | 12／38／92.6 |
| A3 | 0 | 5.5／11.2／17.1%（起始 3ATR/ep：7.0／12.3／18.3%） | 7 | 6（0.36%） | 99.7% | 4／19／54 |
| M20 state | 0 | 5.8／12.4／20.4%（MA(訊號日)：6.2／13.3／21.8%） | 10 | 21（1.24%） | 100% | 3／13／31 |
| M20 cross_sig | 0（5 條進場時無線） | 5.8／12.4／20.4% | 5 | 16（0.95%） | 100% | 4／13／31 |
| M20 cross_entry | 0（21 條進場時無線） | 6.0／12.5／20.5% | 2 | 0 | 100% | 4／13／31 |
| M50 state | 0 | 8.5／18.1／30.6%（MA(訊號日)：8.8／18.5／31.6%） | 3 | 9（0.53%） | 98.5% | 10／30／67 |
| M50 cross_sig | 0（4 條無線） | 8.6／18.1／30.6% | 0 | 5（0.30%） | 98.5% | 10／30／67 |
| M50 cross_entry | 0（9 條無線） | 8.6／18.1／30.6% | 0 | 0 | 98.5% | 10／30／67 |
| F | 0（全部碎形；3×ATR 代用 0 條） | 7.8／16.3／28.6% | 6 | 13（0.77%） | 100% | 6／17／42 |

- 均線族：訊號日收盤已在均線下 M20 5 列、M50 4 列；進場日收盤在均線下 M20 21 列、M50 9 列 ⇒ arm 的三種讀法只差這 5～21 列。
- 「觸發距進場根數」＝ ⓔ 要的「停損當天距進場幾根」的價格層版本（0＝進場日收盤就破）；引擎版還會受「有沒有進場」影響，判定時照報。
- 有效 K 棒兩種定義（traded vs 收盤有限）在這 840 檔上 0 根不同。

## 四、時停九格：價格層會被觸發的部位占比（1,688 條）

| | d＝20 | d＝40 | d＝60 |
|---|---|---|---|
| R0（收盤 ≤ ep） | 52.7%（889） | 48.9%（825） | 48.6%（820） |
| R10（收盤 ＜ 1.1 ep） | 73.3%（1,237） | 66.1%（1,116） | 62.9%（1,061） |
| NH（窗內沒創新高） | 16.1%（271） | 56.7%（957） | 64.6%（1,091） |

全部 1,688 條第 d 根都在排程出場之前（狀態 ok）。

## 五、ⓓ 補股池放寬（逐日池）

- 重建驗證：不去重 S 114,977 列 → 套回 20 根去重 ⇒ 與 signals_S.csv.gz 24,277 列逐列逐值相同；不去重 AND 15,292 列 → 與 and_signals.csv.gz 2,339 列相同；逐日池 13,370 列／929 檔 ∩ 去重鍵 ＝ setup_t1 的 2,036 列（逐值相同）
- 可用池列（xpos_H120 ≥ 0）10,312 列；池列 xpos＜0 3,058 列
- 逐日池大小（未排除持股）：窗內 2,313 個交易日 p10／中位／p90 ＝ 0／2／11（均 4.46）；v1 有訊號的 886 天 ＝ 2／7／17（均 8.78）
- v1 當日訊號池：有訊號日 1／1／3（均 1.91）
- 比例：有訊號日逐日「池／訊號」p10／中位／p90 ＝ 1.67／4.0／10.0；總列數比 6.11
- 池有候選的日子 1,606 天，其中 720 天 v1 沒有訊號
- 池列建線：六族（M 只建 state）＋時停九格 10,312 條全部建成、0 略過
- ⚠ setup_t1 的 closes／opens 只載 v1 訊號的 840 檔 ⇒ 池多 64 檔沒價；ⓓ 判定時 ctx 要用 rerun17.load_prices 補載
- ⚠ 引擎只有一個 sig：現在的 stop_proceeds 補股跟一般新部位用同一個池 ⇒ ⓓ「只有補股用放寬池、一般新部位仍用 v1 訊號」要引擎另加一個補股池參數（本件沒動引擎）

## 六、後續判定給 simulate_mtm 的參數（⛔ 本件未跑）

`listexit_lines.sim(ctx, kw, r)`（H120、10 槽、default_rng(1000＋r)），kw ＝

| 格 | stop_line | stop_line_le | 其他 |
|---|---|---|---|
| P15 | `Y.p15_lines(sig, opens, closes)` | True | stop_proceeds="next" |
| T20 | `Y.t20_lines(sig, closes, valid_of)` | True | 同上 |
| A3 | `Y.a3_lines(sig, opens, closes, bars_of)[0]` | False | 同上 |
| M20／M50 | `Y.ma_lines(sig, closes, bars_of, 20／50, arm=待裁定)` | False | 同上 |
| F | `Y.f_lines(sig, bars_of)[0]` | False | 同上 |
| 時停 R0-d | `Y.time_lines(sig, opens, closes, bars_of, "R0", d)[0]` | True | 同上 |
| 時停 R10-d／NH-d | 同上 "R10"／"NH" | False | 同上 |

`Y.sim_kw(fam, lines)`／`Y.time_kw(kind, lines)` 直接回上表的 kw。bars_of ＝ research11.load_bars（有快取）；valid_of ＝ 由 bars_of 的 idx 做布林。全部 sig 列都要建線（補進來的部位按自己的 (sid, entry_pos) 查線）。
