# USREG-M pre 段｜頻率盤點＋可判定性算術＋讀法選項

**台北時戳**：2026-09-27 00:2x　**寫的人**：回測線（計算子代理）
**判準**：美股登錄 seq2（a3c98a07e882a574，正文）＋seq3（81bfc48eff6258f5）＋seq4（cbfc7610923b9e53）＋seq5（ffb22ecb1bd749ff）；移植來源 台股 PREREGM seq1（9316840579a8d33a）
**程式**：`backtest/researchUSM.py`（偵測器 `backtest/trendline_m.py` 台股同一支、一行未改）
**資料**：us-stock-data `0043f97d974df78ffe5626623aed2caecfd4a5b8`（窗到 2026-08-31 ≤ 2026-09-22 ⇒ 照 seq5 §三 不改釘）；轉接層 46e74a0c8b

> ⛔ 全程沒有算、印或存任何報酬（沒讀 T+1 以後的價格做比值；沒算 R_e／X／基準／勝率）。
> ⛔ 本資料夾只有彙總數字；逐筆事件與逐檔檔案在 `~/us_work/usm/`（repo 外），sha 見 `pre_work_sha.csv`。

---

## 一、閘門

| 閘 | 結果 |
|---|---|
| us_data selftest（`selftest_us_data.py`，輸出改寫到 repo 外、⛔ 沒動 resultsUS/） | **PASS 31｜WARN 3｜FAIL 0**；checks 的 id／status 與已提交版逐列相同；fingerprint 逐位元相同（資料沒變） |
| 台股 fixture F1～F8（`selftest_trendline_m.run_all()`） | **8／8 過**（F7 3,848 次前視比對全同；F8 破壞版 320 次抓到 147 次差異、正式版 0 次） |
| 美股資料型態 fixture UF1～UF8（`researchUSM.usm_fixtures()`） | **8／8 過**（見下） |

```
UF1 NYSE 日曆（跨 5 個平日休市）＋浮點還原價：甲乙丙 都抓在同一根；整條序列等比例縮放 ⇒ 事件逐筆相同（線對價格一次齊次）
UF2 無效 K 棒（high < 收盤）⇒ 當作沒有 K 棒、計數 1；持有期缺 1 天 ⇒ S0 保留、S1 旗標亮
UF3 轉接層斷點（seam／split_div）在 [最早取點, T+21] 內 ⇒ 剔除；邊界 T+21 算、T+22 不算；最早取點之前不算
UF4 連缺 5 日 ⇒ 硬斷點；連缺 4 日 ⇒ 不是；最後一根之後沒資料（下市）⇒ 不算缺日、記持有期內下市
UF5 T+1 沒有 K 棒 ⇒ 剔除_T+1停牌（T 之後全無資料另記已下市）
UF6 T 當天 member＝0 ⇒ 不成事件；持有期內移出 ⇒ 保留、記 R-移出；窗首之前 ⇒ 不成事件
UF7 狀態機（台股 Q2 順序）：合併 20 日、被剔除者不開合併窗、純合併另計
UF8 ⭐ 真實美股還原價 12 檔 × 5 格 × 15 個日子：截斷到 d、或把 d 以後換成別檔價格 ⇒ T ≤ d 的事件 1,800 次比對全同（該批事件 1,303 筆）；
    破壞版（樞紐提早一根確認）同一比法抓到 116 次差異、正式版 0 次 ⇒ 比法分得出來、不是假綠
```

## 二、母體與資料（必報）

```
判定窗 2016-01-04～2026-08-31：2,680 個 NYSE 交易日；T 可落 2016-01-04～2026-07-31（T+21 ≤ 窗尾）：2,659 日；每年 251.54 交易日
有 OHLC 712 檔（全部可用）；無 OHLC 35 檔 ⇒ 不產生事件（seq2 §一）
窗內在指數股-日 1,351,751；其中 35 檔 21,498 ＝ 1.59%  ⇒ 結論句附「缺價的 35 檔不在事件母體，結果偏向存活股」（seq3 §一①）
無效 K 棒（seq5 ⑤）：1 根／1 檔（＝ UA 2021-05-05；scope＝panel 下沒有別根）
轉接層硬斷點（scope＝panel，全期）：3 根（IR seam、DHR、XRX split_div）
連缺 ≥ 5 交易日（首末 K 棒之間，窗首～窗尾＋21）：7 檔；在指數期間的只有 CVC（seq5 ⑤）；其餘 6 檔都在【移出指數之後】（多為被併購後零星報價）⇒ 只影響 R-移出 那一種讀法
不在 NYSE 日曆上的 K 棒：66 根（全是 2026-09-22，窗外；轉接層 §五②）
暖身：面板最早 2015-12-01 ⇒ 期初就在指數的檔只有 22 根暖身（資料事實）；最早保留事件 甲 2016-01、乙 2016-01、丙 2016-02
```

## 三、頻率表（合併後、剔除後；⛔ 不看報酬）

| 格 | 窗內偵測 | 其中不在母體 | 母體內原始 | 合併掉 | 剔除（硬斷點／T+1停牌） | **保留** | 相異檔 | 每檔每年 平均／中位／p90（在指數日分母，曝露 ≥ 1 年 663 檔） | 合併比率（事件／在指數股票年） | 零事件檔 |
|---|---:|---:|---:|---:|---|---:|---:|---|---:|---:|
| **(甲) R5** | 22,667 | 2,632 | 20,035 | 2,666 | 2／1 | **17,366** | 691 | 3.34／3.31／3.97 | 3.32 | 0.0% |
| **(乙) R5** | 2,536 | 255 | 2,281 | 54 | 0／0 | **2,227** | 592 | 0.41／0.38／0.76 | 0.43 | 11.0% |
| **(丙)** | 14,063 | 1,680 | 12,383 | 5,238 | 4／0 | **7,141** | 677 | 1.44／1.35／2.08 | 1.36 | 0.3% |
| (甲) R3 描述 | 36,007 | 4,227 | 31,780 | 7,393 | 3／1 | 24,383 | 703 | 4.67／4.64／5.30 | 4.66 | 0.0% |
| (乙) R3 描述 | 4,625 | 458 | 4,167 | 149 | 0／0 | 4,018 | 649 | 0.75／0.76／1.14 | 0.77 | 3.5% |
| (甲) R10 描述 | 11,513 | 1,333 | 10,180 | 560 | 1／0 | 9,619 | 675 | 1.86／1.84／2.38 | 1.84 | 0.0% |
| (乙) R10 描述 | 978 | 97 | 881 | 4 | 1／0 | 876 | 468 | 0.17／0.15／0.38 | 0.17 | 29.4% |

```
在指數股票年合計 5,237.0；台股 Q6 式（首末 K 棒跨距分母，693 檔）並報：甲 2.83／3.04／3.69｜乙 0.36／0.33／0.67｜丙 1.18／1.17／1.79
純合併（不看剔除）：甲 17,369｜乙 2,227｜丙 7,143
逐年保留：甲 1,555～1,760／年（2026 到 7 月 987）｜乙 175～248｜丙 467～1,057（2018、2022、2023、2025 偏高）
T+1 停牌的 1 筆（甲）＝ T 之後就沒資料（已下市）
線的帳（全期）：甲 R5 樞紐 91,438、成立 36,008 條：被突破 22,962、被新樞紐取代 12,767、撐到 60 日失效 7
對照台股（每檔每年平均）：甲 3.07 → 美 3.34｜乙 0.37 → 0.41｜丙 1.57 → 1.44（描述）
```

## 四、可判定性算術（出口上限）

| 格 | 保留事件 | 有事件的 20 日區段 | 區段上限 | n_eff | 最好能到 | 本資料實際 |
|---|---:|---:|---:|---:|---|---|
| (甲) | 17,366 | 133 | 133 | 133 | 出口③ | 出口③ |
| (乙) | 2,227 | 133 | 133 | 133 | 出口③ | 出口③ |
| (丙) | 7,141 | 132 | 133 | 132 | 出口③ | 出口③ |

```
區段：從窗首每 20 個交易日切，區段號 ＝ (T − 窗首)//20；T 可落 2,659 日 ⇒ 最多 133 段（最後一段 19 日；台股同式 115 段）
⇒ 三格都在出口③ 範圍（事件數遠大於 100、區段 132～133）；描述臂 R3／R10 也都 ≥ 100
描述臂 c（抱 60 日）：T+61 ≤ 窗尾 ⇒ 60 日區段最多 44 段 ⇒ 依構造最好只到出口②（描述臂，⛔ 不判）
⚠ 下面任何一種待定讀法，最多只動保留件數 0～59 筆（見 §五），⛔ 不會改出口
```

## 五、讀法

### 5-1 登錄已寫、本支照寫

```
日曆 NYSE（yahoo_GSPC）｜還原 OHLC（Yahoo × adjclose÷close；Tiingo adj*）｜成本 0.05% 來回、敏感度 0.02%／0.10%（本段不用）
漲跌停不存在 ⇒ 台股「T+1 開盤漲停剔除」拿掉；LULD、熔斷不建模｜停牌＝面板無列 ⇒ T+1 無 K 棒 ⇒ 剔除
事件母體＝T 當天 in_index＝1 且有 K 棒｜暖身 scope＝panel（seq5 ④）｜UA 那根＝無效 K 棒（seq5 ⑤）｜CVC 照 ≥5 日硬斷點
同日拆股＋配息、IR 接縫 ⇒ 硬斷點｜35 檔不產生事件｜下市＝最後成交價了結（計數：持有期內下市 甲 21／乙 2／丙 5）
合併與剔除順序、M1～M8、Q1～Q8 ＝ 台股 PREREGM 裁定 seq156 已裁的讀法，照搬
```

### 5-2 ⚠ 登錄沒寫、影響事件集合（本段兩邊都數了，⛔ 沒選；狀態欄暫用第一個選項）

| 代號 | 問題 | 選項 | 保留件數的差（甲／乙／丙） |
|---|---|---|---|
| R-合併 | 窗內、但 T 當天不在指數的偵測事件，要不要開 20 日合併窗 | ① 不是事件、不開窗（同台股 Q1 先濾窗外）② 也開窗 | ② 會 −7／0／−2 |
| R-跳躍 | seq1「還原因子解釋不了的跳躍」美股怎麼認（美股沒有除權息事件表可以拿來「解釋」） | P0 只認轉接層的 seam／split_div ＋ ≥5 日空缺　P1 另加台股 Q4 同式：相鄰收盤比 ≤ 0.55 或 ≥ 1.8 全算 | P1 會 −0／0／−5（R3 −3）；⚠ 窗內 26 根／15 檔，看日期多是真實暴跌暴漲（2020 能源、2021 迷因股），⛔ 不像資料錯 |
| R-停牌 | seq1 把「停牌（面板該日無列）」列為硬斷點：是只管 T+1，還是 [最早取點, T+21] 內任一天 | S0 只有 T+1 無 K 棒 ⇒ 剔除（台股同形）　S1 範圍內任一天缺 ⇒ 硬斷點 | S1 會 −1／0／0（R3 −3） |
| R-移出 | 持有期 (T, T+21] 內被移出指數 | E0 照常抱到 T+21（scope＝panel 有移出後價格）　E1 移出視同下市、在指數最後一天收盤了結　E2 剔除 | 受影響 59／5／38 筆（其中移出後到 T+21 仍有 K 棒 38／3／33）；⚠ 移出後的價格有幾檔是被併購後的零星報價 |

另報（描述，⛔ 不是選項）：若只用 scope＝covered（舊預設、無暖身、無移出後價格）⇒ 保留 甲 17,065／乙 2,190／丙 7,077；
事件的最早取點用到入指數前的暖身：期中加入 92／14／58、期初已在且用到 2015-12 那 22 根 216／21／12。

### 5-3 ⚠ 登錄沒寫、本體要用（開本體前要定；⛔ 本支沒選）

```
B-基準  X_e 的基準（台股 §四 ＝ 同段 gate3 母體等權）：
        ① T+1 當天 in_index 且有 K 棒的全部股票等權 [open(T+1), open(T+21)]（台股同形；移出／下市者照 R-移出、下市規則）
        ② ^SP500TR 同段（seq1 市場層表把「基準 0050 → ^SP500TR」，但那一列是組合層門檻用的）
B-出場  px(T+21) 沒有 K 棒時：台股 B1 退回「≤ T+21 最後一根收盤」；美股同一條，但與 R-移出 綁在一起
B-分組  §六「上市／上櫃」必報：0043f97 沒有交易所欄 ⇒ ① 拿掉　② 換成別的二分（例：期初已在指數／期中加入；現仍在指數／已移出）
B-假訊號 seq4：同檔、全窗、只排除「過去 20 個交易日內有同檔同訊號真事件」的日子；沒寫死的四處：
        ① 真事件＝保留事件 或 母體內原始偵測事件
        ② 過去 20 日＝[T−20, T]（含當天）或 [T−20, T−1]
        ③ 每檔抽幾筆＝該檔保留事件數（全窗）；可抽日＝在指數、有 K 棒、T ≤ 窗尾−21
        ④ 抽到的日子要不要再套 20 日合併
        先例（⛔ 不代表本件）：台股 researchAvg A11、researchPatAll C8 取「保留事件、[T−20, T]、不合併」
        「不排除」版、台股原「同檔同月」版並列描述（seq2 §二①、seq4 §一）
B-控制組 描述臂 d 的十分位母體：in_index 當日（seq1 §二 對 X 寫了；M 沒寫）
B-預扣稅 seq1「絕對報酬另列扣 30% 預扣稅版」：M 是相對量 X ⇒ 要不要另列 R_e 的扣稅版
B-承接  台股 PREREGM 本體的 B1～B15（只有 B10、丙鄰格曾請台股確認）要不要整套照搬
```

## 六、檔案

```
backtest/researchUSM.py              pre 段程式（fixture UF1～UF8 在同一支）；--fixtures-only 只跑閘
backtest/resultsUSM/pre_freq.json    彙總（計數、分位數、區段數、出口、fixture 字串、sha）
backtest/resultsUSM/pre_work_sha.csv repo 外逐筆檔的列數與 sha256
backtest/resultsUSM/PRE_REPORT.md    本件
~/us_work/usm/（repo 外，⛔ 不入庫）events_{甲,乙,丙,甲_R3,乙_R3,甲_R10,乙_R10}.csv、per_ticker.csv、run.log、fixtures.log、selftest_us/
⛔ 未 commit、未 push、未派 workflow；⛔ 本體未跑
```

— 回測線（計算子代理）
