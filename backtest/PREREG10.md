# PREREG10：研究十三 兩個站得住的訊號疊起來（G1 月營收創高 × 3/5 相似度計分）＋ 大盤狀態濾網 ＋ 組合層對 0050

事前登錄，2026-09-13 00:27（台北），跑前 commit。使用者 09-13 00:10「那就試試看」（把 G1 與 3/5 疊起來做組合層）。程式 `backtest/research13.py`，輸出 `backtest/results13/`。

## 〇、要回答的問題（一句）

兩個各自對股票日母體有超額的訊號（研究三 G1、研究十一 3/5），疊在一起、放進固定槽位的組合，**在年化與最大回落兩項同時**贏得過同窗買 0050 不動嗎？

## 一、輸入（不重算訊號）

- **S ＝ 3/5 訊號**：`results11/signals.csv.gz` 主格 24,286 筆，逐筆的 H60／H120／LD 出場照用（研究十一引擎）。
- **G ＝ G1 訊號**：`results3/panel.csv.gz` 裡 `rev_hi24 == True` 的列，8,827 筆／1,822 檔／2015-12～2026-06 期。訊號日 ＝ 面板 `signal_pos`（M+1 月 10 日後第一個交易日）。⚠ 出場**改用研究十一的引擎重算**（有效 K 棒序列、次一有效 K 棒開盤進、H60／H120 收盤出、LD 第一根跌停次開盤出、上限 120；訊號前 20 根與持有期內有壞根整筆剔除），與 S 同一把尺。訊號日若不是有效 K 棒，取之後第一根有效 K 棒為訊號根。
- **AND ＝ S 內「訊號日當下最新一期月營收創 24 月新高」**：對每筆 S，取該檔面板裡 `signal_pos ≤ pos` 且最近的一列（距離 ≤ 45 個交易日，否則視為無營收資料、不算），`rev_hi24` 為真者。
- **AND-60（變數）**：S 內「前 60 根有效 K 棒內有 G1 訊號日」。
- **S∖AND**：S 減 AND（補集，一定報）。
- **OR**：組合層才有意義——S 與 G 兩條訊號流同時餵同一組槽位，不分先後、隨機挑。
- 母體基準：沿用 `results11/baseline_months.csv`（同月同持有期、無閘門股票日）做月配對超額。LD 無基準。

## 二、逐筆層

每個集合 × H60／H120／LD 報：n、平均、中位、勝率、最壞、p10、超額 月配對 95% CI（H60／H120）、A／B 兩段（2021-01 切）。判定用語同 PREREG9 第三節。

**安慰劑（K線分析 09-09 裁定常設）**：AND 是 S 的子集，子集的超額比母集高可能只是抽樣。在 S 內隨機抽同樣筆數 200 次，報 H120 超額的分佈（p5／p50／p95）與**真 AND 落在第幾百分位**。⛔ AND 超額比 S 高、但落在安慰劑 p95 之內，就只能寫「測不出疊加效果」。

## 三、大盤狀態濾網（變數，只擋進場、不強制出場）

進場日 0050 還原收盤 > 其 200 根日曆位置移動平均（ffill 後）才進；已持有的照規則出。⛔ 只有這一條、只有 200 這個參數，不掃。

## 四、組合層

集合 {S, G, AND, OR} × N ∈ {10, 20} × 規則 {H60, H120, LD} × 濾網 {無, 有} × 200 種子；逐日收盤市值權益（研究十一 `simulate_mtm`），資金 1、現金報酬 0、訊號多於空槽時隨機挑、同檔不重複持有、成本 0.585%／筆、年化 245 根。

每格報：年化 中位與 p10～p90、最大回落 中位與 p10～p90、年化 ÷ |最大回落|（Calmar）中位、槽位使用率、交易筆數；另報 A 窗（模擬起點～2020-12）與 B 窗（2021-01～終點）各自的年化與最大回落中位。

**對照 ＝ 0050 還原價買進持有**，同窗、同 A／B 窗。

**事前判準（一格「贏過 0050」）**：年化中位 ≥ 0050 年化 **且** 最大回落中位比 0050 淺，**且** A、B 兩窗各自也都成立。三條缺一就是「沒贏」。⛔ 不挑格：48 格全報，贏的格數要跟總格數一起寫。⛔ 種子 p10 年化 < 0 的格，即使中位贏也標「不穩」。

## 五、事前預期

- AND 逐筆 H120 超額高於 S（點估計），但 n 會小很多（G1 每檔每月最多一筆、營收創高在 3/5 裡不是多數），安慰劑很可能包住它。
- 濾網會把最大回落縮 5～15 pp、年化縮 2～5 pp（2020-03、2022 大半年空手）。
- 48 格裡「三條同時成立」預期 **0～3 格**；若 0 格，結論就是「這兩個訊號疊起來也贏不過 0050，這十年」。
- OR 的槽位使用率最高、年化接近 S、回落接近 S。

## 六、不做的事

不加第三個訊號、不掃濾網參數、不做分數排序、不調 N 以外的槽位規則、不改 S 與 G 的定義、不重算 S 的出場。跑完只追加，不改本文。

## 追加一（2026-09-13 00:50，跑完之後寫；本文未改）

1. 事前判準三條同時成立：**0／48**。細表另報「只看全窗兩項」2／48（都是 AND），與各分項格數，⛔ 那不是判準，是說明輸在哪一窗。
2. 面板 `rev_hi24` 有 NaN（沒有 24 期歷史的列），程式視為 False；8,827 筆裡 8,818 筆算得出出場（9 筆訊號日後 5 個交易日內沒有有效 K 棒或落在壞根窗）。
3. `results3/panel.csv.gz` 是 09-09 那一版（500 張閘門、修 F1 前的資料）；09-11 在修好的資料上重跑過、結論不變，但面板檔沒有替換。⇒ G 集合帶著那個閘門，S 沒有。AND 因此偏向流動性較好的股票，這一點在本文一節沒寫清楚。
4. 事前預期核對：AND 超額高於 S ✓、安慰劑包住它 ✗（真值第 100 百分位，超出）、濾網縮回落 5 ~ 15 pp ✗（0 ~ 9 pp，G 反而加深）、48 格贏 0 ~ 3 格 ✓（0）、OR 接近 S ✓。
5. 0050 對照的 A 窗是 2016-01-12 ～ 2020-12-31、B 窗 2021-01-04 ～ 2026-09-11；組合的 A／B 窗年化是同一條權益曲線切段算的，不是各自從 1 起跑。
