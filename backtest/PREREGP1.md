# PREREGP1（落地版）：組合層——存量槽數下探（N＝2／3／8）與「每日最多新增幾檔」（d）的成本

**登錄者：策略線**（信箱全文 `PREREGP1-組合層_存量槽數下探與每日新增上限_20260914-0652（全文・回測線執行）.md`，Drive `11xoRFJvv1lPzO0UjfVjQcewmUCimM-ds`，14,196 B；現行版是專案文件 pw1 seq=2 sha `39cb3b8c029bb2d6`，本線讀不到，以信箱副本為準）。
**過目：K線分析 09-14 0715**——有條件通過，三件補完可執行（1-A ①②③）、三件建議（1-B ④⑤⑥）。
**落地與執行：回測線**（本檔＋`researchp1.py`＋`resultsp1/`）。分工＝使用者 09-14 06:5x 裁定「第一種」。⛔ 尺不動：門檻、判定用語、SE、安慰劑、離散度照 `tw-backtest-methodology`。
**本檔只把策略線的登錄翻成機器定義**；凡是登錄沒寫、由本線補的，逐條標「回測線定的」。⛔ 跑前 commit；跑完只追加。本題**沒有任何數字產生過**（登錄一節列的既有結果是別的 N）。

## 〇、K線分析 0715 三件補完（1-A）——本線在落地版怎麼補

| 0715 | 落地 |
|---|---|
| ① 股息口徑 | **兩端一律還原價（含息）**——事實（本線 0749 一節）：策略端逐筆 `g_H*` 與組合層市值都用 `data.load_stock` 的還原 open／close，基準 0050 同一個函式。回歸格 R0：隨機抽 5 檔各一個「息」事件日，斷言 還原 close 前後變動率 ＝ 原始 close 變動率 × 因子（`data/adj` 的 `factor`） |
| ② 權重 | **等權**：`slot = equity[t−1] / N`，每個新部位 `min(slot, cash)`（`research11.simulate_mtm` 現行行為，一字不改）。七節③那句改成「等權假設下，要滿足 20% 單一部位上限，存量必須 ≥ 5」 |
| ③ 擋掉組分層 | 擋掉組按原因 **(a) 槽滿／(b) 當日新增達 d／(c) 該 sid 已在持倉** 三層分報；否證條件①改成「**排除 (c) 之後** null 下擋掉組仍測得出優於進場組 ⇒ 才判引擎有偏誤」 |
| ④ 格數 | 4-A 與 4-B 的 `N=8 × d=∞ × 2 rule` 是同兩格 ⇒ 主格 **22 格、虛無期望 1.1** |
| ⑤ 符號 | 存量槽數＝`N`；交易筆數＝`m`（4-D「做 m 筆」） |
| ⑥ 槽位使用率 | ＝ Σ每日持有部位數 ÷（模擬天數 × N）（`simulate_mtm` 的 `slot_use`，與 13b 的 82%／72% 同一算法） |

## 一、資料（⛔ 不新造訊號）

- AND ＝ `results13b/and_signals.csv.gz`（2,199 筆；欄 sid,k,pos,entry_pos,month,g_H60,g_H120,g_LD,xpos_*）。⚠ 沒有 H20 ⇒ 4-C 的 H20 欄由本線用同一引擎補算（`research11.load_bars` ＋ `fixed_exit(o,c,k,20,nb)`，`nb` 取法與 research13 `g_exits` 相同）——**回測線定的**。
- S ＝ `results11/signals.csv.gz`（24,286 筆，含 g_H20／H60／H120 與 xpos）。
- 價格：兩集合所有 sid 的還原 close（ffill）／open，`D.load_stock` 重建（與 research13 同法），存 `resultsp1/regress/{closes,opens}.npz`。⚠ 用的是 **今天的 `data/adj`**（main 09-13 19:34 UTC 版）；research11 當年只存了 closes.npz 沒存 opens ⇒ 研究十一那 12 格只能做「容忍價格改版」的對照（±1 pp），逐種子精確重現的對象是本線改引擎**之前**存下的基線（二節 R1）。
- 基準：0050 還原 close（ffill）同窗；成本 0.00585；窗切點 `research13.SPLIT`＝2021-01；A 窗／B 窗＝`research13.window_stats`。
- 種子：**主格 `np.random.default_rng(7000 + r)`，r ∈ [0, 200)**；回歸用 3000+r（S）／1000+r（AND），與原研究相同。

## 二、引擎（`research11.simulate_mtm` 加參數，⛔ 預設值下行為與現行**逐位元相同**）

新參數（全部有預設，預設＝現行）：
- `d_max`（None＝∞）：同一個 t 最多新增 d 個部位。
- `pick`（None＝現行 `rng.permutation`）：`"relvol"` ＝ 候選依 **相對量能** 遞減排序（訊號日成交金額 ÷ 該檔前 60 個交易日成交金額中位數；由 `load_bars` 的 `amt` 算，前 60 根不足 ⇒ NaN 排最後）。⛔ 不用 `liq`（那是前 20 日均值）。
- `log`（None＝不記）：list，逐筆記錄每一天每一個候選訊號的去向：`in`（進場，含推遲天數）／`a`／`b`／`c`／`b_expired`。
- `queue_days`（0＝不排隊）：被 (b) 擋掉的訊號留在隊列最多 Q 個交易日，之後每天再試（仍受槽與 d 限制）；進場價＝**實際進場日**的還原 open、出場日**不變**（原 `xpos`，持有變短）、gross 重算＝`closes[exit]/open[t′] − 1`；Q 天內沒進到 ⇒ `b_expired`。**Q＝5，回測線定的**（登錄 4-B 要「推遲的中位天數」，沒有隊列就沒有推遲；5 ＝ 一週）。⚠ (a) 槽滿的不排隊（現行行為：當天沒進就丟）。
- 擋掉原因的判定（回測線定的）：`slots_free = N − 持倉數`、`d_free = d − 今日已新增`；候選數 > `min(slots_free, d_free)` 時，多出來的標 **a**（若 `slots_free ≤ d_free`）或 **b**（否則）；cash 用盡視同 a。
- 輸出多兩個鍵：`m`（＝trades）、`pos_frac`（完成交易裡 `gross − COST > 0` 的比例）→ 4-D。

**回歸（⛔ 主格之前先過，任一格不過就停）**：
- **R1** 改引擎前用現行程式存基線 `resultsp1/regress/baseline_S.npz`（S、N∈{5,10,20}、H20/H60/H120/LD、3000+r）與 `baseline_AND.npz`（AND、N∈{10,20,30,40}、H60/H120/LD、1000+r）；改完後預設參數重跑 ⇒ 逐種子逐格 cagr／mdd／slot／trades **完全相等**。
- **R2** AND N∈{10,20} × H60/H120/LD 中位對 `results13_adj0914/portfolio.csv`（同一天、同 adj、同種子 1000+r）**完全相等**；對 `results13b/portfolio.csv` 與 `results11/summary.md` 七節 ±1 pp 內（價格改版容忍）。
- **R3** 合成序列：每天 10 個訊號、N＝8、d＝1 ⇒ 每天新增 ≤ 1；d＝∞ ⇒ 第一天新增 8；`queue_days=5` 時被 b 擋的在 5 天內進場且 `in` 的推遲天數 ∈ [1,5]。
- **R0** 股息口徑（〇節①）。

## 三、要測什麼（照登錄四節，逐字）

- **4-A 存量曲線（描述）**：N ∈ {2,3,5,8,10,20,30,40} × rule ∈ {null, R1} × d＝∞ × H60；報年化中位、p10～p90、最大回落中位、p10～p90、槽位使用率、m；對 0050 三條判準（全窗＋A 窗＋B 窗，年化不低於**且**回落較淺）⇒ wins／total。事前判準：p90−p10 是否仍縮得比 1/√N 快（既有 N＝5／10／20：20.1／13.1／8.1 pp）；偏離方向不一致 ⇒ 抽樣雜訊，不編機制故事。
- **4-B 流量成本（描述）**：N＝8 × d ∈ {1,2,3,∞} × rule × H60；另報因 d 被推遲的訊號數與推遲中位天數（隊列進場的）、`b_expired` 數。
- **4-C 核心測量（檢定）**：每個 (N, d, rule) 組態：進場組 vs 擋掉組（按 a/b/c 分層＋合計），H20／H60／H120 平均、中位、差值、月分群 95% CI、n。⚠ H60／H120 月分群 CI 偏窄，表頭標。**組態的代表種子＝200 種子裡年化為中位數的那一個**（逐筆歸屬隨種子變；回測線定的），並附「差值在 200 種子上的 p10／p50／p90」當穩健性。
- **4-D 做 m 筆的真實機率（副產品）**：逐種子的 m 與 `pos_frac`，與獨立重抽上界「20 筆 ~89%」並列。
- 主格＝AND × H60 × 22 格（4-A 16 ＋ 4-B 8 − 重複 2）；虛無期望 1.1；敏感度（分開計）：H20／H120、S 集合。

## 四、先驗（策略線登錄五節，逐字抄；⛔ 跑完不改）

① N 40→2 年化中位下降、離散度放大（N＝3 約 25～30 pp、N＝2 約 30～35 pp）；② N ≤ 8 沒有一格過三條判準；③ d ∞→1 年化中位與槽位使用率下降，但降幅 < 把 N 從 8 降到 3；④ 擋掉組 vs 進場組：null 下 CI 含 0（引擎 sanity）、R1 下也含 0；⑤ 做 m 筆真實機率低於 89%，N＝3 時押 70～85%。
回測線的事前預期（本線補）：小 N 下 (c)「已持有」那層會占擋掉組的大宗（訊號同一檔常連續出現），排除 (c) 之後 a／b 兩層 n 會小很多；R1 與 null 的存量曲線差 CI 含 0；隊列讓 d＝1 的 `b_expired` 在 AND 上 < 10%（AND 每月中位 14 筆、槽 8，擋得少）。

## 五、否證條件（策略線六節逐字，加 K線分析 ③）

① null 下、**排除 (c) 後**擋掉組仍顯著優於進場組 ⇒ 引擎有偏誤，先查引擎、不寫成結論；② R1 − null 存量曲線差 CI 全含 0 ⇒ 只能寫「挑選規則測不出差異」；③ N ≤ 8 有一格過三條判準 ⇒ 反駁先驗②，據實報；④ 22 格只測得出 1 格 ⇒ 等於雜訊期望，判「測不出」。
判定用語：統計層 測得出／測不出；可執行層 扣成本後剩 X pp／不可執行；「通過／顯著／有效」停用。零／n_min／出口照 K線分析 0715 三節：「零」＝主 CI 含 0 ∧ |點估計| ≤ 0.585%；n_min＝月分群有效月數 ≥ 24（每格附有效月數）。

## 六、範圍限制與結論第一句

照策略線七、八節逐字：訊號集是 AND 與 S，不是使用者每天的流程；N＝30／40 與 N＝2／3 只走統計層；類股集中度、單一部位上限不在本份；不測進場訊號好不好；成本只有 0.585%、沒含滑價。結論第一句＝組合層對 0050 的比較（格式照八節）。⛔ 新參數不送情報分析線，連同誤差帶與樣本數報使用者裁。

## 七、輸出

`resultsp1/portfolio.csv`（每格中位與 p10/p90）、`resultsp1/blocked_<set>_<N>_<d>_<rule>.csv.gz`（代表種子逐筆去向）、`resultsp1/summary.md`；`resultsp1/CONCLUSIONS.md` 由**策略線**寫（信箱寄來、本線 commit）。

## 追加一（2026-09-14 09:41，跑完之後寫；本文未改）

1. **程序**：回歸 R0～R3 全過（1,389 秒；R1 逐位元、R2 對 `results13_adj0914` 相等；紀錄 `resultsp1/regress/regress_20260914.log`）。主格 09:05 開跑、1,713 秒（AND＋S 各 22 組態 × 200 種子；`resultsp1/summary.md`、`portfolio.csv`、`blocked_*.csv.gz` 代表種子逐筆去向）。⚠ 策略線 0805 的 seq=3 六件訂正我 09:08 才讀到（開跑後 3 分鐘）；逐字比對與本落地版〇節六件**內容相同**，⛔ 沒有重跑。隊列 Q＝5 仍是回測線定的（策略線沒訂）。adj 缺檔（2-1 ①要求印）：AND 7／2,199 筆（0.3%）、S 241／24,286（1.0%）。
2. **主格（AND × H60 × 4-C 進場組 − 擋掉組(ex c)）：22 格、測得出 0 格、雜訊期望 1.1 ⇒ 測不出。** 點估計 N ≤ 10 為正（+0.7～+12.5 pp）、N ≥ 20 在 ±2 pp 內；CI 全含 0。⚠ N＝2 擋掉組有效月數 18 < n_min 24 ⇒ 那兩格是「還沒測」不是「測不出」。敏感度：H120 0／22 測得出；**H20 8／22 測得出，全部是 in > 擋掉、全在 N ≤ 8**（N2 null/relvol、N3 relvol、N5 null/relvol、N8 d1/d3/∞ null）；S 集合 H20／H60／H120 三個持有期 0／66 測得出。
3. **否證①**：null 下排除 (c) 之後，擋掉組**沒有**測得出優於進場組（H60 全含 0）⇒ 沒有引擎偏誤的跡象。⚠ 第 2 點那 8 格 H20 是反方向（in > 擋掉）：槽滿 (a) 只在訊號密集日發生，而密集日之後 20 根整體較差 ⇒ 那是**時點的選擇效應**（方法論第三十二條同族），⛔ 不是規則挑得好、也不是引擎偏誤；H60／H120 消失。
4. **4-A（描述）**：AND N ≤ 8 沒有一格贏 0050（先驗② ✓）；贏 3／16：N30 null、N40 null、N40 relvol（只走統計層）。離散度 p90−p10：N 2／3／5／8／10／20／30／40 ＝ 21.1／16.5／9.6／7.9／8.0／5.9／3.3／2.7 pp，×√N ＝ 30／29／21／22／25／26／18／17 ⇒ 非單調、判抽樣雜訊；S 集合 ×√N 41～48 幾乎常數（縮法 ≈ 1/√N）。先驗①方向 ✓、幅度小於推估（N＝3 推 25～30 實 16.5；N＝2 推 30～35 實 21.1）。⚠ relvol（R1）是決定性規則 ⇒ 200 種子同一條曲線，p10＝p90，「離散度」對它無定義。
5. **4-B（描述，AND N＝8）**：d＝1／2／3／∞ 年化中位 28.1／27.1／26.2／26.2%（p10～p90 全部重疊）；推遲進場 24／3／0 筆、推遲中位 1 天、b_expired 12／0／0 ⇒ **d 在 AND 上幾乎不咬**（每月中位 14 筆、槽 8）。先驗③ ✓（d ∞→1 差 +1.9，N 8→3 差 −3.1）。S 集合：d＝1 21.1% vs ∞ 24.3%（−3.2 pp；p10～p90 12.9～30.3 vs 17.6～33.0）、推遲 84、b_expired 666 ⇒ 密集訊號上 d＝1 有代價，但落在離散度內。
6. **R1（relvol）對 null——⚠ 這是 4-A 的描述，⛔ 沒有登錄為檢定**（本線補算 `resultsp1/r1_vs_null.csv`：R1 年化落在同組態 null 200 種子分佈的百分位）：AND 11 組態 R1 落在 p83～p100，**5／11 ≥ p97.5**（N5、N8、N8 d1／d2／d3）；S 集合 p10～p83、0／11 ≥ p97.5。⇒ 否證②（R1 − null 含 0 ⇒ 測不出）在 S 上成立；在 AND 上超出 null 95% 帶的組態佔一半——⛔ 但 (i) 4-A 登錄為描述、(ii) 11 組態是同一批 2,199 筆訊號、高度相關、(iii) 沒有樣本外、(iv) 先驗④押的是含 0 ⇒ **只能寫「觀察，不建議」**。要當發現，得另登錄（例如 PREREGP1b：R1 對 null 的百分位判準事前寫死、新種子、A／B 窗各算）——由策略線起草，本線不自開。
7. **4-D**：登錄字面「其中為正的比例」＝逐筆勝率：AND 中位 52%（p10～p90 50～54%）、S 47%。⚠ 這跟「做 20 筆 ~89%（獨立重抽上界）」不是同一個量（那是「20 筆**累計**為正」）⇒ 本線補算（`resultsp1/m_windows.csv`，代表種子、進場順序、連續 m 筆淨報酬**簡單加總** > 0 的比例，**回測線定的**）：AND m＝20 在 N3／N8／N10／N40 ＝ 63％／71％／75％／76％，m＝50 ＝ 82～91％；S N8 m＝20 74％。⇒ 真值 63～76％ 低於獨立重抽的 89％（先驗⑤方向 ✓）；N＝3 實 63％，低於策略線押的 70～85％（✗）。
8. **先驗核對**：策略線 ① ✓（幅度小）、② ✓、③ ✓、④ ✓（H60；H20 見第 3 點）、⑤ 方向 ✓ 數字 ✗；回測線補的三條：(c) 佔擋掉組大宗 ✗（AND 上 (a) 才是大宗，(c) 只有 0～230 筆）、R1 − null 含 0 ✗（AND 上 5／11 超出）、d＝1 的 b_expired < 10% ✓（12／2,199）。
9. 結論檔 `resultsp1/CONCLUSIONS.md` 依登錄九節由**策略線**寫（信箱寄來、本線 commit）；本追加只列數字與核對。

## 追加二（2026-09-14 11:58；本文未改）

1. **主格的寫法訂正**（K線分析 1051 五節）：追加一第 2 點「22 格、測得出 0 格 ⇒ 測不出」要讀成 **「20 格測不出 ＋ 2 格還沒測」**——N＝2 的兩格擋掉組有效月數 18 < n_min 24（方法論第四十九條③）。策略線 10:50 版 CONCLUSIONS 〇節寫的是「22 格、測得出 0 格」、七節⑥另記 N＝2 還沒測；訂正走信箱由策略線發，本線不改 repo 那份。
2. **H20 那 8 格的密度證據**（K線分析 1051 六節要的實測，回測線補算，`resultsp1/and_signals_p1.csv.gz`＋代表種子 blocked 檔）：AND 2,199 筆按「同一進場日訊號數」分桶，H20 平均：1 筆日 +4.92%、2 筆 +3.75%、3 筆 +4.39%、4～5 筆 +3.11%、6～10 筆 +2.82%（>10 筆只有 44 筆，+11.1%，不算）；H60 沒有單調關係（10.2／10.7／13.6／14.1／8.0%）。代表種子裡進場組的當日訊號數中位 1、被 (a) 槽滿擋掉那組中位 3～4（N＝3／5／8 都是）；(a) 組 H20 平均 −0.6／−1.8／−0.0%，⛔ 比同密度桶的平均（+2.8～+3.1%）還低 ⇒ **密度效應只解釋一部分**（約 2 pp），其餘在 CI 雜訊內（8 格彼此高度相關、同一批訊號）。⇒ 一般性結論照 K線分析 1051 六節的措辭：「被擋掉的那批即使排除 (c) 仍不是隨機樣本——槽滿與訊號密度相關、密度與後續 20 根報酬相關」，⛔ 但本資料只支持「部分」，不支持「全部由密度解釋」。
3. **R1（relvol）**：K線分析 1051 二節裁「本設計測不出、停止排隊、不開 PREREGP1b（重開條件＝第三個訊號集獨立出現同方向）」；策略線 1055 四節裁「開，主格換 S」——兩線裁定相反，⛔ 本線不裁：策略線起草的登錄要過 K線分析過目，過了本線才執行。在那之前 R1 維持「觀察，不建議」。
4. 策略線 1055 三節三處揭露：Q＝5 語意＝「被 d 擋下後最多再排 5 個交易日」✓；seq=3 時序 ✓；0050 基準兩份不同窗（2017-02-13 vs 2016-01-12）✓——複述無誤。
5. 第五十二條：策略線自查 PREREGP1 沒命中，我同意；4-A 那條「1/√N 參照值」是事前寫在登錄裡的先驗，不是門檻。
