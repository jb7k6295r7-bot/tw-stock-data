# 資料快照腳註：分支 `data/` 把 7 檔 `-DR` 誤標成 `kind=='stock'`

> **裁定依據**：裁定線 20260924-2051（seq 85）§一①②　—— ⛔ **不重跑**，只加一行腳註
> （逐字：「形狀同停牌 44 筆：很小聲，但要標」）
> **回測線交件**：20260924-2044（溯源）／本檔（落地）
> **程式**：`backtest/dr_snapshot.py`（溯源，只讀）、`backtest/dr_footnote.py`（落地，冪等）

---

## 一、事實

```
本線 worktree      /home/chemtim/tw-p17/data/meta/stocks.csv
分支               claude/stock-analysis-backtest-iv9xji
檔案 blob sha      cb01b28f0c4fb83afe503f37e2dcd24bb4b61834
最後動到它          57ab95643（2026-09-19 18:18 UTC）
快照時點            last_seen 欄最大值 ＝ 2026-09-18
```

那一份裡這 7 檔存託憑證的 `kind` 都是 `stock`（第 2963～2987 行）：

```
9103 美德醫療-DR   9105 泰金寶-DR   9106 新焦點-DR   9110 越南控-DR
9136 巨騰-DR       9157 陽光能源-DR  9188 精熙-DR
```

`origin/main`（f98304f7c）上這 7 檔全是 `dr`。變動點逐版查出來是
**`75ff13d98`（2026-09-18，`stock`）→ `d5b2094e1`（2026-09-21，`dr`）之間**。

## 二、後果（⭐ 量出來的，⛔ 不是估的）

`backtest/data.py:48` 的 `load_universe()` 條件是 `kind == 'stock'` ∧ `market ∈ {twse, tpex}`：

| 樹 | 母體檔數 | 其中名稱含 `-DR` |
|---|---:|---:|
| `origin/main` f98304f7c | 2,126 | **0 檔** |
| 本線分支快照 cb01b28f | **2,131** | **7 檔** |

而 7 檔裡**真的進到 P4 面板 `eligible`** 的只有兩檔：

```
9103  面板 135 列｜eligible True 15
9105  面板 135 列｜eligible True 35
9106／9110／9136／9157／9188 ⇒ eligible 恆 0（⛔ 從來沒進過任何一格）
⇒ 合計 **50 股-月**，占 eligible 全體 59,718 股-月的 **0.0837%**
```

⭐ 這同時解釋了 D4 seq17 §4-3 為什麼要剔**恰好兩檔**：
2,039 母體 ＝【判定窗 [523,2835] 內曾 eligible 的 1,586 檔】∪【新增普通股 453 檔】
⇒ 9103／9105 從「曾 eligible」那一半進來 ⇒ `elig_d4` 剔 2 檔 ⇒ **2,037**。

## 三、裁定的三條處置

```
① D4 ⇒ ⛔ 不重跑。出口 A 取決於 S1′ 對 0050 兩腳都沒過，而 S1′／S0_pool 已剔掉那兩檔
   ⇒ 只在 Δ_mkt(甲) 那一欄加腳註（台股策略線 D4 結案措辭 seq=2 已加）
② P4 起所有用分支 data/ 的交件 ⇒ 同一行腳註，⛔ 不逐件重跑 ⇒ **本檔 ＋ 下面 §四 的 51 個 .md**
③ 今後在分支上跑 ⇒ 二選一寫進登錄：
   (甲) 先把 data/ 同步到 main　　(乙) 明文排除名稱含 `-DR` 的
```

⭐ 權威歸屬（裁定線 seq85 §三③）：**`backtest/` 以分支為準；`data/` 以 main 為準。**

## 四、腳註加在哪裡（範圍是量出來的）

⛔ 本線不用「猜哪些件受影響」的方式決定範圍。做法：把每個 `results*` 目錄下所有
`.csv/.md/.gz/.json/.txt` 掃過一遍，看**有沒有真的出現那 7 檔的代號**。

```
共掃 56 個目錄、369 個檔 ⇒ 真的出現 DR 代號的 **31 個目錄**
⇒ 這 31 個目錄下的 **51 個 .md** 各加一行腳註
⇒ 落檔 backtest/results_step2/dr_footnote_scope.csv（56 列，含 0 命中的那 25 個目錄）
```

⚠ 誠實交代三件：

```
① 「代號出現在檔案裡」⛔ 不等於「它影響了結論」——
   它可能出現在剔除名單或診斷表裡（例如 results_d4 的 blocked_453.csv）
   ⇒ ⭐ 所以腳註的字面只說「母體來自那份快照」，⛔ 不說「結論被污染」
② 反過來，「代號沒出現」也只表示【以本次掃描為準】沒出現 ⇒ 那 25 個目錄仍列在 scope.csv 裡備查
③ ⛔ 本支只動 .md，⛔ 不動任何 .csv ——
   ⭐ 因為 handover_check.sh 的逐位元閘門比的是 `resultsp16/*.csv`（15 個）
   ⇒ 先查過那道閘門的範圍才動手，⛔ 不是動完才想起來
```
