# PREREG18：研究十八 快速噴出後該不該提早停利＋收盤位置 p 在突破根上（回測線登錄 2026-09-14 05:15）

來源：K線分析 09-13 1020 一節（O'Neil：一般 +20～25% 停利，但三週內就漲 20% ⇒ 至少觀察八週；與研究十四「等不到拉回那批 +22%」同一個問題的相反答案）、0955（敏感度 G 收盤位置 p；研究十六在 B3 回抽確認根上測不出，K線分析 1438 同意改在突破根上測）。我讀信箱的時戳：04:42。⛔ 跑前 commit；跑完只追加。本題**沒有任何數字產生過**。

## 〇、訊號集與資料

- 訊號集：**研究十一 3/5 主格**（`results11/signals.csv.gz`，cell `30|3|20`，24,286 筆；本研究再套 `liq ≥ 5,000 萬`（`liq` 欄＝前 20 根成交金額均值）與研究十五同口徑，⚠ 研究十一原本的流動性口徑不同，寫在這裡）。訊號根 k ＝ 突破型訊號當根（C1 近 20 根 ≥ 30%、C3 量 ×3 …），正是 0955 講的「突破那一根」。
- K 棒 `research11.load_bars`；進場 `open[k+1]`（研究十一 P1）；對帳：無特別處理版的 H60 報酬要與 signals.csv.gz 的 `g_H60` 逐筆相等。
- 成本 0.585%。基準：研究十一同月母體（`results11/baseline_months.csv`）。

## 一、快組／慢組（1020 逐字，交易日換算寫死）

```
快組 ＝ 進場後 15 個交易日內（t ∈ [k+1, k+15]）有 close[t] ≥ entry × 1.20
慢組 ＝ 沒有
⚠ 用收盤不用盤中高點（與本庫「盤中穿越不算」一致；盤中版當敏感度另報一格）
```

## 二、出場規則（對兩組都跑；t* ＝ 第一次 close ≥ entry×1.20 的根，慢組若在 15 根後才觸及也照算，永不觸及 ⇒ X1／X2／X3 退化成 X0）

| 規則 | 出場 |
|---|---|
| X0 完全不特別處理 | 主格 H60：`close[k+60]`（另報 H120） |
| X1 觸及 +20% 當天出場 | `close[t*]` |
| X2 觸及後再持有 40 根（≈ 八週） | `close[t*+40]` |
| X3 觸及後改用 MA50 追蹤 | t* 之後第一根 `close[u] < MA50[u]` ⇒ `close[u]`；上限 `t*+120` |

- 乾淨窗 [進場, 出場] 內含壞根 ⇒ 無出場（同研究十五）。
- 比較（同筆配對、月分群 CI）：快組 X2−X1、X3−X1、X0−X1；慢組同三個 ⇒ 6 格。
- 判讀（1020 逐字）：X2 或 X3 顯著優於 X1 ⇒ O'Neil 那條成立、與研究十四方向一致；X1 最好 ⇒ 研究十四要重新解讀。慢組一起報，分辨「快組特性」還是「整體都適用」。

## 三、p 在突破根上（0955 規格搬到研究十一訊號根）

p ＝ (close[k] − low[k]) ÷ (high[k] − low[k])；G1 ≥ 0.618、G2 ≥ 0.5、G3 ≥ 0.75、G4 ≥ 0.80；各兩版 (a)、(b) `high − low ≥ 0.5 × ATR20（簡單平均）`；`high == low` A 案 p ∈ {0,1}、B 案另列鎖死。報酬用 H20（主判定）／H60；每格對補集 ＝ 月分群 bootstrap（`research16.boot_diff`）。⇒ 8 格（H20）。⚠ 3/5 訊號根常是漲停根（C2 條件），鎖死筆數會比研究十六多很多，B 案必報。

## 四、格數與虛無期望

二節 6 ＋ 三節 8 ＝ **14 格，虛無期望 0.7**。⛔ 與研究十七／十九不合併。

## 五、事前預期

- 回測線：快組 X2 − X1 為正、測得出（+3～+6 pp；研究十四那批 +22% 是同一族）；X3 − X1 正但小於 X2；慢組三格含 0；p 八格含 0（研究十六 G 的重複）但鎖死那群（一字漲停）報酬高於其餘——那是 C2 條件的機械結果，不當 p 的證據。
- K線分析（1020）：X2／X3 優於 X1（O'Neil 成立）。

## 六、不做

不掃 20%／15 根／40 根以外的參數；不做組合層；不改研究十一任何一格；不把 p 的門檻掃到四個以外。

## 追加一（2026-09-14 05:10，跑完之後寫；本文未改）

1. 跑時 14 秒；`research18.py`；結果 `results18/`；對帳 9,278 筆逐筆相等。
2. 二節寫「永不觸及 ⇒ X1／X2／X3 退化成 X0」，程式實作是**未觸及者 X1～X3 記空值**、配對只取觸及者；慢組的三個配對因此是「日後觸及」條件下的比較（5,858／7,933），本文的退化寫法沒用到，讀法寫在 CONCLUSIONS 一節。
3. 14 格測得出 6（快慢組六個配對），p 八格 0；鎖死群（B 案另列）+6.68 測得出但不在登錄格數內、也不當 p 的證據。
4. 事前預期核對在 CONCLUSIONS 三節：回測線 4 中 1 錯（慢組不含 0）；K線分析 1 中。
