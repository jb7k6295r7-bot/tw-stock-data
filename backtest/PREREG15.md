# PREREG15：研究十五 纏論第三類買點（K線分析 v2 登錄 2026-09-13 09:00；回測線執行）

K線分析登錄的全文在跨線信箱 `PREREG15-研究十五_纏論第三類買點_v2_20260913-0900（全文・回測線執行）.md`（Drive id `109AsvOYA7tda4IS5PbezuoGWVfOeTZ3B`），本檔是**執行者的落地版**：逐條把她的定義對到程式，寫死她沒寫到的實作細節。⛔ 跑前 commit；跑完只追加。

## 〇、這測的不是纏論

單層日 K、不遞迴、不背馳、線段不實作（筆當次級別）。結論只寫「本簡化版的第三類買點測得出／測不出」。只測第三類。

## 一、機器定義 → 程式（`backtest/chan.py`）

| 她的條文 | 程式 |
|---|---|
| 1.1 包含處理：方向比前一根非包含 K 的高點，更高向上、更低或相等向下；第一根當向上 | `merge_bars`：合併 K 棒 [-1] 高點 > [-2] 高點 ⇒ 向上；否則向下；只有一根 ⇒ 向上 |
| 1.2 分型：頂＝中間根高最高且低最高；底鏡像；確認日＝第三根 | `fractals`：確認日 ＝ 第三根合併 K 棒的**最後一根原始 K 棒** |
| 1.3 新筆（主格）：①處理後不共用 K；②頂最高 K 與底最低 K 之間（不含）原始 K ≥ 3 | `pens(mode="new")`：①中間根索引差 ≥ 3；②後一分型中間合併 K 的**第一根原始索引** − 前一分型中間合併 K 的**最後一根原始索引** − 1 ≥ 3（⚠ 用合併 K 的原始邊界代替「最高／最低那一根」，差最多 1～2 根，寫在這裡） |
| 1.3 老筆（敏感度 B）：頂底之間至少 1 根獨立 K | `pens(mode="old")`：中間根索引差 ≥ 4 |
| 取代規則：同向分型無反向分型隔開者取更極端 | `pens`：同型更極端者取代端點 |
| 筆的完成確認日 ＝ 下一個反向分型的確認日 | `ends[k]["confirm_raw"]` ＝ 端點 k+1 分型的確認日 |
| 1.4 主格中樞：五筆 b1..b5 全部與 [ZD,ZG] 有交集；ZG＝min(b1.high,b2.high)、ZD＝max(b1.low,b2.low)；確認日＝b5 完成確認 | `centers_and_signals(n_pens=5, zg_mode=2)` |
| 敏感度 C：ZG／ZD 用三筆全部 | `zg_mode=3`：ZG＝min(b1..b3 high)、ZD＝max(b1..b3 low) |
| 敏感度 D：三筆中樞 | `n_pens=3`（⚠ 結論要標「≡ 突破前高後回檔不破前高，研究二已測過」） |
| 中樞之後的筆與 [ZD,ZG] 有交集者延伸中樞 | 延伸不設筆數上限（她的條文沒寫上限；v1 我寫 9，改掉） |
| 1.5 S2 離開筆：中樞最後一筆**之後**的上升筆，終點 > ZG | `k` 從 `s + n_pens` 起，⛔ 不可以是中樞內的筆 |
| 1.5 S3 回抽筆：下降筆，終點 ≥ ZG ⇒ 第三類買點，訊號日＝S3 完成確認日；終點 < ZG ⇒ 放棄組③ | `B3` / `ABANDON`，`lo2 >= zg` |
| 對照組②：向上離開就進場 | `C2`：中樞確認日之後**第一根收盤 > ZG 的原始 K 棒**為訊號日（她沒定義時點；取最早、無前視的那一版，寫在這裡） |
| 進場 ＝ 訊號日後次一可進場 K 棒開盤 | 有效 K 棒序列上 `k+1`，且 `open` 有限 > 0 |

## 二、母體、閘門、資料口徑

- 母體：`load_universe()`（上市櫃普通股、含已下市、興櫃不在），排除 `data/meta/industry.csv` 產業碼 17（金融保險）、22（生技醫療）；2016-01-04 起的訊號（序列從 2015 起算，敏感度 A 改 2016 起算）。
- 前置閘門：處置期＋後 5 日（`data.disposal_mask`）、注意股當日（`data.load_attention_dates`）、硬斷點（`data.breakpoints`＋`applies`）、樣本不足（有效 K 棒 < 260 根整檔不算）。⚠ 停止融資融券：資料庫沒有逐日旗標（`stocks_margin` 無 note 欄），**不擋**，寫明。
- 流動性：訊號日前 20 根有效 K 棒成交金額均值 ≥ 5,000 萬（主格）；無閘門另報。
- 資料口徑：`data.load_stock()` 還原價（她第二節逐字收了我的公式）。訊號記 `has_adj`。
- 乾淨窗：只查 [進場根, 出場根]：內含壞根（斷點／洞 ≥ 5 根／幽靈事件）⇒ 該筆該持有期無出場。報砍掉 %、砍掉那批的產業分佈與「窗內有無除息事件」對留下那批的差。

## 三、出場

H20（主判定）、H60（第二欄）、H120（照報不當主判定）、A2、A3（Wilder-14，`research11.cond_exit`）。成本 0.585%。

## 四、對照組與安慰劑

① 母體基準：同月、所有通過閘門的有效 K 棒（次開進、H 收盤出）；② C2；③ ABANDON；④ 安慰劑 A（進場平移 +7 個交易日，映到次一可進場 K 棒）、安慰劑 B（同月同數量，從①的股票日隨機抽，seed 20260913）。報「主 − B」（全部超額）與「主 − A」（時點部分）及時點佔比。

## 五、敏感度

A 序列起點（全歷史 vs 2016 起）、B 老筆／新筆、C ZG／ZD 二筆／三筆、D 五筆／三筆。各從主格單獨翻一個開關。任一翻轉 ⇒ 不進建議層。

## 六、門檻與報表

去重 20 根貪婪（與不去重都報中位）；A／B 兩段（2021-01）；月分群 CI；六關同過才進建議層；每格 n／中位／平均／勝率／p10／最壞／CI；主組過統計層才做組合層（5／10／20 × 200 種子，對 0050，逐日市值）。結論檔第一句 ＝ 組合層對被動基準。

## 七、事前預期

- K線分析：主組 ≤ 對照組②，或差不出來。
- 回測線：同上；另預期五筆中樞讓訊號數比三筆版少一半以上；閘門砍掉六成訊號但期望值不掉（研究十一經驗）。

## 八、不做

不測第一、二類；不遞迴；不掃參數；不因結果改門檻。

## 追加一（2026-09-13 09:45，跑完之後寫；本文未改）

1. 六關同過 ⇒ 進建議層；組合層 6 格 0 格贏過 0050（+24.6%／−34.0%）。結論檔第一句照七節寫組合層。
2. 去重 20 根對 B3 沒砍掉任何一筆（B3 天生間隔 > 20 根）⇒ 去重／不去重兩表相同。
3. 乾淨窗砍掉 H20 1.0%／H60 3.0%／H120 8.3%；砍掉那批 100% 窗內有還原事件（壞根多半就是事件日），產業分佈與留下那批相同。
4. 安慰劑「時點佔比」H20 ＝ 201%（主−A 1.37 > 主−B 0.68）：比例 > 100% 的讀法 ＝ 選股那一半 ≈ 0 或負、超額幾乎全在時點；H60 54%、H120 23%。
5. 事前預期核對：K線分析「主 ≤ ②」✗（主贏②三個持有期）；回測線「五筆比三筆少一半以上」✗（13,384 vs 17,298，只少 23%）；「閘門砍六成、期望值不掉」半對（砍 55%，但期望值**掉**：無閘門 +1.47 vs 有閘門 +1.15 pp）。
6. 三處落地裁定（新筆原始 K 用合併邊界、C2 ＝ 中樞成立後第一根收盤 > ZG、停止融資只擋 `O`）已去信 K線分析，若她改判則重跑並追加二。
7. `eligible.csv.gz`（62 MB，安慰劑 B 抽樣池）不進 git。
8. ⛔ 二節寫「停止融資融券：資料庫沒有逐日旗標，不擋」是寫 PREREG 時還沒查到 `stocks_margin` 的 `note` 欄；程式實際**擋了 `note` 含 `O`（停止融資）的訊號日**、不擋 `X`（例行停券期）。本文不改，以此條為準（程式與 CONCLUSIONS 一致）。

## 追加二（2026-09-13 14:08；本文未改）

1. ⛔ K線分析 1032 那封的「外部先驗登錄段」（Sperandeo 2B、468 檔美股 2010–2020、打敗基準約 33%）：她 1220 自行宣告無效（09:50 結果已寄出，比那封早 42 分鐘）。本檔**從未**加入那一段（我 13:58 才讀到信箱），此處記為**事後補記**：那份外部證據於結果產出後才引入，不得作為事前預期；內容可在討論引用，須註明「事後」。
2. 三處落地裁定 K線分析 1220 全數同意。她要求補寫的兩條：(a) 包含處理的方向只用「前一根合併 K 棒的高點 vs 再前一根」（`chan.merge_bars` 第 31 行 `mh[-1] > mh[-2]`），逐根往前遞推，⛔ 沒有用到之後的 K，因果安全；(b) 停止融資只擋 `note` 含 `O`，⛔ 不擋 `X`：X 是每年例行停券期，集中在股東會與除權息前後，擋它會引入綁在除權息上的季節性偏誤。以後不准「順手」補上 X。
3. 「無閘門那格更好」的 CI 在 `results16/CONCLUSIONS.md` 四節：對基準敏感，不寫成結論。
4. 0925 要的兩件：D 三筆版提高報告層級 → `results15/CONCLUSIONS.md` 追加一；「三部曲沒完成」那批 → 研究十六二節（C2\|沒完成 507 筆，H20 −10.8 pp）。

## 追加三（2026-09-14 04:50；本文未改）——資料側三件事，會動到閘門的適用範圍，不動數字

1. ⛔ **停止融資閘門只對上市生效**（情報分析 09-14 04:13 的測量：`otcmargin` 的 `note` 欄 2026-09-10 才出現，之前 11 年半整欄不存在）。本線在分支資料上驗過：`data/stocks_margin/` 有 `O` 的上櫃股 81 檔、全部只在 2026-09-10 那一天；上市 242 檔、2015 起就有。⇒ 研究十五「閘門全套」那句要讀成「處置＋注意＋硬斷點＋樣本不足全市場生效；停止融資只在上市生效，上櫃 2016–2026-09-09 的訊號沒有被這道閘門擋過」。⚠ 對數字的影響：上櫃 B3 只有 09-10／09-11 兩天可能被擋，可忽略；但 PREREG15 二節那句「停止融資：擋 `O`」的**適用範圍**要縮。⛔ 前瞻紀錄（AND×H60）的規則沒有這道閘門，不受影響；但情報分析提醒的「前瞻與歷史落在欄位存在與否兩側」對任何日後用 O 閘門的上櫃研究都成立，要在 PREREG 寫明起算日。
2. ✅ 「三個獨立傳統同一個形狀」可以恢復當提升 D 三筆版位階的理由（K線分析 09-14 03:24 重查：判準④出自一般圖表型態文獻、早於詩魂材料進庫一週；纏論自成體系）。引用時附她指定的那句：「獨立性於 2026-09-14 事後查證，來源各自發展成立；但當初主張時未經查證。」⚠ 三者只證到來源各自發展，不是三次獨立實測；Minervini 不算第四個。
3. F2 那 21 筆的最終狀態（CODE 09-14 03:36 的測量）：有官方漲跌停數字的範圍內「我方因子錯」**0 筆**（12 筆在官方漲跌停之內、3 筆無漲跌幅限制 ETF 的真行情、6 筆上櫃未證實）。⇒ 本研究把它們當壞根剔除**沒有錯掉任何一筆好資料的證據**，但也沒有證明它們該剔除；維持剔除（判準是比值，不是「因子錯」）。⚠ 另：main 上 `data/adj/` 的 `cum_factor` 於 09-14 03:2x 全庫重算（官方 ref 捨去到分 ⇒ 改用 `前收 − 權值息值`；2,357 檔、最大 +2.99e-02，2330 +5.8e-05；換過市場的 4 檔去重 bug 修掉、cum 掉 19～43%）。本研究與研究十六跑在分支快照（`data_sha` 見各 CONCLUSIONS 檔頭），⛔ 沒有吃到這次重算；重跑會另起追加，不改本文。

## 追加四（2026-09-14 08:02，在 main 的新 `cum_factor` 上重跑；本文未改）

- 資料庫線 09-14 0658 說 `data/adj` 穩了（唯一一次重算＝因子改用未捨入參考價，2,357 檔）⇒ 本線 `git checkout origin/main -- data/adj`（921 檔有變）後整份重跑，輸出另開 `results15_adj0914/`，⛔ `results15/` 原封不動。程式一字未改（`research15.load_margin_o` 只多了缺欄回報）。跑 597 秒。
- 結果：**結論不變**。B3 訊號 5,525 → 5,528；主格 H20 +1.15 → +1.16 pp（CI +0.69～+1.63）、H60 +1.87 → +1.86、H120 +3.02 → +3.00；主−② +1.83 → +1.84；放棄組 −0.94 → −0.95；五個變體全部 ±0.02 pp 內；六關照樣全過；組合層六格照樣沒贏 0050（年化中位差 ±1 pp 內）。
- ⚠ 唯一超過 0.1 pp 的差在**安慰劑 B**（同月同數量隨機股票日）：H20 主−B +0.68 → +0.82、H60 +1.84 → +2.03、H120 +3.36 → +3.10 ⇒ 時點佔比 201％／54％／23％ → **170％／49％／25％**。成因是母體池差了幾列，隨機抽到的股票日不同（同一個種子）⇒ 這是安慰劑 B 自己的抽樣雜訊（單次抽樣），⛔ 「時點佔比」那三個數字要讀成 ±30 pp 的量級，不是精確值；結論（H20 幾乎全在時點、H120 大半在選股）不變。

## 追加五（2026-09-14 11:58，上櫃停止融資閘門補齊後重跑；本文未改）

- 資料庫線 run 144 把 `universe/otcmargin` 的 `note` 欄回補到 2,850／2,850 日檔，並於 11:29 派 transpose ⇒ `data/stocks_margin/` 上櫃檔有 O 的從（09-10 前）約 81 檔變 **275 檔**（全部 573 檔）。本線 `git checkout origin/main -- data/stocks_margin data/universe/otcmargin` 後整份重跑，輸出 `results15_otcO_0914/`（與追加四同一份 `data/adj`，只差這道閘門；633 秒）。追加三第 1 條「停止融資閘門只對上市生效」**自此不再成立**。
- 主格：B3 過閘門去重 5,528 → **5,432**（少的 96 筆裡上櫃 40 筆是閘門擋的、其餘是合格股票日母體縮小後去重的變動）；H20 +1.16 → **+1.18 pp**（+0.71～+1.65）、H60 +1.86 → +1.90、H120 +3.00 → +3.10；主−②、放棄組、五個變體、六關、組合層六格結論全部不變。
- **上櫃段**（月分群 CI，基準＝同市場合格股票日同月均值）：

| 上櫃 B3 | 訊號 | 合格股票日 | H20 超額 | H60 超額 | H120 超額 |
|---|---:|---:|---:|---:|---:|
| 閘門空轉（追加四） | 1,701 | 309,127 | +0.66（−0.14～+1.47） | +1.91（+0.45～+3.36） | +3.65（+1.39～+5.92） |
| 閘門生效（本追加） | 1,661 | 302,712 | +0.70（−0.11～+1.51） | +2.04（+0.56～+3.51） | +3.99（+1.68～+6.31） |
| 上市（兩趟相同） | 3,827 | 799,918 | +1.43（+0.93～+1.94） | +1.89（+1.10～+2.67） | +2.78（+1.40～+4.16） |

  ⇒ 被上櫃停資閘門擋掉的 40 筆略弱於留下的（H120 +3.65 → +3.99），方向與上市那道閘門在研究十五一節看到的「過閘門反而變差」**相反**；量都在 CI 內，結論不變：上櫃 H20 仍含 0、H60／H120 仍測得出。
