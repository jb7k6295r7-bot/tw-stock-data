# 四型的【圈號沿革註記】（⛔ 引用 2026-09-20 以前的回測線報告之前先讀這一頁）

依 K線分析線 2026-09-20 2155 §一【裁 (乙)】立此註記；
⭐ **逐字內容依 2230 §〇-3⑤ 定版**（2230 訂正了 2155 §一 的一句，見第五節）。

```
⚠ 2026-09-20 22:30 之前，`centers_v3.json` 的標籤字串、`TYPE_OF_IDX`、與 `resultsp1` §十二 的 ②／③ 圈號，與本專案其餘文件相反。⛔ 引用那些來源的舊內容時，②／③ 要對調；⭐ 而【型名本體】全庫一直一致 ⇒ 依型名讀就不會錯。
```

<details><summary>⚠ 2155 §1-4 ② 的第一版逐字（⛔ 已被上面那段取代，留著是因為沿革本身是內容的一部分）</summary>

```
⚠ 2026-09-20 21:55 之前，回測線側檔案與報告中的 ②／③ 圈號與本專案其餘文件相反（回測線側：②＝正在噴出、③＝純技術+回檔）。⛔ 引用 2026-09-20 以前的回測線報告時，②／③ 要對調；⭐ 而【型名本體】兩側一直一致 ⇒ 依型名讀就不會錯。
```

</details>

---

## 一、⭐ 訂正後的定版對應（⛔ 這一份為準）

| cluster（k-means 索引） | 型名本體 | 圈號（(乙) 定版） | 假說欄 |
|:-:|---|:-:|:-:|
| 0 | 營收＋回檔 | ① | H3 |
| 2 | 純技術＋回檔 | ② | H4 |
| 3 | 正在噴出 | ③ | H2 |
| 1 | 死水 | ④ | H1 |

⭐ **cluster ↔ 型名 ↔ 數字** 三者從頭到尾兩線一致；這次動的**只有圈號字元**。
⛔ ①（營收＋回檔）與 ④（死水）**兩側從來一致** ⇒ 引用它們的結論一個都不必動（2155 §1-4 ④）。

## 二、⛔ 這一次實際動到的東西（⭐ 逐項，⛔ 沒有任何數字改變）

```
① backtest/forward/p4_types/centers_v3.json 的 cluster_index_to_type 兩個圈號字元
   全檔 sha256 前 16：23be85b004977222（09-15 策略線投遞版）→ 1144304f1068094e（本次）
   ⭐ 而【數值本體】（schema／feature_order／cont_cols／bool_cols／mu／sd／centers_z）
     的 sha256 前 16 ＝ dfd5863a6566b6bc，**訂正前後相同**
   ⇒ ⭐⭐ 這正是〈一百一十二〉的落地：全檔 sha 會把【顯示標籤】與【定義】混在一起，
     所以這裡兩個 sha 都留，⛔ 不可以只看全檔那一個就說「中心換了」
② backtest/researchp4.py 的 TYPE_OF_IDX／HYP／TYPES／REF／discrimination 的圈號字元
   ⛔ 行為一格都沒變（⭐ 見下面第四節的鑑別力那一段）
③ 本檔（新增）
```

## 三、⛔ 舊報告一個字都不改（2155 §1-4 ①）——⭐ 而要對調著讀的是這些

```
⚠ 下列回測線側文件**內文不動**，⛔ 而讀它們的 ②／③ 時要對調：
  backtest/P4_v3_回溯分析.md      （⭐ 它同時是本件的【判準】，見第四節）
  backtest/resultsp4/summary.md、summary.csv、placebo.csv、quantiles_wide.csv…（09-20 以前的那幾趟）
  backtest/resultsp4/TYPE_RECHECK.md（21:25 交件；⛔ 它文末「裁定前不動 TYPE_OF_IDX」已由本註記接手）
  backtest/PROGRESS.md 09-20 21:25 以前各節
⭐ 而跨線比對一律依【去掉圈號的型名本體】（2155 §1-4 ③）——
  backtest/p4_type_recheck.bare() 就是那一份實作，⛔ 不要再寫第二份。
```

## 四、⛔⛔ 判準本身就是 (甲) 寫的 ⇒ 程式與判準的圈號【現在不一致，而這是預期的】

`backtest/P4_v3_回溯分析.md`（本件的登錄／判準，⛔ 依 2155 §1-4 ① 不改）逐字是 (甲) 圈號：

```
§189  「鑑別力檢查　把 ④ 與 ② 的標籤對調重跑；若仍「④負②正」⇒ 程式有 bug」
§591  「判定四型不變（① H3 ＋、② H2 ＋、④ H1 −、③ H4 零）」
```

⇒ 那裡的 **② ＝ 正在噴出（H2）**、**③ ＝ 純技術＋回檔（H4）**。
⇒ ⭐ 所以 `discrimination()` 對調的那一對是【死水 ↔ 正在噴出】，訂正後它寫成 `④死水 ↔ ③正在噴出`
   ——**對調的是同一對，行為沒有變**，⛔ 不可以照 (乙) 的字面把它改成「死水 ↔ 純技術＋回檔」。

⚠ ⇒ 回測線據此回報一件事實（⛔ 不是裁定）：策略線 2215 §一 寫「全庫裡與 (乙) 相反的只有兩處」
   （centers_v3.json 標籤字串、resultsp1 §十二）——**至少還有第三處，而且它是判準**：
   `P4_v3_回溯分析.md` §189／§591。⛔ 本線不改它（2155 §1-4 ① 逐字），只在這裡標明要對調著讀。

---

## 五、⭐ 2230 的兩處訂正，與本線要補的一件事實

```
① K線分析線 2230 §〇-1 訂正自己：2155 §一「§十二 那兩個圈號⛔不必互換」是【(甲) 的後果】
   ⇒ ✅ 在 (乙) 之下【§十二 那兩個圈號就是要互換】（策略線 2215 §二 讀得對）
   ⇒ ⛔ 而 §十二 是策略線的文件 ⇒ 走【訂正追加凌駕、原文一字不改】，⛔ 回測線不碰
② 2230 §〇-3 把處置分成兩種，⭐ 而本線這一側屬於【程式／設定】那一半：
   `centers_v3.json` 標籤字串與 `TYPE_OF_IDX` ⇒ ⭐ **直接改、⛔ 不留錯的版本**
   （理由逐字：「一份【還在被執行】的設定，加註記沒有用 —— 它照樣跑出相反的標籤」）
   ⇒ ✅ 本線 2026-09-20 22:40 已照此落地（commit d96483bf0）。

⛔⛔ 而本線要補一件 2230 §〇-3⑤ 的逐字註記【沒有列到】的事實：
   本線側的**舊報告**也是 (甲) 寫的，而它們不在那份清單裡 ——
     `backtest/P4_v3_回溯分析.md` §189／§591（⭐ 它同時是本件的【判準】）
     `backtest/resultsp4/` 09-20 以前那幾趟的 `summary.md`／`summary.csv`／`placebo.csv` 等
     `backtest/resultsp4/TYPE_RECHECK.md`（21:25 交件）
   ⇒ ⭐ 所以本頁第三節那份清單**照舊有效**，⛔ 不因 2230 縮短了逐字註記的範圍而失效。
   ⇒ ⚠ 本線 2235 §三 已把 P4_v3 §189／§591 那一處回報出去（2230 寫於 22:30、本線 2235 在其後）。
```
